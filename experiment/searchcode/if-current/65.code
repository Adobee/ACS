public boolean reachedWall(int goalX, int currentX, int currentY, int goalOrientation, int goalPosition, int goalY) {
if (currentX == goalX &amp;&amp; currentY == goalY)
goalX -= insetX;
goalY -= insetY;
if (goalPosition == 0)
if (goalOrientation == 0) {
if (currentX == goalX - 1 &amp;&amp; currentY == goalY)

