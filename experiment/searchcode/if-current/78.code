//currentWord = wordList.get(loop);
wordList.remove(loop);
//currentWord = currentWord.toLowerCase();
currentChar = currentWord.charAt(loop2);
if (currentChar < 97 || currentChar > 122) {
//if (currentChar != 39 &amp;&amp; currentChar != 45)

