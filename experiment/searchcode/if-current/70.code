public class ProgressiveAgeStrategy implements AgeStrategy{

public int computeNextAge(int currentAge){
if (currentAge < -100){
return currentAge + 100;
} else if (currentAge == -100){
return -1;
} else if (currentAge == -1){

