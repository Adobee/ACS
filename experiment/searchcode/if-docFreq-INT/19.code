Map<String, Integer> docFreq = new HashMap<String, Integer>();
int docCount = 0;
/* For each block */
for (File block : dirlist) {
private static void writeDict(String output, Map<String,Integer> docFreq, int docCount) throws IOException {
BufferedWriter docWriter = new BufferedWriter(new FileWriter(new File(output, &quot;docFreq.dict&quot;)));

