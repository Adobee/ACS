public Type getApplyType (Type t1, Type t2) {
if (!canApply(t1, t2)) {
return Type.ERROR;
Type t1 = v1.getType();
Type t2 = v2.getType();
if (!canApply(t1, t2)) {
throw new IllegalArgumentException();

