_base = base;
_sizes = sizes;
}

public static boolean MatchArray(TypeArray t1, TypeArray t2)
{
if(!MatchType(t1._base, t2._base))
return false;
if(t1._sizes.length != t1._sizes.length)
return false;

