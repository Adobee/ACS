BLOCK, INLET, RIVER, DRAWN;

public void draw(Graphics g, HexEdge e, double scale, boolean force) {
if (!force &amp;&amp; this != DRAWN) return; // draw nothing
private Stroke getStroke(float size) {
if (size != savedSize) {
savedStroke = new BasicStroke(size, BasicStroke.CAP_ROUND, BasicStroke.JOIN_ROUND);

