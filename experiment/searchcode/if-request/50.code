* Sends {@link konto.model.Request Request} to an {@link konto.model.User User} or to a {@link konto.model.Group Group}
* @param request
* @return {@code true} if a request is sent, {@code false} if a request is already sent but user or group did not confirm the request

