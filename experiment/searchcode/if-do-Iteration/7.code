import lti.alpha.lang.schemeobject.Symbol;

public final class Do extends Drv {
private final Itr[] iterationSpecs;
private final Exp test;
* expr ...) command ...) (letrec ((loop (lambda (var ...) (if test (begin (if
* #f #f) expr ...) (begin command ... (loop (do &quot;step&quot; var step ...)

