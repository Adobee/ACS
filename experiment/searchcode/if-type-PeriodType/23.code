package com.owera.xaps.dbi.report;

import java.util.Calendar;

public enum PeriodType {
ETERNITY(-1), MONTH(Calendar.MONTH), DAY(Calendar.DAY_OF_MONTH), HOUR(Calendar.HOUR_OF_DAY), MINUTE(Calendar.MINUTE), SECOND(Calendar.SECOND);

private int type;

private PeriodType(int type) {
this.type = type;

