public class Solution1_2
{
private long abs( long a, long b )
{
if( a < b )
{
return b - a;
rSum -= A[P - 1];
long diff = abs( lSum, rSum );
if( diff < minDiff )
{
minDiff = diff;
}
}
return ( int )minDiff;

