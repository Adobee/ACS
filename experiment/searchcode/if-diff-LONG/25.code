return text.substring(0, 1).toUpperCase(Locale.US) + text.substring(1);
}

public static String getFancyDiff(Date date)
{
long current = new Date().getTime();
Long hourDiff = minDiff / 60;
Long dayDiff = hourDiff / 24;

if (secDiff < 60)
{
//return String.format(&quot;%s %s&quot;, secDiff.toString(), secDiff == 1 ? &quot;second ago &quot; : &quot;seconds ago&quot;);

