package onePiece.alliance.web.dao.impl;

import onePiece.alliance.web.dao.PermissionDao;
import onePiece.alliance.web.entities.Permission;
Permission permission = em.find(Permission.class, permissionId);
if (permission != null) {
em.remove(permissionId);
}
}
}

