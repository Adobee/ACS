private Drawable applicationIcon;


private LinkedHashMap<String, Integer> permissionMap = new LinkedHashMap<>();
for (String mString : permissionMap.keySet()) {
if (mString.equals(permissionName)) {

