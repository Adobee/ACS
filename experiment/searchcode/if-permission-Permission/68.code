import org.springframework.stereotype.Service;

import com.sourcio.hrsystem.busobj.Permission;
import com.sourcio.hrsystem.dao.PermissionDao;
import com.sourcio.hrsystem.service.PermissionService;
Permission Permission = permissionDao.getPermissionById(permissionId);
if (Permission != null) {
Permissions.add(Permission);
}
}
return Permissions;
}

}

