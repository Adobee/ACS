public int canCompleteCircuit(int[] gas, int[] cost) {
if (gas == null || gas.length == 0) {
return -1;
}

for (int i = 0; i < gas.length; ) {
int left = 0;
int startpos = i;
int endpos = startpos;

