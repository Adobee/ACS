public void modifyValue(int aDelta) {
mValue += aDelta;
if (mValue > mMaximum + mMaximumMod) mValue = mMaximum + mMaximumMod;
}

public void modifyMaxTemp(int aDelta) {
mMaximumMod += aDelta;
if (mValue > mMaximum + mMaximumMod) mValue = mMaximum + mMaximumMod;

