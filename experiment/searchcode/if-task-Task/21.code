for (Task t : taskList) {
if(t.getID() == taskID){
return t;
}
}
return null;
}

/**
* add a task
Task task = getTaskObjectByID(taskID);

if (task != null) {
//stop task and delete it from task list
task.stop();

