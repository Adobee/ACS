package io.github.pcouaillier.taskmanager;
import java.util.List;

public class TaskList {
private List<Task> lTask;
public void addTask(Task task) {
this.lTask.add(task);
}
public boolean removeTask(Task task) {
if(lTask.contains(task)){

