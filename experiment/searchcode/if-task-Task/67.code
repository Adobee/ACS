package com.tianwen.gov.roster.threadpool;
import java.util.TimerTask;

public class TimeoutTask extends TimerTask
public void run()
{
if (taskHandle == null || taskQueue == null)
{
return;
}

switch (taskHandle.getState())

