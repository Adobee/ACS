package org.freedoit.server.util;

import org.freedoit.common.shared.SharedTask;
import org.freedoit.server.om.Task;
sharedTask.getStartTime(), sharedTask.getDueTime(), sharedTask.getFinished(), sharedTask.getTaskTag(),
sharedTask.getStatus(), null, null);

if (sharedTask.getTaskId() != null)
newTask.setTaskId(sharedTask.getTaskId());

