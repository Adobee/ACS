return WSiteCommon.convertUtf8(s);
}

protected Task getTask(String s,int i)
{
Task task = new Task();
if(i==0)
task = ta.getTaskOnDomain(s);
else if(i==1)
task = ta.getTaskOnSiteId(s);
else if(i==2)

