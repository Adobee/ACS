chooseConventionDialog.setVisible(true);

// Si on a appuyé sur OK, on continue.
if (chooseConventionDialog.getReturnValue() == DialogReturnValue.OK) {
editConventionDialog.setVisible(true);

// Si on a validé, on continue
if (editConventionDialog.getReturnValue() == DialogReturnValue.OK){

