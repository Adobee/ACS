import org.apache.commons.math3.linear.RealMatrix;

/**
* Created by nikita on 15.03.15.
*/
public class ConjugateGradientSolver implements LinearSystemSolver {
return solve(MatrixUtils.createRealMatrix(a), MatrixUtils.createColumnRealMatrix(b), eps);
}

private Solution solve(RealMatrix a, RealMatrix b, double eps) {

