final Map<Double,Long> counts = new HashMap<>();
for(double value : doubles)
{
if (counts.get(value) == null) counts.put(value, 0l);
for(int i = 1; i < functions.size(); ++i)
{
double output = functions.get(i).compute(value);
if(output < minimum)

