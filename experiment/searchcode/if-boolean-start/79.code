private boolean start;
private boolean l;
private boolean r;

public JoypadState() {
super();
reset();
this.select = select;
}

public boolean isStart() {
return start;
}

public void setStart(boolean start) {
this.start = start;

