//  dx   is the movement in x direction
//  dy   is the movement in y direction
public void add ( int dx, int dy){
//compute new end point
if (event.isShiftDown())
d=	LARGE_INCREMENT;
else
d=SMALL_INCREMENT;

//add line segment
if ( keyCode== KeyEvent.VK_LEFT)  add(-d,0);

