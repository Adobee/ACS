array[first] = array[i];
array[i] = temp;
}
}

static void InsertionSortShift(int[] array) {
int j;                     // the number of items sorted so far
data[first + i] = temp[i];
}

static void ShellSort(int[] array) {
int increment = array.length / 2;

