double m2 = Min + BI[i] * (j + 1);
for (int m = 0; m < v.length; m++)
if (v[m] > m1 &amp;&amp; v[m] < m2)
ICC[i][j]++;
FA[n][i] = IL[n][i] / AN;
EA[n][i] = ILE[n][i] / AN;
}
}

// NFM calculations
for (int n = 1; n < Nmax; n++) {

