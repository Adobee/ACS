public double[] getX() {
double[] res = new double[min(x.size(), min(y.size(), min(z.size(), t.size())))];
return res;
}

public double[] getY() {
double[] res = new double[min(x.size(), min(y.size(), min(z.size(), t.size())))];

