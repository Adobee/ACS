dfs(new int[n], 0, n);
return count;
}

public void dfs(int[] columns, int row, int n) {
public boolean checkValid(int[] columns, int row, int col) {
for (int i = 0; i < row; i++) {
if (columns[i] == col || row - i == Math.abs(col - columns[i])) {

