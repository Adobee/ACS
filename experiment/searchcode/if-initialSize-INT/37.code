ViewStack(){
this(10);
}
/**
* 初始化栈，声明大小
* @param initialSize
*/
ViewStack(int initialSize){
if(initialSize>0){
private void ensureCapacity() {
int index;
if(top == capacity){
capacity *= 2;
Object[] newData = new Object[capacity];

