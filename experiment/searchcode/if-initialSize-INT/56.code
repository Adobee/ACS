while(fileIn.hasNext() == true)
{
line = fileIn.nextLine();

list.add(line);
}
fileIn.close();

int initialSize = list.size();
for(int i = 0; i < initialSize; i++)
{
for(int j = 0; j< initialSize - i -1; j++)
{
if(getChars(tempArr[j]) > getChars(tempArr[j+1]))

