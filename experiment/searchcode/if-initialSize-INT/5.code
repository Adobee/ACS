public static Point getLocation(Point initialSize, int id) {
Dimension screenSize = Toolkit.getDefaultToolkit().getScreenSize();
if (CENTER == id) {
return new Point((screenSize.width - initialSize.x) / 2,

