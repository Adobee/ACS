private int cycle;

private int initialSize;
private int size;
private int maxSize;
private int cycles;

private Color color;
if(growing) {
if(cycle > cycles) {
growing = false;
return;
}
size = initialSize + (cycle * STEP);

