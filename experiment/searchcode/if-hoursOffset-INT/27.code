return ThreadLocalRandom.current().nextInt(10000000, 99999999);
}

public static int getRandomNumber(int length) {
if (length < 1 || length > 10) {
public static DateTime getSameTimeInUTC(DateTime now) {
String offset = TIME_ZONE_FORMATTER.print(now).split(&quot;:&quot;)[0];
int hoursOffset = Integer.parseInt(offset);

