class SumTask extends Thread {
long[] interval;
boolean last;
int count = 0;
HashMap<Long, Integer> hash;
System.out.println(&quot;Start task with interval: [&quot; + interval[0] + &quot;, &quot; + interval[1] + &quot;].&quot;);
if (last) {
interval[1]++;
}
for (Long t = interval[0]; t < interval[1]; t++) {

