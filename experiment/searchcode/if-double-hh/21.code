public MonteCarlo(double hh, double kk, double rr) //Constructor
{
h = hh;
k = kk;
public boolean insideCircle(double x, double y)
{
boolean retValue = false;
if((x-h)*(x-h) + (y-k)*(y-k) <= (r*r))

