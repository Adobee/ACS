public class Validator extends utilities.validation.Validator
{
public boolean isValid(Object o)
{
boolean isValid = false;

if(o instanceof Function)
{
isValid = super.isValid(o);
}

return isValid;
}
}

