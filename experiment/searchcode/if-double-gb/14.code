private double            ga               = 1;
private double            gb               = 0;
private double            gc               = 80;
gaussianArray[i] = ga * Math.pow(Math.E, -1 * Math.pow(i - gb, 2) / (2 * gc));
}
}

public double sum() {
double sum = 0;

