maze[r][c] = &#39;.&#39;;
}

int minRow = MAX;
int maxRow = 0;
int maxCol = 0;

for (int i = 0; i < MAX; ++i) {
for (int j = 0; j < MAX; ++j) {
if (maze[i][j] == &#39;.&#39;) {
minRow = Math.min(minRow, i);

