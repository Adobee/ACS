//at each level we cut off two elements from both the width and height
int minCol=0;
int minRow=0;
int maxRow=matrix.length-1;
//right
for(int i=minCol;i<=maxCol;i++) printElement(matrix[minRow][i]);
if(minRow > maxRow || minCol > maxCol) break;

