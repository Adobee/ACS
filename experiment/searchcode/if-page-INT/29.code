public static int getTotalPage(int everyPage,int totalCount) {//get total page
int totalPage = 0;
if(totalCount != 0 &amp;&amp;totalCount % everyPage == 0) {
totalPage = totalCount / everyPage;
return (currentPage - 1) * everyPage;
}
public static boolean getHasPrePage(int currentPage) {//get if previous page

