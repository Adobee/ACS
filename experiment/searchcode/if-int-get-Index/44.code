int index = startIndex;
// find token
int newIndex = BasicRules.getLastToken(str, index);
if (index == newIndex)
int index = 0;
int newIndex = 0;

try
{
newIndex = getLastVersion(str, index);
if (index == newIndex)

