phoneId = Long.valueOf(id);
}

Phone phone;

if (phoneId > 0) {
phone = Phone.find.byId(phoneId);
String id = phoneFormData.phoneId;

long phoneId = 0;

if (id != null) {
phoneId = Long.valueOf(id);

