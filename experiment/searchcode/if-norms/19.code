this.norms = new ArrayList<Norm>();
}

/**
*
* @param norm
*/
public void add(Norm norm) {
if(!this.norms.contains(norm))
this.norms.add(norm);
for(Norm norm : norms)
if(!this.norms.contains(norm))
this.norms.add(norm);
}

/**
*
* @param norm

