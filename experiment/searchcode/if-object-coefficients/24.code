printResults(roots);
}

/**
* Input coefficients of quadratic equation.
*
* @return Object coefficients type with coefficients of quadratic equation
* Calculate roots of quadratic equation.
*
* @param coefficients Object coefficients type with properties that is coefficients of quadratic equation.

