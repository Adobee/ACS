first = processMap(map, first, parent, outputStream);
return first;
}

public String buildParent(String parent, String parent2) {
if (parent != null &amp;&amp; parent2 == null) {
first = writeString(first, parent, entry.getKey(), (String) value, outputStream);
} else if (value instanceof HashMap) {
//noinspection unchecked
first =

