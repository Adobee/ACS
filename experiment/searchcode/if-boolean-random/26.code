* Générateur aléatoire suivant une loi uniforme.
*/
public class UniformBooleanGenerator {

private final Random random;

public UniformBooleanGenerator(Integer seed) {
if (seed == null) {
this.random = new Random();

