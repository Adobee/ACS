int width = max - min;
if (min < 0 || width < 0) throw new IllegalArgumentException();

int random;
do {
random = 0;
for (int bit = 1; bit <= width; bit <<= 1) {
if (seed.nextBoolean() )
random |= bit;

