rank = new Vector<Integer>(rankMax + 1);
if (gradRank != roleRank) {
for (int i = 0; i < rankMin; i++) {
for (int i = 1; i < rank.size(); i++) {
output = output + rank.get(i);
if (i + 1 < rank.size()) {

