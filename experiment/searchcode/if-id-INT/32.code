public class ImproveWeight_union {
private int[] id, track;
public ImproveWeight_union(int N) {
id = new int[N];
track = new int[N];
int i = root(p);
int j = root(q);
if (track[i] > track[j]) {
id[j] = i;
track[i] += track[j];
}
}
}

