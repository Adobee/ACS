StdDraw.setXscale(0, N);
StdDraw.setYscale(0, N);
double x = 1.0;
double y = 1.0;
for (int i = 0; i < N; i++) {
for (int j = 0; j < N; j++) {
if (((i + j) % 2) == 0) {
StdDraw.setPenColor(color3);
StdDraw.circle(j+x/2, i+y/2, x/2);

