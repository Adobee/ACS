protected int noOfItems;
protected int cpos;

public List(int max){
noOfItems = 0;
myList = new Listable[max];
}

public boolean canAdd(){
return (myList.length <= noOfItems);
}

public int length(){

