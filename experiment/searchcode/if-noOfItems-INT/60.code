int first = 0;
int last = noOfItems - 1;
boolean moreToSearch = (first <= last);
boolean found = false;
int last = noOfItems - 1;
int midPoint = (first + last) / 2;
boolean found = false;

while (!found){

