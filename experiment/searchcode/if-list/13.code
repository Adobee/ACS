List<List<T>> l = new ArrayList<>();

if(list == null) {
throw new RuntimeException(&quot;null list&quot;);
}

List<T> subL = new ArrayList<>();
for(T t:list) {
subL.add(t);
if(subL.size()==size) {
l.add(subL);

