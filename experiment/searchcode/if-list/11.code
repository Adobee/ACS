List<List<Integer>> resultList = new LinkedList<List<Integer>>();
if (numRows >= 1) {
List<Integer> list = new LinkedList<Integer>();
list.add(1);
resultList.add(list);
}
if (numRows >= 2) {
List<Integer> list = new LinkedList<Integer>();

