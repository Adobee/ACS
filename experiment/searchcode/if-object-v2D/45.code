@Override
public boolean equals(Object o) {
if (o != null) {
if (o instanceof Point) {
bounds = new RectF(x - size / 2, y - size / 2, x + size / 2, y + size
/ 2);
}

public void moveTo(Vector2D v2d) {
point = point.add(point.scalarMultiply(-1)).add(v2d);

