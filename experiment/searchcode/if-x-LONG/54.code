long c = -2*(x+y);
long d = b*b - 4*a*c;
double nd = (-b + Math.pow(d, 0.5))/2*a;
if (Math.round(nd)!=nd) return -1;
if (x==0) return 0;
long n = (long)nd;
if (n==0) return 0;
if (n>=x) return 1;
long count = 0;

