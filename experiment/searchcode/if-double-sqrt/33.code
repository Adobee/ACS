private static double calcualteSquareRoot(double n, double low, double high) {
double sqrt = low + (high - low)/2;
double diff = n - sqrt * sqrt;
if(diff > error) {
return calcualteSquareRoot(n, sqrt, high);

