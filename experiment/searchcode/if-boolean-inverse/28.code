//if the determinant = 0, the matrix does not have an inverse
public static boolean isInvertable(double[][] mat){
boolean inverse;
double determinant = mat[0][0]*mat[1][1] - mat[0][1]*mat[1][0];
if(determinant == 0) inverse = false;

