public static double G(double x) {
return Math.exp(lnG(x));
}

/**
* Calculate logGamma function with Lankzros approximation formula
factor = Math.exp(alpha * Math.log(x) - x - ln_gamma_alpha);

if (x > 1 &amp;&amp; x >= alpha) {
// continued fraction

