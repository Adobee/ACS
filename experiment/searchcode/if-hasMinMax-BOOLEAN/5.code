final int minx, miny, maxx, maxy;
final int gridx, gridy;
final Grid[] grids;
final float [] image;
boolean hasMinMax;
if (!hasMinMax) {
hasMinMax = true;
min = v;
max = v;
} else {
if (min > v) min = v;

