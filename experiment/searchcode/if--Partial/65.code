number = MIN + (int)(Math.random() * ((MAX - MIN) + 1));
System.out.println(number);
int partialSum = 0;
int partialNum = number;
while (partialNum > 0) {
int rem;
rem = partialNum % 10;
if(rem == 0)

