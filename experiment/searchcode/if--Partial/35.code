point.x = point.x - partial[i].x;
point.y = point.y - partial[i].y;
int k;
if (partial[i].x != 0)
{
if (point.x % partial[i].x == 0)
{
k = point.x / partial[i].x;
if (k > 0 &amp;&amp; partial[i].y * k == point.y)

