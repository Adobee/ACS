double dPhiS = Calc.diskDiff(omega, phiS, omegaControllerIndex);
double beta_u = Math.pow(10, -12);
double beta_o = 1;
double beta, dPhiR, dPhiO;
dPhiO = dPhiS + dPhiR;
if (dPhiO * omegaController < -0.5) { beta_u = beta; }
else { beta_o = beta; }}
}

