/* prefetch start in Base64 coordinates */
private long startPosition;
/* prefetch end in Base64 coordinates */
private long endPosition;
this.data = data;
}

public boolean inRange(long checkStartPosition, long checkEndPosition){
if(checkStartPosition >= startPosition &amp;&amp;

