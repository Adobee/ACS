public void update(Position obj);

public Position findById(Long id);

public Position createIfNull(long userId);

public PositionInfo getSmsLocation(Integer type, long userId, Long telephone, User driver);

public void updateSmsLocateState(int smsLocate, Long id);

