List<Value> values) throws TwisterException {
double average = 0;
double avgSquare = 0;
double maxDelta = 0.0;

double[] averages;
average += averages[0];
avgSquare += averages[1];

if (maxDelta < averages[2])
maxDelta = averages[2];

