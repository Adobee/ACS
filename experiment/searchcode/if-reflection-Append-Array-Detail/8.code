public ToStringBuilder append(String fieldName, boolean[] array,
boolean fullDetail) {
style.append(buffer, fieldName, array, fullDetail ? Boolean.TRUE
: Boolean.FALSE);
return this;
}

/**
* <p>
* Append to the <code>toString</code> an <code>byte</code> value.

