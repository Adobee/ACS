int charArrayIndex = 0;
for (int loopIndex = 0; loopIndex < repeatedList.size(); loopIndex++){
int characterCount = repeatedList.get(loopIndex);
charArrayIndex += loopIndex > 0 ? repeatedList.get(loopIndex - 1) : loopIndex;

