package com.android.test.waterfall.utils;

import net.tsz.afinal.FinalBitmap;
import android.os.Environment;
public FinalBitmap getFinalBitmap() {
if (finalBitmap == null) {
finalBitmap = FinalBitmap.create(TestAppLication.getContext());

