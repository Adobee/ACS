package com.android.test.afinaldemo.utils;

import net.tsz.afinal.FinalBitmap;
import net.tsz.afinal.bitmap.display.Displayer;
getFinalBitmap(context).display(imageView, uri, DUtil.getWith(context) / 3, DUtil.getWith(context) / 3);
}

public FinalBitmap getFinalBitmap(Context context) {
if (finalBitmap == null) {

