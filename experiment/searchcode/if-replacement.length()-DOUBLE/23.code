double left = Double.parseDouble(matcher.group(1));
double right = Double.parseDouble(matcher.group(5));
String replacement = null;
if (operation.equals(&quot;*&quot;)) {
replacement = String.valueOf(left * right);
} else if (operation.equals(&quot;/&quot;)) {

