public class Scale {
public int scaleLabel; // num of #s - num of b s
public int scaleVector[];
int c1 = (this.m <= this.n / 2) ? this.m : this.m - this.n;
int scaleLabel = (c1 * k) % n;
if (!(Math.abs(scaleLabel) <= n / 2)) {

