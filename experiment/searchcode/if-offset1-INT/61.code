String k = mid.toString().substring(3, 4);//用于第四位为0时的转换
if (!k.equals(&quot;0&quot;))
{
for (int i = mid.length() - 4; i > -4; i = i - 4)
{//分别以四个为一组
int offset1 = i < 0 ? 0 : i;
int offset2 = i + 4;

