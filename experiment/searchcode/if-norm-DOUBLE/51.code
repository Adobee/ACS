double normA = 0.0;
double normB = 0.0;

for(int i = 0; i < A.length; i++) {
product += A[i] * B[i];
normA += Math.pow(A[i], 2);
normB += Math.pow(B[i], 2);
}
if(normA == 0 &amp;&amp; normB == 0) return 2.00;

