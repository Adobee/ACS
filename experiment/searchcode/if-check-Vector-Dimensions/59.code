return 0;

Vector dimensions = this.dimensions();
double xy = dimensions.getX() * dimensions.getY(),
* Gets the dimensions of the region as a vector.
* @return See the previous line.
*/
public Vector dimensions() {

