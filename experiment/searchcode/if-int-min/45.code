int[] record = new int[A.length];
return getMin(A, A.length - 1, record);
}

int getMin(int[] A, int target, int[] record) {
return 0;
}else{
int min = Integer.MAX_VALUE;
for(int i = 0; i < target; i++){
if(A[i] + i >= target){

