package obj.effects.continuous;

import java.util.UUID;

import obj.Attachment;
import obj.effects.EffectImpl;
* @Override public boolean applyThis() { if (marker != null)
* target_obj.addMarker(marker); return true; }

