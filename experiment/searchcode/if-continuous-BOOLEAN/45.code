System.out.println(continuousPoker.isContinuous(numbers));
}


public boolean isContinuous(int[] numbers) {
if(numbers == null || numbers.length <5)
largeIndex++;
}

// 间隔数目大于大小王数目，则不可能是顺子，否则可以利用大小王补齐
boolean isContinuousPoker = countGap > countZero ? false : true;

