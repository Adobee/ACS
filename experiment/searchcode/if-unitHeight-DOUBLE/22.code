* The images should have same dimensions(unitWidth and unitHeight).
*
* @param images
* @param header
* @param unitWidth
g.drawImage(bi, x, y, null);
x += unitWidth;
if (x >= outputWidth) {
x = 0;
y += unitHeight;

