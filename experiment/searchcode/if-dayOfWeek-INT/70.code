import java.util.*;

public enum DayOfWeek
{
MONDAY (&#39;M&#39;),
TUESDAY (&#39;T&#39;),
WEDNESDAY (&#39;W&#39;),
List<DayOfWeek> list = getDays();

DayOfWeek current = toDayOfWeek(this.letter);
int index = list.indexOf(current);

