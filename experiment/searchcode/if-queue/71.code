@Override
public T pop() {
if (queue.size() == 0) {
throw new IllegalStateException(&quot;No elements to pop!&quot;);
public T peek() {
if (queue.size() == 0) {
throw new IllegalStateException(&quot;No elements to pop!&quot;);

