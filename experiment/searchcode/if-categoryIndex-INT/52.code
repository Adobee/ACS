CustomList adapter;
boolean isEmpty = false;
int categoryIndex = -1;int catalogIndex = -1;
SharedPreferences sharedPreferences;
catalogIndex = getIntent().getIntExtra(&quot;catalogIndex&quot;, -1);
categoryIndex = getIntent().getIntExtra(&quot;categoryIndex&quot;, -1);
sharedPreferences = PreferenceManager.getDefaultSharedPreferences(ProductManager.this);

