Category category = new Category(name);

categoryList.add(category);

}

public boolean editCategoryName(int index, String newName) {
if(checkIfIndexValid(index)) {
public boolean editProductInCategory(int categoryIndex, int productIndex, String productName, int productPrice) {
if(checkIfIndexValid(categoryIndex)) {

