public void addAutoCancel(String key, AutoCancel autoCancel)
{
autoCancels.put(key, autoCancel);
}

public boolean containKey(String key)
return cancelAuto(String.valueOf(key));
}*/

public boolean cancelAuto(String key)
{
if (StringUtil.isStringEmpty(key))

