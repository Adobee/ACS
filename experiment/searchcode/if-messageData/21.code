import com.sabre.academy.tddformatstyle.model.MessageData;
import com.sun.media.sound.InvalidDataException;
private void checkIntegrityOfData(MessageData messageData) throws InvalidDataException {

if (StringUtils.isBlank(messageData.getBody()) ||

