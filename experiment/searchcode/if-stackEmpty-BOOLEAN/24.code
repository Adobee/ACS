for (int count = 0; count < symbol.length(); count++) {
// check if Stack is empty
if (stack.stackEmpty())
stack.push(symbol_char[count]);
stack.push(symbol_char[count]);

}
// check if stack is empty
if (stack.stackEmpty()) {
return true;
} else {
return false;
}
}
}

