letter = Character.toLowerCase(letter);

boolean result = false;
if (word.contains(String.valueOf(letter))) {
usedCharacters[pos] = false;
}
} else if (!isComplete()) {
int i = 0;
for (boolean b : usedCharacters) {

