return source.length() - index;
}

char pop()
{
if (getSize() == 0)
throw new StackEmpty(&quot;No more chars&quot;);
if (getSize() == 0)
throw new StackEmpty(&quot;No more chars&quot;);

if (index + pos < 0 || index + pos >= source.length())

