return string.substring(0, string.length() - numOfChars);
}

private static boolean isConsonant(char character) {
return &quot;aeiouAEIOU&quot;.indexOf(character) < 0;
}

private static boolean isVogal(char character) {
return !isConsonant(character);

