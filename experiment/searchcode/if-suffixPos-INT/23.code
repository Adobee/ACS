this.aspirate = aspirate;
}

public String getRadical(){
String suffix = template.toString().split(&quot;:&quot;)[1];
int suffixPos = infinitive.lastIndexOf(suffix);

return infinitive.substring(0, suffixPos);

