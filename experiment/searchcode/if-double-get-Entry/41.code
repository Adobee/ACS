Map<String, Double> C = B;
B = A;
A = C;
}

double dotProduct = 0;
for(Entry<String, Double> e : A.entrySet())
{
if(B.containsKey(e.getKey()))
for(Entry<String, Double> e : A.entrySet())
{
l += e.getValue() * e.getValue();
}
return Math.sqrt(l);
}
}

