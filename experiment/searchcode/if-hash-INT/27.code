* @param key The hash key.
* @return The index for the hashtable.
*/
public int hash(String key) {
int hash = (2* key.hashCode() +1 ) % 32;
if(hash < 0)
hash = -hash;
return hash;
}

/* (non-Javadoc)

