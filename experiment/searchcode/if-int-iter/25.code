public ListNode partition(ListNode head, int x) {
ListNode prRoot = new ListNode(0), scRoot = new ListNode(0);
for (; iter != null; iter = iter.next) {
if (iter.val < x) {
prIter.next = iter;
prIter = prIter.next;

