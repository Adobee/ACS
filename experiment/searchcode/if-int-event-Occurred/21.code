Object[] listeners = this.eventListeners.getListenerList();
for (int i = 0; i < listeners.length; i += 2) {
if (listeners[i] == t) {
((core.models.events.EventHandler) listeners[i+1]).eventOccurred(event);
}
}
}
}

