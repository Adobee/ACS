currentTime = (int)(System.currentTimeMillis() - gameStart); // Gets the tie since the game has started.
currentMillis = currentTime;
if (currentMillis >= 1000) { // Checks if more than 1 second has elapsed.
time += &quot;00&quot; + secondsTime;
}
else if (currentSeconds < 1000) {
time += &quot;0&quot; + secondsTime;
}

if (currentMillis < 10) {

