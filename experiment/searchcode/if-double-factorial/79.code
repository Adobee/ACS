for (int f = 1; f <= x; f++) {
factorial *= f;
}

return factorial;
}

public double sin(double radians) {
double r = radians;
radians += radians / (double)factorial;
r+= radians;
} else if (count % 2 != 0) {
radians -= radians / (double)factorial;

