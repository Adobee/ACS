for(int i = 0; i < R; i++)
{
for(int j = 0; j < C; j++)
{
if(!seen[i][j])
{
res++;
int R = M.length;
int C = M[0].length();
if(i < 0 || i >= R || j < 0 || j >= C || seen[i][j] || M[i].charAt(j)!=c)

