public static Dimension scaleDimension(Dimension dimension, int maxDimension) {
return scaleDimension(dimension.getWidth(), dimension.getHeight(), maxDimension, maxDimension);
}

public static Dimension scaleDimension(int width, int height, int maxDimension) {

