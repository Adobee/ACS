this.add(money.negate());
}

/**
* Multiplies the money in this wallet by the given multiplicand.
* @param multiplicand the multiplicand operator
*/
public void multiply(long multiplicand) {
checkNotNull(multiplicand, &quot;Multiplicand cannot be null&quot;);

