private List<Client> searchClients = new ArrayList<>();

@Autowired
ClientDao clientDao;

@Override
public boolean addClient(Client client) {
return clientDao.addClient(client);
return clientDao.getClients();
}

@Override
public boolean addSearchClient(Client client) {
searchClients.add(client);

