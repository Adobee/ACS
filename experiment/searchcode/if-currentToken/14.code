for(int i = 0; i < parsedProgram.size(); i++) {
currentToken = parsedProgram.get(i);

if(currentToken.equals(Tokens.DOCB)) {
currentToken = &quot;<html>&quot;;
} else if(currentToken.equals(Tokens.DOCE)) {

