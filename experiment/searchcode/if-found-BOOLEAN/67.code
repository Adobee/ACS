public boolean isNormalPassword(String password) {
Pattern pat;
Matcher mat;
boolean found;

pat = Pattern
mat = pat.matcher(password);
found = mat.matches();
if (found) {
found = true;
} else {
found = false;

