output.newLine();
writeThumbnail(series.coverUrl, seriesKey, 0, 0, output);

if (series.genres != null) {
writeThumbnail(season.coverUrl, seriesKey, season.seasonNo, 0, output);

if (season.episodeDetails != null) {
int i = 0;

