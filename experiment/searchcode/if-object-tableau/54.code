String[] tableau = string.split(&quot;[ (,)]&quot;);

switch (tableau[0]) {
case &quot;matrix&quot; :
a = Double.valueOf(tableau[1]);
ArrayList<Object> list = new ArrayList<>();

for (Object o : forme) {
Point p = apply(o);
if (p != null)
list.add(apply(p));

