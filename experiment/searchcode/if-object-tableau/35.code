if (last(commands).equals(&quot;tab1&quot;)) {
_game.tableauToFoundation(1);
}
if (last(commands).equals(&quot;tab2&quot;)) {
_game.tableauToFoundation(2);
}
if (last(commands).equals(&quot;tab3&quot;)) {

