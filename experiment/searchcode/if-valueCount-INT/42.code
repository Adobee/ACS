public class WordPatternReducer extends Reducer<IntWritable,Text,IntWritable, Text>{
private Map<String,Integer> valueCount=new HashMap<String,Integer>();
while (it.hasNext()){
newval=it.next();
if(valueCount.containsKey(newval.toString())){

