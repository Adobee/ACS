int value = values.get(i);
int valueCount = getValueCount(turn, value);
if (valueCount >= 3) {
for (int i = values.size() - 1; i >= 0; i--) {
int value = values.get(i);
if (value != threeValue) {
int valueCount = getValueCount(turn, value);

