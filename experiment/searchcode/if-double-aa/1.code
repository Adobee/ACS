protected static final int craterRadius = 6; // blocks from target location
protected static final double craterRadiusSq = craterRadius * craterRadius;

@Override
public void draw() {
removeTopCrater(craterX + x, craterY + 6, craterZ + z, i);
//				removeBlock(craterX + x, craterY +  6, craterZ + z, crater5[i] == AA);
//				removeBlock(craterX + x, craterY +  7, craterZ + z, crater5[i] == AA);

