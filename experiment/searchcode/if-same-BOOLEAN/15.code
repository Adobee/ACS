interface XML{
//determines if this XML is the same as
//some given XML
public boolean same(XML xml);
this.name = name;
this.value = value;
}

//determines if this is same with given att
public boolean sameAtt(Att att){

