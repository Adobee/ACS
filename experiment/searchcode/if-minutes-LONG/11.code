long hours = mss / (60 * 60);
long minutes = mss / 60;
if (minutes > 60) {
minutes = minutes % 60;
}
long seconds = (mss % 60);
if (hours > 0) {
return hours + &quot;	小时	&quot; + minutes + &quot;	分钟	&quot; + seconds + &quot;	秒	&quot;;

