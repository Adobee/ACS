* @return
*/
public static String formatDuring(long mss) {
long hours = mss / (60 * 60);
long minutes = mss / 60;
if (minutes > 60) {
minutes = minutes % 60;
}
long seconds = (mss % 60);
if (hours > 0) {

