public static int compare(BigDecimal d1, BigDecimal d2, int... pricision) {
DecimalFormat df = new DecimalFormat(&quot;0.####&quot;);
if (pricision.length > 1 ? true : (pricision.length == 1 ? pricision[0] < 0 : false)) {
public static boolean isZero(BigDecimal d) {
return compare(d, BigDecimal.ZERO) == 0;

