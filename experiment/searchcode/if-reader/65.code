//        for (Reader a : readerDAO.getAll()) {
//            if (a.getName().equals(oldName)) {
//        for (Reader a : readerDAO.getAll()) {
//            if (a.getName().equalsIgnoreCase(name)) {

