import org.milyn.ect.formats.unedifact.UnEdifactSpecificationReader;

public class SmooksEDIFile {

private final UnEdifactSpecificationReader reader;

SmooksEDIFile(UnEdifactSpecificationReader reader) {

if (reader == null) {
throw new IllegalArgumentException(&quot;reader == null&quot;);

