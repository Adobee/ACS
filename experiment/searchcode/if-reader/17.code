import com.epam.library.models.Role;
import com.epam.library.models.Reader;
import java.util.ArrayList;
import java.util.List;
Reader reader = new Reader();
for (int i = 0; i < getReader().size(); i++) {
getReader().get(i);
if (id == BaseEntity.getId()) {

