public final long newSize;

public DiffStats(int added, int removed, long oldSize, long newSize) {
this.added = added;
this.newSize = newSize;
}

@Override
public boolean equals(Object o) {
if (this == o) return true;

