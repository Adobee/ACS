package be.gerard.common.aop.util;

import java.lang.annotation.Annotation;
import org.springframework.util.Assert;
for (Annotation annotation : annotations) {
if (annotationClass.isInstance(annotation)) {
return (T) annotation;
}
}

return null;
}

}

