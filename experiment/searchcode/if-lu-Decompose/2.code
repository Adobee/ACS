@Override
protected boolean handleDecompose() {
m = a.getM();
n = a.getN();
lu = new double[m][n];
s += luRowI[k] * luColJ[k];
}
luRowI[j] = luColJ[i] -= s;
}
// Find pivot and exchange if necessary.

