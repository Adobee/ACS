double[][] LU = new double[dim][dim];
boolean odd = decompose(pi, LU);
for (int i=0; i<dim; i++) {
ret *= LU[i][i];
int[] pi = new int[dim];
double[][] LU = new double[dim][dim];
decompose(pi, LU);

