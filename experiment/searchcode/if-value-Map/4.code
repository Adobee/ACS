public Object getValue() {
Map map = (Map)mapValueModel.getValue();
if (map == null) {
public void setValue(Object value) {
Map map = (Map)mapValueModel.getValue();
if (map == null) {
return;
}
map.put(key, value);
}
}

