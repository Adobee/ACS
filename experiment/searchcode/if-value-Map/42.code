import java.util.Comparator;
import java.util.Map;

public class TreeMapValueComparator implements Comparator<Integer>{
public int compare(Integer o1, Integer o2) {
int subValue = map.get(o2) - map.get(o1);
if (subValue == 0) {

