do{
sortedSmthng = false;
for(int a = 0;a < (sort.length-rechtsbegrenzer);a++){
if((a+1)<sort.length){
if( (minToMax &amp;&amp; sort[a].compare(sort[a+1])>0) || (!minToMax &amp;&amp; sort[a].compare(sort[a+1])<0) ){

