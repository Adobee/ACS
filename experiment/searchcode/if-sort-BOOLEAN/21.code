public void bublleSort(double[] sort) {
double temp = 0;
boolean isChange = false;// 交换标志
int len = sort.length;
for (int j = 0; j < i - 1; j++) {// 注意要减1，否则越界（下面有j+1）
if (sort[j] > sort[j + 1]) {
temp = sort[j + 1];
sort[j + 1] = sort[j];
sort[j] = temp;

