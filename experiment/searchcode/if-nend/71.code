int nLength = s.length();

if (nLength == 0) {
return strRet;
}
//try from the middle
StringBuffer sbResult = new StringBuffer();
int nStart = 0;
int nEnd = 0;
if (nLength % 2 == 0) {
nStart = nLength / 2 - 1;

