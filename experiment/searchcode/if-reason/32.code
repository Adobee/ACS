OrderReason orderReason = new OrderReason();
orderReason.setRid(rid);
orderReason = getReason(orderReason);
if (rid == -1l || orderReason != null) {
Orders order_Reason = new Orders();
order_Reason.setOrderId(orderId);
order_Reason.setStatus(null);
if (status == 3L) {

