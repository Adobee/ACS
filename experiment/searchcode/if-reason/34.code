private String reason;

/**
* @param reason
*/
private ReceiptReasons(final String reason) {
for (final ReceiptReasons reasonr : ReceiptReasons.values()) {
if (reasonr.getReason().equals(reason)) {

