public TSMCommand(TSM pl){

plugin = pl;
}

/*
* This splits the reason from the rest of the arguments
while(n <= args.length){

reason.concat(args[n]);
reason.concat(&quot; &quot;);
n++;
}

if(reason == &quot;&quot; || reason == &quot; &quot;){

