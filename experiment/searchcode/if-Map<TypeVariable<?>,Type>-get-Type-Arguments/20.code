private void addNeededTypeArguments(Map<TypeVariable, Type> typeArguments) {
if (this.typeArguments == null) {
private boolean addNeededTypeArgument(Type type,
Map<TypeVariable, Type> typeArguments) {
if (type instanceof TypeVariable

