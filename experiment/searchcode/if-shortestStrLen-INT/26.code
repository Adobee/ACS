public static boolean isBlank(String str) {
int strLen;
if (str == null || (strLen = str.length()) == 0)
if (shortestStrLen == 0)
return 0;
int firstDiff = -1;
int stringPos = 0;
do {
if (stringPos >= shortestStrLen)

