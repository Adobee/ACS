protected Point computeSize(Composite composite, int wHint, int hHint,
boolean flushCache)
{
AComposite sc = (AComposite) composite;
private void updateBars(AComposite comp)
{
// 0. 确定是否需要滚动条
boolean needH = comp.needHScroll(false);
boolean needV = comp.needVScroll(needH);

