return &quot;just now&quot;;
}
int diffSeconds = (int)Math.ceil(diff / 1000);
if(diffSeconds == 0){
return diffSeconds + &quot; second ago&quot;;
}
int diffMinutes = (int)Math.ceil(diffSeconds / 60);
if(diffMinutes < 60){

