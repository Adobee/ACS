public void permuter(int tableau[], int i, int j) {
int temp;
temp = tableau[i];
tableau[i] = tableau[j];
tableau[j] = temp;
fin = (t.length - 1);
boolean permut = true;
do {
permut = false;
for (i = 0; i < fin; i++) {
if (t[i] > t[i + 1]) {

