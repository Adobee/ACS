double sumxx=0;
double sumyy=0;
double sumxy=0;


int extentX=(int)Math.round(3*sigmaX);
extentX=Math.max(extentX, 2);
double dx2=x-midx;
dx2=dx2*dx2;
double t=arr[base+x];
if(dx2+dy2<=extent*extent)
if(t<minIntensity)

