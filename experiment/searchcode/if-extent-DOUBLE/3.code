private void exponentiationRun() {

println ( pow(0.5, -2) );

}

public double pow(double x, double extent) {
double variableX = x;

if (extent < 0) { /* Отрецательная степень */

// Чтобы работал счетчик, минусовую переменную делаем положительной

