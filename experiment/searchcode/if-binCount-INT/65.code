double [] bins = new double [7];
double [] binCount = new double[7];
// Calculate Tq and Tw
int size = allRequests.size();
bins[0]+= r.responseTime/r.serviceTime;
binCount[0]++;
}
else if (r.serviceTime <= 0.020){

