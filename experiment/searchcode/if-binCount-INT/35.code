return tempTime;
}

public float delta(int bin)
{
if (bin < 0 || bin > binCount)
return -1;
float tempTime = delta();
return average;
}

public long getCount(int bin)
{
if (bin < 0 || bin > binCount)
return -1;

