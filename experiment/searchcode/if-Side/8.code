public int getSideC() {
return sideC;
}

/**
* Check to see if triangle is Isosceles
* @return boolean
return (getSideA() == getSideC()) &amp;&amp; (getSideB() == getSideC());
}

/**
* Check to see if triangle is Isosceles
* @return boolean

