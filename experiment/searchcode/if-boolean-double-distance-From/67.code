private final boolean distanceEnabled;

public VoronoiNoise(int seed, double frequency, double displacement, boolean distanceEnabled) {
candidateY = positionY;
}
}
}

double value;

if (distanceEnabled) {
double distanceX = candidateX - x;

