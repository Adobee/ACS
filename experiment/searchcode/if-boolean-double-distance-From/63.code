this.end = end;
}

/**
* Finds the point at distance units from the base along the segment
* and returns null if distance is out of the segment&#39;s range.
public double evaluateAtPoint(Vector3D point)
{
double distance = super.evaluateAtPoint(point);

if(Double.isNaN(distance))

