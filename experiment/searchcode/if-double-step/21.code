assert lowerStep == 0 || lowerStep >= minLot;
double upperStep = upperStep(size);
if (distance(size, lowerStep) <= distance(size, upperStep)) {
double adjustedSize = Math.min(size, maxLot);
assert adjustedSize >= 0;
double lowerStep;
if (stepLot == 0) {

