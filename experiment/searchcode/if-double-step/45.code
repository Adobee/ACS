double integral = 0;

public CDF(double input, boolean mon, double step){
x = input;
type = mon;
if(mon==true){
return Math.sqrt(-2 * Math.log(Math.sqrt(2 * Math.PI) * pdf));
}

public static double computationLimit(double step){

