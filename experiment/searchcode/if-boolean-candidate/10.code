public String getCandidate()
{
return candidate;
}
public Boolean isPalindrome()
{
int len=getCandidate().length();
int start=0;
int finish=len-1;
Boolean palindrome = true;

while (start <= finish)
{
if (candidate.charAt(start) != candidate.charAt(finish))

