return copmareIvariable(d);
}

private int copmareIvariable(Ivariable d) {
if (!hasIvariable())
return d.hasIvariable() ? 1 : compareIvalue(d);
else if (!d.hasIvariable())
return -1;
else {
int c = ivariable.compareTo(d.getIvariable());

