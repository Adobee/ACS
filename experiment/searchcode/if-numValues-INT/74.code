int[][] population;
int []elite;
int numIndividuals;
int numValues;
int genomeLength;
Random rnd;
for(int i = 0; i < genomeLength; ++i)
{
if(rnd.nextDouble() < mutProb)
{
int n = rnd.nextInt(numValues);

