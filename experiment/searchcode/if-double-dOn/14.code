public static double dot(double[] a, double[] b) {
if (a.length != b.length) {
throw new RuntimeException(String.format(
&quot;Vector lengths (%s, %s) don&#39;t match&quot;, a.length, b.length));
}
double sum = 0;
for (int i = 0; i < a.length; i++) {

