// Make sure a class is set
//
if (weatherClass == null) {
// Load the default class
WeatherState newWeatherStatus = weather.processWeatherChange();

// Check if the weather has changed
//
if (newWeatherStatus != weather) {

