protected boolean verifyColumnTypeByLabel(List<T> recordsForScanning, int index) throws Exception {
if(recordsForScanning.size() <= 0){
//Si tiene traccia dei valori nulli (sono accettati purché non lo siano tutti).
boolean allNull = true;
//Si scorrono le righe campione (l&#39;header del file non è mai presente tra i record)

