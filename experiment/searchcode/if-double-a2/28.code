package t10_klassen.a2_4_polynome;

public class Parabel
{
private double a;
private double b;
double q = c / a;
double diskriminante = Math.pow(p / 2, 2) - q;
if (diskriminante < 0)
{
return new double[]{};

