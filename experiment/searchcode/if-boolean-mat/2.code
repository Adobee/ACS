this(data, false);
}

public Mat(float [] data, boolean transposed) {
if(transposed)
setTransposed(data);
this(data, false);
}

public Mat(float [][] data, boolean transposed) {
if(transposed)
setTransposed(data);

