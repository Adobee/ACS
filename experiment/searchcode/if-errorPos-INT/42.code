- (fontHeight - fm.getAscent());
}
int yend = ybaseline + height;
if (yend > (y + height))
g.setFont(new Font(&quot;Consolas&quot;, Font.BOLD, 15));
if(Errors!=null){
while(ErrorPos<=ErrorSum&amp;&amp;(int)(Errors[ErrorPos].getWidth())==startingLineNumber){

