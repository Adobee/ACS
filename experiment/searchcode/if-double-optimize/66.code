double speedLimit,
double nextSpeedLimit,
double distanceUntilSpeedLimit
) {
double brake = 0;

if (canTakeAction(time)) {
private double reachSpeed(double speed, double targetSpeed) {
double throttle = 0;

if (speed < targetSpeed) {
if (targetSpeed > 110) {

