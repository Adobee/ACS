public TimeStreamTest()
{
super();
}

private void testPattern(long   time,
String pattern,
boolean okay = (expected.equals(received));

if (!okay) {
if (pattern.equals(&quot;&quot;)) {
String expectedIntString = toIntString(expected);

