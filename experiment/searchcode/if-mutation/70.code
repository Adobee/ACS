package evolutionary.strategies.mutation;

import utils.UtilityMethods;

import com.google.java.contract.Requires;
protected void mutate(Genotype input) {
for (Gene g : input) {
if (UtilityMethods.random() < geneMutationChance) {

