ValueAxis domainAxis = (ValueAxis) getDomainAxis(i);
if (domainAxis != null) {
if(domainAxis instanceof LinkAndBrushAxis) {
for (int i = 0; i < getRangeAxisCount(); i++) {
ValueAxis rangeAxis = (ValueAxis) getRangeAxis(i);
if (rangeAxis != null) {
if(rangeAxis instanceof LinkAndBrushAxis) {

