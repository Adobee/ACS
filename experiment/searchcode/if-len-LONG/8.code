int len = s.length();

int best_len = 0;
int cen = 0;
String long_pali = &quot;&quot;;

for(int i = 0; i < (2*len - 1); i ++)
cen = i/2;
}
}
}
if(best_len % 2 == 0)
long_pali = s.substring(cen - best_len/2 + 1, cen + best_len/2 + 1);

