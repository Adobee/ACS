System.arraycopy(array, 0, result, 0, len);

return (T) result;
} else if (clazz == long[].class) {
int len = ((long[]) array).length;
System.arraycopy(array, 0, result, 0, len);

return (T) result;
} else if (clazz == Long[].class) {

