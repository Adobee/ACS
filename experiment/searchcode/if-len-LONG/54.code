try {

// Update the readable file length
if (position + (long) len > writeLength) {
readLength = position + (long) len;
}

// If we are over writing a write buffered section of the file then flush the buffer before we continue

