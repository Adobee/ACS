public static long[][] mpsum(long[][] a, long k) {
if(k == 0)
return new long[a.length][a[0].length];
if(k % 2 == 0)
public static long[][] munit(int len) {
long[][] ans = new long[len][len];

for(int i = 0; i < len; i++)

