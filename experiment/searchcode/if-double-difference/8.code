private Log log = LogFactory.getLog(this.getClass());
private double epsilon;

public AcceptCloseValuesDifferenceListener(double epsilon) {
this.epsilon = epsilon;
}

/**
* Returns true if difference of a numeric node.

