double x = x1 + h;
double y = y0;
double w = t(x1);
double difference = 0;
double[] maxDif = new double[2];
System.out.format(&quot;%.2f\t%.2f\t%.2f\t%f\n&quot;, x, y, w, difference);
x += h;


if (Math.abs(maxDif[0]) < Math.abs(difference)) {

