int minRank = 100;
int index = 0;
int evicted = -1;
for (int i = 0; i<NUM_FRAMES; i++) {
if (pageFrames[i] == -1){
entryTimes[referenced] = rank++;
return evicted;
} else if (minRank > entryTimes[pageFrames[i]]) {
index = i;

