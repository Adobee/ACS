public class BasicGridGraph implements GraphFactory
{
public String create (int numNodes, MessageHandler messageHandler)
{
if (Math.sqrt(numNodes) % 1 != 0.0)
String graphString = String.valueOf(numNodes) + &quot; Agents&quot;;

for(int i = 0; i < numNodes; i++)
{
if (i % squareLength != squareLength - 1)

