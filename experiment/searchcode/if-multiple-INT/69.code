char[] numberArray= numberBlock.toCharArray();
long multiple = 1;
long max_multiple = 1;
int max_start = 0;
for(int i=0; i<blockLength-13; i++)
int value = Character.getNumericValue(numberArray[i+j]);
multiple *= value;
}
if(multiple>max_multiple)

