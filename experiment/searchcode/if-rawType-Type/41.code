this.object=object;
this.type=type;
//		if(null == object){
//			computeNullType(rawType);
//		}else{
//			computeType(rawType);
computeNullType(rawType);
}else{
computeType(rawType);
}
}
public ValueObject(Object object){
if(null != object){

