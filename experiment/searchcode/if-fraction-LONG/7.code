int [] r = new int[3];
r[0] = a[0] * b[0] + c * (b[0]);
}
}
class Fraction {
private long top;
private long den;
Fraction (long a, long b)
{
top = a;
return new Fraction(a.n() * b.n(), a.d() * b.d());
}
long n () { return top; }
long d () { return den; }

