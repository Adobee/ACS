public static final Geometry[] toGeometryArrays(final Geometry geom) {
int numGeometries = geom.getNumGeometries();
if(numGeometries <= 0)
return new Geometry[] {};
else {
Geometry[] geometries = new Geometry[numGeometries];
for(int i = 0; i < numGeometries; i++)

