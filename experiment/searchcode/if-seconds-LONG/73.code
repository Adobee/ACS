private static final Logger logger = Logger.getLogger(AddWait.class);
private long lSeconds;

/**
* Sets up the test fixture. Called before every test case method.
try {
if (getlSeconds() == 0)
setlSeconds(AppProperties.getAsLong(&quot;RELEASE_WAIT_TIME&quot;, 0));

