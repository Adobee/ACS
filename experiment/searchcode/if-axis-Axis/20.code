roots = nextRoots;
i++;
}
if( interval[1] != axis) {
s = new Line2D.Double((i - 1) + axis,-roots[0] + axis
g2.draw(s);
}
} else if( interval[1] == axis) {
roots = e.getValue(axis);
i = axis - 1;
vertex = interval[0];

