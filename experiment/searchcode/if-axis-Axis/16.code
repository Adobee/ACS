attributeIn  = aIn;
attributeOut = aOut;

if (isSet)
{
addAxis(new Axis(&quot;&quot;, attributeIn), AxisCategory.SET_INPUT); //$NON-NLS-1$
public IdentityDependency (Axis inputAxis, Axis outputAxis, boolean isSet)
{
if (isSet) {
addAxis (inputAxis, AxisCategory.SET_INPUT);

