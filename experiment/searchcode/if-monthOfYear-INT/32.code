public class CalendarDate implements Comparable<CalendarDate> {

private int dayOfMonth;
private CalendarMonth monthOfYear;
private int year;
private YearType yearType;

public CalendarDate(int dayOfMonth, CalendarMonth monthOfYear, int year) {
if (monthOfYear == null || dayOfMonth < 0 || dayOfMonth > 31

