protected FilterConfig filterConfig = null;
/**
* 是否启用，默认不启用，安全性要求高时选择启用，影响性能
*/
protected boolean active = false;
String actValue = filterConfig.getInitParameter(&quot;active&quot;);
if (&quot;true&quot;.equalsIgnoreCase(actValue))

