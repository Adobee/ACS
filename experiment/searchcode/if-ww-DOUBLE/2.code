ww = Math.max(ww, v / w);
}
double result = 0;
for (int i = 0; i < ww; i++) {
result += (double) app[i] / (double) hh * (double) (ww - i)
/ (double) ww;
}
result += 0.01;
System.out.println(result);
}
sc.close();
}
}

