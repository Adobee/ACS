private ReceiptThread recpThread =new ReceiptThread();

private IPPDTransResultV2Service service = new PPDTransResultV2ServiceImpl();
return;
}
if(recpThread == null){
logger.error(&quot;【&quot; + cmd.getCommandName() + &quot;】处理线程未启动！&quot;);
return;

