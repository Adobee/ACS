logger.error(&quot;处理线程stop失败！cmd is null&quot;);
return;
}
if(recpThread == null){
logger.error(&quot;【&quot; + cmd.getCommandName() + &quot;】处理线程未启动！&quot;);
private boolean stopped = false;

public void setCmdName(String cmdName) {
if (cmdName != null) {
this.cmdName = cmdName;

