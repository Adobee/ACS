} else {
return true;
}
}

protected boolean isEmailValid(String emailStr) {

if(!isFieldValid(emailStr)){
intent.putExtra(Intent.EXTRA_TEXT, content);

if(recipientsCC.size() > 0){
String[] recpCC = new String[recipientsCC.size()];
for(int i = 0; i < recpCC.length; i++){

