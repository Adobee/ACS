new Order<>(graph, visitor, GraphVisitor.Direction.POST);
if (!list.isEmpty()) {
order = list;
}
}

public boolean hasOrder() {
return order != null;
}

public Iterable<V> order() {
return order;
}
}

