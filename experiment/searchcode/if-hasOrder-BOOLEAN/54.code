new Order<>(graph, visitor, GraphVisitor.Direction.PRE);
if (!list.isEmpty()) {
order = list;
}
}

public boolean hasOrder() {
return order != null;
}

public Iterable<V> order() {
return order;
}
}

