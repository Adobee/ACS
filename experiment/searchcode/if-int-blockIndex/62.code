private ArrayIntList createBlock(int blockIndex)
{
if (blockIndex < blockSizes.length) {
return new IntList(blockSizes[blockSizes.length-1-blockIndex]);
offset += blockSizes[i];
}

int result = 0;
if (blockIndex >= 0) {
result = blocks.get(blockIndex).getInt(index-offset);

