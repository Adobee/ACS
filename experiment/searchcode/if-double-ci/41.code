this.sigma2 = sigma2;
centroid = new double[numCentroid][numDim];
for (int ci = 0; ci < numCentroid; ci++) {
for (int ci = 0; ci < numCentroid; ci++) {
if (weight2[ci] == Double.NaN || weight2[ci] < 1E-20) {
for (int di = 0; di < numDim; di++) {

