private static ParserConfiguration instance;

private boolean sanitizeXHTML = true;
private boolean fixCDATA = true;
return instance;
}

/**
* @return the sanitizeXHTML
*/
public boolean sanitizeXHTML() {
return sanitizeXHTML;

