double fin = 0;
if (maxCoefficient > p) {
double sum = 0;
sum += original[i-1][j-1];
sum += original[i-1][j];
sum += original[i+1][j+1];

fin = (1.5 * ((maxCoefficient / (double) sum) - 0.333));
if (fin < threshold) {

