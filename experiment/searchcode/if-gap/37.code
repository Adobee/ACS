for (i = 0; i < gap; i++) // 直接插入排序
{
for (j = i + gap; j < n; j += gap)
if (A[j] < A[j - gap]) {
int temp = A[j];
for (j = gap; j < n; j++)
// 从数组第gap个元素开始
if (A[j] < A[j - gap])// 每个元素与自己组内的数据进行直接插入排序

