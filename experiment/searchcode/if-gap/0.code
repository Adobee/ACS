double gap = 1;

while (gap*2.2 < getElementCount()) {
gap=gap*2.2;
}
int gapI = (int) gap;

if (gap < gapI) {
start++;

}
gap = (double) gapI;
gap = gap / 2.2;
gapI = (int)gap;
if (gap < gapI) {
gapI = gapI -1;

