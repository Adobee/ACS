indiceGap = j + gap;

if (array[j].compareTo(array[indiceGap]) > 0) {
Util.swap(array, j, indiceGap);
trocou = true;
}
}
if (gap == 1 &amp;&amp; !trocou) {
return;
}
}
}

private int calculaGap(int gap) {

