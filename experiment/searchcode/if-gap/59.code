System.out.println(&quot;array[&quot; + i + &quot;] = &quot; +array[i]);
}

private static void shellsort(int[] a, int n)
{
int i, j, temp, gap;

gap = n/2;
for (j = i; j >= gap; j-=gap)
{
if(a[j-gap] < max)
{
a[j] = a[j-gap];
}
}
a[j] = max;
}

gap = gap/2;
}
}
}

