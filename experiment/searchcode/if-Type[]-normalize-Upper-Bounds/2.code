WildcardType rw = (WildcardType) r;
return equalsSet(lw.getUpperBounds(), rw.getUpperBounds(), contexts) &amp;&amp;
return ret;
}

public Type normalizeType(Type t, Reconstruction reconst) {
if (t == null) {

