// returns true iff factory kind is new
public boolean addFactory(Factory factory) {
String name = factory.getName();
LinkedList factoryList = (LinkedList) this.factoryListForFactoryName.get(name);
if (factoryList == null) {

