public String getDiff()
{
/* computes the difference of the two calendars in milliseconds */
long millisDifference = getMillisDiff();
/* returns the difference */
if (daysDifference == 1)
return daysDifference + &quot; day &quot; + hoursDifference + &quot; hours &quot;

