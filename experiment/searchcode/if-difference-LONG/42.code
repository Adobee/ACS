* @return returns the sum square difference of the numbers between [1, max] inclusive
*/
public static long getSumSquareDifference(int max) {
assert(max >= 0);
if (max == 0 || max ==1) return max;
long sum = 0;

