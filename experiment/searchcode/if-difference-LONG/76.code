List<Bill> billList = billDao.getListByTailDifferenceId(new Long(idList[i]));
if (billList != null &amp;&amp; billList.size() > 0) {
public Boolean shareTailDifferenceAll(int dataSource, Long userId) {
if (dataSource == 0) {
Bill bill = new Bill();

