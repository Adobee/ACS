Point midpoint = midpoint();

if (a.x > b.x) {
a.x = midpoint.x + xOff;
b.x = midpoint.x - xOff;
} else {
a.x = midpoint.x - xOff;
b.x = midpoint.x + xOff;
}

if (a.y > b.y) {
a.y = midpoint.y + yOff;

