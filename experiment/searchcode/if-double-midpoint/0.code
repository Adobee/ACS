package comp559.a1;

public class Midpoint implements Integrator {

private static double[] midpoint = new double[0];
// Make sure our temporary array is correctly sized
if (midpoint.length != n) {
midpoint = new double[n];
}

// Get the derivative into midpoint

