int midpoint = (begin + end) / 2;
if (end < begin){
return index;
}
// change index to valid index, IF applicable
if (sortedArray[midpoint] == key) {
return midpoint;
}
if (sortedArray[midpoint] > key){

