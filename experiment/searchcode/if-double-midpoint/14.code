// Calculate the midpoint and the square of the midpoint.
double midpoint = (end + start) / 2;
double midpoint_squared = midpoint * midpoint;

// If we&#39;ve found the square root, return it.
if (midpoint_squared == input)

