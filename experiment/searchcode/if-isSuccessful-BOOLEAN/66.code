TaskList recurringTasks = Command.getTasksList().getTasksByRecurringID(task.getRecurringID());

boolean isSuccessful = true;

// Ensures that if occurrence is already deleted from the recurring task,
ArrayList<String> tagsList = (ArrayList<String>) getCmdParameters().get(CmdParameters.TAG_LIST);

boolean isSuccessful = false;
// If it only contains the &#39;#&#39; delimiter, remove all tags.

