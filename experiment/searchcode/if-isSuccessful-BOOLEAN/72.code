* This is a short term job. The thread will terminate as soon as data is read.
* isSuccessful indicates if the data is read successfully.
* We should use a thread pool to operate this thread.
private static volatile ArrayList<Trade> currentHistory;
private static volatile boolean isSuccessful = false;

