double[] table = new double[boxes.length];
double maxHeight = 0.0;
for (int i = 0; i < boxes.length; i++) {
table[i] = boxes[i].h;
return maxHeight;
}

static class Box implements Comparable<Box> {
double w;
double d;

