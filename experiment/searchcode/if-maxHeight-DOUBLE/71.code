public class DP19 {
public double solve(double[][] array) {
ArrayList<Box> boxList = new ArrayList<Box>(array.length * 3);
double[] table = new double[boxes.length];
double maxHeight = 0;
for (int i = 0; i < boxes.length; i++) {
table[i] = boxes[i].h;

