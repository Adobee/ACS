return add(a, b);
}

int multiply(int a, int b) {
boolean negative = false; // Sign
if (a < 0) {
throw new Exception(&quot;Divisor shouldn&#39;t be zero!&quot;);
boolean negative = false; // Sign
if (a < 0) {
negative = !negative;
a = sub(0, a);

