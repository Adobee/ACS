String unscale;
int scale;
int pointPos;
int expPos = s.indexOf(&#39;E&#39;);

if ( expPos >= 0 ) {
int signPos = ( c[0] == &#39;+&#39; || c[0] == &#39;-&#39; ) ? 0 : -1;
while ( p < expPos ) {
if ( p != pointPos &amp;&amp; p != signPos )

