long blackToWhiteCost = x + z;
long whiteToBlackCost = y + z;
long total = 0;

if (x > whiteToBlackCost) {
total = total + b * (y + z);
} else {
total = total + b * x;
}

if (y > blackToWhiteCost) {

