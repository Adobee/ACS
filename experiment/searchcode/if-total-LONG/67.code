long x = X;
for (int i = 1;; i++) {
long y = (x + ((n + x - 1) / x)) >> 1;
if (y == x)
return i;
total += (b - a + 1) * x;
} else if (x + 1 == y) {
long j = search(a, b, x + 0.5);
total += (j - a + 1) * x;

