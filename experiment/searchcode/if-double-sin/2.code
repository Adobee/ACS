sierpinski(n, length, xcenter, ycenter);
}

public static double sin(double angle)
{
return Math.sin(Math.toRadians(angle));
}

public static void sierpinski(int n, double len, double xc, double yc)
{
if(n == 0)

