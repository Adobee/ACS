double alphaN = alphabeta(d, depth-1, alpha, beta, 2);
undo(turns.get(i), d);
if(alphaN > alpha){
alpha = alphaN;
if(depth == initialDepth)
if(depth == initialDepth)
t = turns.get(i);
}

if(beta < alpha)

