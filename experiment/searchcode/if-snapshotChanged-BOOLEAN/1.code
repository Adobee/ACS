imageNames.remove(s);
return s;
}

boolean stepBack() {
if (selectedImageName != null) {
int i = imageNames.indexOf(selectedImageName);
return true;
}
}
return false;
}

boolean stepForward() {
if (selectedImageName != null) {
int i = imageNames.indexOf(selectedImageName);

