import static org.junit.Assert.assertNull;

import org.junit.Test;

import de.ust.skill.generated.annotation.A;
import de.ust.skill.generated.annotation.SkillState;
// annotation is not null
A a1 = new A();
a1.setA(a1);
state.getA().add(a1);

// annotation is null if type is unknown

