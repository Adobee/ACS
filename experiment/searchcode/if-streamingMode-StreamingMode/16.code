eValue = streamingMode.bandwidthPredictor(bandwidths);
if (eValue < 0) {
counter--;
}
//                if (!isIdle) {
//                    isIdle = true;
//                    eValue = streamingMode.bandwidthPredictor(bandwidths);

