File file = new File(filepath);

modifiedDate = file.lastModified();
}

public boolean isModified (String filepath) {
System.out.println(&quot;NEW MODIFIED DATE: &quot; + file.lastModified());

if (file.lastModified() != modifiedDate) {
return true;
} else {
return false;

