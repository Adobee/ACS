private int totChildren;
private int numChildren;
private Path pathRecord;

public treeNode( treeNode daddyO, int numKids, Path pehth ) {
public treeNode getChild( int index ) {
if( index < numChildren ) {
return children[index];
} else {
System.out.println( &quot;>>> Tried to access invalid index of tree children&quot;);

