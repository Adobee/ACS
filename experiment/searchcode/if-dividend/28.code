* @param divisor
* @return
*/
public int divide2(int dividend, int divisor) {
if (divisor == 0)
return Integer.MAX_VALUE;

if (dividend == 0)
return 0;

if (dividend < divisor)
return 0;

int ans = 0;

