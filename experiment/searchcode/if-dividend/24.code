public int divide(int dividend, int divisor) {

if (divisor == 0)
return 0;

if (dividend == 0)
return 0;

if (divisor == 1)
return Math.abs(dividend);

if (dividend < divisor)
return 0;

dividend = Math.abs(dividend);

