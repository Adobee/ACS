* @param AB table to calculate
* @param X array with answer
* @return
*/
public static boolean gauss(int n, double[][] AB, double[] X)
for (i = 0; i < n - 1; i++)
{
for (j = i + 1; j < n; j++)
{
if (Math.abs(AB[i][i]) < eps) return false;

