String str = (params[0] == null &amp;&amp; options.isStringNullAsBlank())?&quot;&quot;:(String)params[0];
if(str == null) return null;
boolean wholeWord = (Boolean) params[1];
char ch = b.charAt(i);
if(Character.isWhitespace(ch)){
capitalizeNext = true;

