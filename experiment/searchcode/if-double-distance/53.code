if (maxDistance == null || maxDistance < maxDistanceIfGreater) {
maxDistance = Double.valueOf(maxDistanceIfGreater);
if (minDistance == null || minDistance > minDistanceIfSmaller) {
minDistance = Double.valueOf(minDistanceIfSmaller);
}
}
}

