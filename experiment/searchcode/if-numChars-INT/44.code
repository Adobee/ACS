public class StringCompression {
public static String compressString(String input) {
if (input.length() == 0)
return &quot;&quot;;

char repChar = input.charAt(0);
int numChars = 1;
StringBuilder builder = new StringBuilder(input.length());

