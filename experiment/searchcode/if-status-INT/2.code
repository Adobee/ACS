waitingForAnswer(0), accept(1), reject(2);
private int vacationStatusInt;

public int getVacationStatusInt() {
return vacationStatusInt;
VacationStatus vacationStatus = null;
for (VacationStatus v : values()) {
if (v.vacationStatusInt == vacationStatusInt)
vacationStatus = v;
}
return vacationStatus;
}

}

