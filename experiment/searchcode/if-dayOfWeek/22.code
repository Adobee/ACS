public static String getStringDayOfWeek(int[] days) {
String dayOfWeek = &quot;&quot;;
for (int i = 0; i < days.length; i++) {
int day = days[i];
if (i == days.length - 1) {
dayOfWeek = dayOfWeek + day;
} else {

