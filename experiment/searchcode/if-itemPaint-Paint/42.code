private ItemView monthView;
private Paint itemPaint;
private float itemTextHeight;

public BqDatePicker(Context context) {
if (itemPaint != null) itemPaint.setColor(color);
if (yearView != null) yearView.postInvalidate();

