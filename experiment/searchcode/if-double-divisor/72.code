public double calculateAverage(int n) {
double average = 0.0;
int t = data.getTime();
if(data.getTime() <= n)
average += data.getNthPrice(i)*(i+1);
divisor += (i+1);
}
average = average/((double)divisor);
}
else
{
int divisor = 0;

