private int greatestCommonDivisor = DEFAULT_DIVISOR_VALUE;
static public int count;

public FractionNumberImpl(int dividend, int divisor) {
if (count < 2) {
System.out.print(++count + &quot; fraction of your entered numbers: &quot; + dividend + &quot;/&quot; + divisor);
if (dividend >= divisor) {
greatestCommonDivisor = greatestCommonDivisor(dividend, divisor);

