//     int subtractor;
//     int tag = 1;

//     if((double)dividend * (double)divisor < 0) tag = -1;
long divisorL = Math.abs((long)divisor);
long subtractor;
int tag = 1;

if((double)dividend * (double)divisor < 0) tag = -1;

