double T, // time to maturity
double r, // interest rate
double q) // dividend yield
{

if (T <= 0.0) {
for (int i = 3; i < 200; i += 2) {
double prev = p;
t *= z2 / i;
p += t;
if (p == prev) {
return 0.5 + p;
}
}

return (z > 0) ? 1 : 0;
}
}

