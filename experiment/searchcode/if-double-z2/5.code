package complex;
import java.lang.Math;

public class Complex
{
double x;
double y;

public Complex(double a, double b)
z3.y = (z1.x * z2.y) + (z1.y * z2.x);
return z3;
}

public static double modulus(Complex z)
{
double zbar = (z.x * z.x) + (z.y * z.y);

