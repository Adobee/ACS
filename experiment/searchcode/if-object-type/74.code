this.object=object;
this.type=type;
//		if(null == object){
//			computeNullType(rawType);
//		}else{
//			computeType(rawType);
this.object=hiValue;
this.hiValue=hiValue;
this.loValue=loValue;
if(null == object){
computeNullType(rawType);

