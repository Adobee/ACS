return timerStarted;
}

/**
* @return if timer can end
*/
public boolean isCompletable() {
return completable;
SmartDashboard.putNumber(&quot; ERROR&quot;, this.previousError);
}

public boolean isCompleted() {
// If the Controller is completable, then the error will need to be

