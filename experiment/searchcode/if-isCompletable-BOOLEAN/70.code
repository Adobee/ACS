List<Task> taskList = new ArrayList<Task>();
boolean canAssign = false;
if (getSelectedRows().size() > 0) {
public boolean isRollBackable(AbstractUser user) {
Task task = getSelectedRow();
if (task != null) {

