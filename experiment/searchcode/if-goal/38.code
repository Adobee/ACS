newPriority = (time + toGoal.getPriority())/2;

}else if( to==goalListAdapter.getCount()-1 ){
Goal toGoal = (Goal)goalListAdapter.getItem(to);
}else{
if( from < to ) {
// 下に下がる場合
Goal up = (Goal) goalListAdapter.getItem(to);

