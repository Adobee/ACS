done = (t_e == 0.0);
if (!done)
{
System.out.printf(&quot;%.3f\n&quot;, solve(t_e, t_r));
}
}

in.close();
}

private static double solve(double e, double r)
{
double v;
if (dbg) System.err.println(r + &quot; | &quot; + e);

