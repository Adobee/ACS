boolean solve = false;
if (b == 0) {
int x = 0;
double sqrt = Math.sqrt(c / 2.0);
for (int y = 1; y < sqrt &amp;&amp; !solve; y++)
if (Math.sqrt(c - y * y) % 1 == 0) {
int z = (int)Math.sqrt(c - y * y);

