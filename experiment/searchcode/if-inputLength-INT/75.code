*            every test, no tests, or once at the top
*/
public RandomInput(String name, InputLength il, boolean parity, int show) {
super(name, show);
// adjust the parity of each byte, if necessary; used mainly for Triple-DES
if (parity) {
for (int i = 0; i < val.length; i++) {
int b = val[i];

