for(int i=0;i<chars.length - 4;i++) {
// Product of 5 digits in a row
int one = Character.getNumericValue(chars[i]);
int two = Character.getNumericValue(chars[i+1]);
int three = Character.getNumericValue(chars[i+2]);
int four = Character.getNumericValue(chars[i+3]);

