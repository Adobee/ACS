import easycare.dao.EcxIntegratorRepository;
import easycare.dao.IntegratorPatientRepository;
import easycare.dao.PatientRepository;
EcxIntegrator integrator = findEcxIntegratorByDisplayName(integratorName);

if (integrator == null) {
throw new NullPointerException(&quot;The ecx integrator with name=\&quot;&quot;

