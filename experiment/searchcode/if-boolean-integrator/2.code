public void testShouldUpdateExistingIntegratorPatientAssociation() {
final boolean accessPermitted = false;
verify(mockIntegratorPatientRepository).removeOrganisationPatientsFromIntegrator(org, integrator);
}

private IntegratorPatient buildIntegratorPatient(final boolean integrated) {

