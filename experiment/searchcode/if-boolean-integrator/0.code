return integratorPatientRepository.findByPatientWithAccessPermitted(patient);
}

public boolean isIntegratorAssociatedWithPatient(EcxIntegrator integrator, Patient patient) {
if (integrator == null || patient == null) {

