// prevents NullPointerException() from occuring -> O(1)
private int getHeight(Node node) {
if (node == null)
return -1;
deleteNode(root);
return true;
}

// if node smaller than root
else if (node.getKey() < root.getKey())

