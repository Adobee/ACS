return count[0];
}

private void totalNQueens(int[] count, int[] rows, int row) {
if(row >= rows.length) {
private boolean isValid(int[] rows, int row) {
for(int i = 0; i < row; i++) {
if(rows[row] == rows[i])
return false;

