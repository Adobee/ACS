public NegateOperation(Expression e)
{
super(e);
}

@Override
public double evaluateExpression()
{
if(exp.evaluateExpression() < 0)
{
return exp.evaluateExpression() + 2 * (exp.evaluateExpression());
}

if(exp.evaluateExpression() > 0)

