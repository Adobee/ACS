TreeNode test0 = new TreeNode(30);
test0.left = new TreeNode(15);
test0.left.left = new TreeNode(10);
2. The general recursive solution to such methods which return
a boolean to decide if a tree is a xxx tree:

