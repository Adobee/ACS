package onionrouting.directory.shutdown;

import java.util.Scanner;

import org.apache.log4j.Logger;
boolean shutdown = false;
while (!shutdown &amp;&amp; (input = sc.next()) != null) {
if (input.equals(&quot;!shutdown&quot;)) {

