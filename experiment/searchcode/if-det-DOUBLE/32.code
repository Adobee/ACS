package engine.model;

class TriangleHelper {
private Vertex C;
private double a1, a2, b1, b2, c1, c2, d1, d2, det;
b2 = B.y - C.y;
det = a1 * b2 - b1 * a2;
if (det != 0) {
c1 = b2 / det;
c2 = -a2 / det;

