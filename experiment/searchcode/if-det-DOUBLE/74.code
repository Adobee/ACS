public void setDeterminator(int det)
{
this.det = det;
}

public double value()
{
return 1.0 * num / det;
for(int n:numList)
for(int d: detList)
if(d==n &amp;&amp; d>biggest)
biggest = d;

num /= biggest;
det /= biggest;

