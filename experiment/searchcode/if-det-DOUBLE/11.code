import java.awt.Color;


public class ClusterShell extends Missile
{
private double detMillis;
super(w, h, x, y);
detMillis = millis;
this.setColor(Color.CYAN);
}

public boolean countdownDet(double millis)

