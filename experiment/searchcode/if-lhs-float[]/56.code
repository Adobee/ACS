public float sqrMagnitude()
{
return (x * x) + (y * y);
}

public float magnitude()
{
if(x == 0)return y;
return new Vec2(lhs.x * rhs, lhs.y * rhs);
}

public static Vec2 Divide(Vec2 lhs, float rhs)
{
if(rhs == 0)

