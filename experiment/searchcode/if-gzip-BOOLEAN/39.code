private final boolean gzip;

/**
* @param listener the data listener
* @param gzip if true use gzip handling with crc and trailers,
GZipUnpackState tr = null;
int remaining = inf.getRemaining ();
boolean finished = false;
if (gzip) {

