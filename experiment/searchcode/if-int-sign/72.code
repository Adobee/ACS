public int max(int a, int b){
int c = a-b;
int sa = sign(a);
int sb = sign(b);
int sc = sign(c);
return 1^n;
}
/**
* Returns 1 if positive, 0 is negative
*/
public int sign(int n){
return flip((n >> 31) &amp; 1);
}
}

