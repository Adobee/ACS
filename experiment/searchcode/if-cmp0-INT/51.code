private final String _PRF14 = &quot;#014_&quot;;
private final int USER_CLASS_COL = 5;

private String calcBase;
private String sharBase;//20151015	問題の・・・シェア集計ベース
List<String> totals = file2List(sumOfGT_Path, &quot;\t&quot;);

// loyBase+1がロイヤルテイベースと合計の位置が一致しているかどうか確認する
int whichCol = -1;
if (calcBase.equals(RepDict.PRC)) {

