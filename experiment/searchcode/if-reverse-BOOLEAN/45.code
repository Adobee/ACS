* @author dillon
*/
public class Rovic extends Victor
{
private boolean reverse=false;

public Rovic(int channel,boolean reverse)
public void set(double speed,boolean reverse)
{
if (reverse)
super.set(speed * -1.0);
else
super.set(speed);
}

}

