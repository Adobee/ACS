public int hashCode()
{
return 0;
// 省略equals的实现(可用eclipse自动生成)
}

@Override
public boolean equals(Object obj)
{
User tmp = (User)obj;
if (this == obj)
{
return true;
}
if (this.password.equals(tmp.password)

