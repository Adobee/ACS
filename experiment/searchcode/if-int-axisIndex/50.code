Map<String, Object> settingProperties = chartSetting.getPropertyValues();
int axisIndex = getAxisIndex(settingProperties);

if (log.isDebugEnabled())
throw new JRRuntimeException(&quot;No \&quot;id\&quot; property present in yAxis chart settings&quot;);
}

int axisIndex;
if (axisIndexProp instanceof Number)

