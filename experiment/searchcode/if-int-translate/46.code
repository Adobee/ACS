continue;
}
if (dataset.sampleTime > endTime) {
break;
}
int i1 = Math.round((float) (dataset.sampleTime - startTime) * timeScale);
f = vertMax;
}
int j1 = Math.round(f * dataset.scale);
if (dataset.isDisabledSample()) {

