package items;

import java.util.ArrayList;

public class OwnerType {

private static ArrayList<OwnerType> types = new ArrayList<OwnerType>();
for (OwnerType it : types) {
if (it.getName().equals(name)) {
return it;
}
}

return null;

