.println(&quot;****************prettyPrint********************&quot;);

if (i == 0 &amp;&amp; (!context.isBenchmark() || context.hasStats())) {
if (LOG.isLoggable(Level.FINEST)) {
* removals / nodes, sep, 100.0
* conflictnodes / nodes));
}
}

if (context.hasStats()) {

