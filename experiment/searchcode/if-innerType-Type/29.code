toReturn = type.substring(firstSplit, endSplit);
} else {
// If no innerType was provided, assume that it is Integer
toReturn = &quot;Integer&quot;;
public Vector<String> getImport() {
Vector<String> imports = super.getImport();
if(innerType.getImport().size() != 0){

