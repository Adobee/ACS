public boolean hasErrors(CodeMaterial from) {
DataType innerType = wExpr.getInnerWord().getDataType(from);
return innerType != DataType.REAL
WordInExpression inner = wExpr.getInnerWord();
DataType innerType = inner.getDataType(from);
if (innerType != DataType.REAL

