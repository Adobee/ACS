String lines[] = trendLines.split(&quot;,&quot;);

for (int i=0; i < lines.length; ++i)
{
String primaries[] = lines[i].split(&quot;:&quot;);
int count=0;
for (int j=0; j < primaries.length; ++j)
{
if (count==0)

