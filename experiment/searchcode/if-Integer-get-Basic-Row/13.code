&quot;信息提示框&quot;, JOptionPane.INFORMATION_MESSAGE);
return;
}

if (basic.getId() != 0) {
model.addRow(new Object[] { basic.getId(),
* dao.selectWareHouseByid(Integer .valueOf(conditionText));
* if (house.getId() != 0) { model.addRow(new Object[] {

