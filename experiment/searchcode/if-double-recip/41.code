public Quaternion reciprical () {
double norme = Math.sqrt(W*W + X*X + Y*Y + Z*Z);
if (norme == 0.0)
double norme = Math.sqrt(W*W + X*X + Y*Y + Z*Z);
if (norme == 0.0)
{
W = 1.0;
X = Y = Z = 0.0;
}
else
{
double recip = 1.0/norme;

