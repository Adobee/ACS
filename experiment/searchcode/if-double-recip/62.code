if (Math.abs(x - xm) <= (tol2 - 0.5 * (b - a))) {
return new double[]{x, fx};
}
if (Math.abs(e) > tol1) {
double r = (x - w) * (fx - fv);
} else {
d = p / q;
double u = x + d;
if (u - a < tol2 || b - u < tol2) {

