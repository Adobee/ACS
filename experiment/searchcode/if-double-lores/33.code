public static String locationToString(Location<? extends Extent> loc){
if(loc.getExtent() instanceof World){
World world = (World)loc.getExtent();
List<Text> lore = item.get(Keys.ITEM_LORE).get();
String loreS = null;
for(Text txt : lore){
if(loreS == null){

