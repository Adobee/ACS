public String pack(Long[] resolvedObject) {

String pacman = &quot;&quot;;
if (resolvedObject.length != 0)
for (int i = 0; i < resolvedObject.length; i++) {
public Long[] resolve(String packedObject) {

if (!packedObject.equals(&quot;&quot;)) {
String[] pacman = packedObject.split(separator);

