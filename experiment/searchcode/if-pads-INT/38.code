private PadHandler padhandler;			// Where pad presses are sent
private PadButton[] pads;				// Array of pads
private int padSize;
pads = new PadButton[121];

// Fill in pads
for(int y = 0; y < 16 * 8; y += 16) {
for(int x = 0; x < 9; x++) {

