Pads      pads      = m.getPads();

if (pads == null) {
pads = new Pads();
m.setPads(pads);
}
return pads;
Pads         cfg       = container.getPads();

if (cfg == null ) {
assert false : &quot;ERROR in removeIODirectives : should never reach this part ...&quot;;

