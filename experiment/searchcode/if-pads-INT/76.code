if (cfg.getBoolean(&quot;lobby.JumpPads.Enable JumpPads&quot;) == Boolean.FALSE.booleanValue()) {
return;
}
if (cfg1.getStringList(&quot;lobby.Disabed World for both JumpPads&quot;).contains(location.getName())) {
int ID8 = playerLoc.getWorld().getBlockAt(playerLoc).getRelative(1, -1, 1).getTypeId();
if ((ID == cfg.getInt(&quot;lobby.JumpPads.PressurePlateID&quot;)) &amp;&amp;

