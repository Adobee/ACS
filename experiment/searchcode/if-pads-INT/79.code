int loadTelepads()
{
int countTelepad = 0;

NBTTagList pads = nbtConfiguration.getNBTTagCompound().getList(&quot;TelePads&quot;);

for (int i = 0; i < pads.size(); i++)
{
NBTTagCompound padCompound = (NBTTagCompound) pads.get(i);

