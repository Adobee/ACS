private static int connectionTimeout = 0;
private static int readTimeout = 0;
private static int maxStale = 0;
//	public static boolean isFilesDownloaded = false;
final URLConnectionImageLoader imageDownloader = new URLConnectionImageLoader(this, connectionTimeout, readTimeout);
if (maxStale > 0) {

