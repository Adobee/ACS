* rien précisé dans sa requête.
* </p>
*/
private final int maxStale;

/**
* Indique que la donnée ne doit pas être persisté sur le chemin de retour
public CacheControl(final boolean noStore, final boolean isPublic, final int maxStale, final int maxAge) {
this.noStoreFlag = noStore;
this.publicFlag = isPublic;
this.maxStale = maxStale;

