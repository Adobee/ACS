public static void createLists(BinaryNode root) {
LinkedList<BinaryNode> currentDepth = new LinkedList<BinaryNode>();
if (root != null) {
currentDepth.getLast().next = b.left;
currentDepth.add(b.left);
}
}
if (b.right != null) {
if (currentDepth.isEmpty()) {

