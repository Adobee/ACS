ArrayList<Boolean> cachePresence;

public SensorCachePresence(Sensorable sensor) {
super(sensor);
return cachePresence.get(cachePresence.size() - 1);
}

public boolean getCache(int i) {
//        if (i >= 0 &amp;&amp; i < cachePresence.size()) {

