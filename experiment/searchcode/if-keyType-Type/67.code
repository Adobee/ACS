public class Criteria {

public enum FilterType {
MESSAGE, GEO
}

public enum KeyType {
if (keyId != null ? !keyId.equals(criteria.keyId) : criteria.keyId != null) return false;
if (keyType != criteria.keyType) return false;

