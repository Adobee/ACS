//Save the secondary value to ensure it is put at the end of the list
secondaryValue = cards[i].getValue();
}
else if (matches.get(i) == 2)
{
tiebreakerValues.add(0,  cards[i].getValue());
tiebreakerValues.add(0, cards[i].getValue());
}
else if (cards[i].getValue().ordinal() > tiebreakerValues.get(0).ordinal())

