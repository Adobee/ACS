private ArrayList<Card> drawDeck = new ArrayList<Card>();

/*  public Card draw(Boolean isAdventurer) { // vanilla draw and adventurer card
if(isAdventurer) {
for(Card card : drawDeck) {
if (card.type.equals(&quot;Treasure&quot;)) {

