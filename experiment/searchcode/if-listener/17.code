@Override
public void addPropertyChangeListener(IPropertyChangeListener listener) {
// Create the preferences listener if it
initializePreferencesListener();
// does not exist
addListenerObject(listener);
}

