int currentIndex;
if(highIndex == lowIndex){
currentIndex = highIndex;
}
else if((highIndex - lowIndex) % 2 > 0){
currentIndex = (highIndex - lowIndex + 1)/2 + lowIndex;
}

Node n = new Node(arr[currentIndex]);
if( currentIndex > lowIndex){

