*          sequence T2 < T1 is duration of the pulse.
*
*          returns 1 if T0 + n*T1 <= T < T0 + n*T1 + T2 (n is an integer)
public void compute() {
double T = variables[0].getValue();
double T0 = variables[1].getValue();

