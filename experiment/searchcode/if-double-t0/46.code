private boolean validTrajectory = false;
private Point sf;
private double deltaT;
private double t0;
camera.setDx(0);
camera.setDy(0);
} else {
if(t < t0 + deltaT) {
double[] mx = ax.getMatrix();
double sx = mx[0] + mx[1]*t + mx[2]*t*t + mx[3]*t*t*t;

