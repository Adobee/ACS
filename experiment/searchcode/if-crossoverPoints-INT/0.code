List<Integer> crossoverPoints = new ArrayList<>();
for (int i=0; i<numberOfCrossoverPoints; i++) {
crossoverPoints.add(rng.nextInt(parent1.totalParameters()));
}
Collections.sort(crossoverPoints);

int end = parent1.totalParameters();

