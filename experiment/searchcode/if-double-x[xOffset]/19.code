/** pps = pixels per second */
public void lerpToGameObject(GameObject object, double xOffset, double yOffset, double delta, double pps)
{
if (x < object.getX() + xOffset)
{
x += delta * pps;

if (x > object.getX() + xOffset)

