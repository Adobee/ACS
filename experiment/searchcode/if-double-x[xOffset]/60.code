private Frame2D source;
private int xOffset;
private int yOffset;
private int height;
private int width;
this.source = source;
if(xOffset >= source.getWidth() || xOffset < 0  || yOffset >= source.getHeight() || yOffset < 0)

