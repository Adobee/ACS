point = strValue.indexOf(DECIMAL_PART_CHAR.charValue());

String valClear;
/*// removing last zeros
if (point > -1) {
int i=strValue.length()-1;
return valueToCompare;
else {
int i=0;
for (; i < value.length ; i++) {
if (value[i] != valueToCompare.value[i])

