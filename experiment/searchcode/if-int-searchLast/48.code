int first = searchFirst(array, target, 0, n-1);
if(first == -1)
return first;
int last = searchLast(array, target, first, n-1);
return -1;
}

public int searchLast(int[] array, int target, int low, int high) {
if(array == null || array.length == 0)

