public PartialType getPartialType(int viewType) {

for (PartialType type : mPartialListType) {
if (type.getType() == viewType) {
setList(list, null);
}

/**
* The list passed to the adapter along with the partial types list
*
* @param list         The list passed to the adapter

