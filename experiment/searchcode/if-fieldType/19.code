public Field(FieldType fieldType, int link) {

if (fieldType == null)
throw new IllegalArgumentException(&quot;FieldType fieldType must not be null&quot;);

if (link < 0)
throw new IllegalArgumentException(&quot;int link must be 0 (zero) or greater&quot;);

