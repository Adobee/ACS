double globalX, globalY, angle;
double speedX, speedY;
protected double acc, speedMag, range, currDist;
public void setValues(double angle, double x, double y) {
this.angle = angle;

globalX = x;
globalY = y;
speedX = Math.cos(angle) * speedMag;

