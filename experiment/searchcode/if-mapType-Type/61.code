public enum MapType {
/**
* Normal map.
*/
NORMAL(&quot;normal&quot;),
/**
* Terrain (elevation) map.
for (MapType mapType : MapType.values()) {
if (mapType.getName().equals(name)) {
return mapType;
}
}
return null;

