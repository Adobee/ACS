/* 60 */     int toRead = Math.min(b.length - off, len);
/* 61 */     if (toRead <= 0) {
/* 62 */       return 0;
/*    */     }
/* 64 */     if (remain > 0)
/* 65 */       toRead = Math.min(remain, toRead);
/* 66 */     if (toRead <= 0) {

