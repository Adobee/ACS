public static List<Formiga> criar(int m, int n, Double feromonio[][], Double distancia[][],
double a, double b, double q0) {
List<Formiga> populacao = new ArrayList<>();
Formiga k = new Formiga(j, n, feromonio, distancia, a, b, q0);
if (j >= n - 1)
j = 0;
populacao.add(k);
}

return populacao;
}

}

