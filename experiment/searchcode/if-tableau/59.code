private int lg,col;
private int[][] tableau;

/**
* Constructeur de la classe
* @param int[][] plateau de jeu
return (lg+col>0);
}
/**
* Suivant
* @return tableau
*/
public Integer next() {
if (col>0) col--; else {lg--;col=tableau[0].length-1;}

