private int[] tableau;

public TriSelection() {
tableau = new int[100];
this.rempliTableau();
for (i = tableau.length - 1; i > 0; i--){
plusGrand = 0;
for (j = 1; j <= i; j ++) { //locate smallest
if(tableau[j] > tableau[plusGrand])

