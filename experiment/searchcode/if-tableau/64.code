public Map<String,String> codage(ArrayList<Couple> tableauProb){
//init tableau
ArrayList<Couple> tableauP = new  ArrayList<Couple>();
int r = (1 - tableauP.size()) % (this.destAlphLength - 1);
if ( r < 0) r += (this.destAlphLength - 1);
System.out.println(r);

while (tableauP.size() >= this.destAlphLength ){

