public boolean back(boolean tableau[][], int position, int cpt) {

System.out.println(cpt);

// Si tout le plateau a été visité
if (vide(tableau) == true)
System.out.println(cpt);

if (tableau[i][j] == true)
return back(tableau, position + 1, cpt);

if (tableau[i][j] == false) {

