stopwordsVocabalary = new HashSet<String>();
addDefaultStopWords();
}

private void addDefaultStopWords() {
// Stopwords list from Rainbow
*            the word to add
*/
public void add(String word) {
if (word.trim().length() > 0)
stopwordsVocabalary.add(word.trim().toLowerCase());

