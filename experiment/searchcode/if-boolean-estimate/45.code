private String conditionCount;

/** The estimate mins. */
private String estimateMins;

/** The total estimate mincnt. */
/** The last row. */
private boolean lastRow;

/**
* Checks if is last row.
*
* @return true, if is last row

