photo.setContentState(ContentState.LOADING);
if (saveTraffic) {
photo.getLoadOption().loadSource = LoadSource.ONLY_FROM_CACHE;
photo.setContentState(ContentState.LOADING);
if (saveTraffic) {
photo.getLoadOption().loadSource = LoadSource.ONLY_FROM_CACHE;

