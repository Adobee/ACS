*		the value currently referenced
*/
public S getSource()
{
if (isEmpty() &amp;&amp; isValidKeyAvailable())
{
source = loadSource();
}
return source;
}

/**
* Allows write access to the named attribute

