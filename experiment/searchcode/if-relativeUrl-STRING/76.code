cssUrl = cssUrl.substring(0, questionIndex);

}

linkInfo.setLinkWithoudParam(cssUrl);

String relativeURL = null;

if (ctx.getWebRoot() != null&amp;&amp; ctx.getWebRoot().length() <=cssUrl.length()) {
String webRoot=ctx.getWebRoot();
relativeURL = cssUrl.substring(ctx.getWebRoot().length());

}

String l_main_root= ctx.getMainRoot();

