public void addVector(Vector v){
Vector vector = this.getVector(v);
if(vector != null){
vector.mergeProperties(v.getProperties());
Vector vector = this.getVector(feature);
if(vector != null){
vector.addProperty(property);
} else{
vector = new Vector(feature);

