void handleItem(Item item) {
if (isLegendary(item)) {
return;
}

if (item.name.equals(&quot;Craftspeople Potion&quot;)) {
increaseQuality(item);
return;
}

if (isCheese(item)) {
handleCheese(item);

