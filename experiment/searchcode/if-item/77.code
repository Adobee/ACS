package item.discount;

import item.Item;
import checkout.Checkout.ItemListener;

public class Discount implements ItemListener {
this.counter = counter;
}

@Override
public void itemIs(Item item) {
if (item.equals(this.item)) {

