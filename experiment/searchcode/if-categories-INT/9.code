//remove coupon
for (int i = 0; i < categoriesList.size(); i++) {
Categories categories = categoriesList.get(i);
Categories categories = findCategoriesById(Integer.parseInt(category.getId()));
if (category.getIdParent() != null) { categories.setParent(Integer.parseInt(category.getIdParent().getId())); }

