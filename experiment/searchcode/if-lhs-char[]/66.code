public int compare(String lhs, String rhs) {
if (lhs == null) lhs = &quot;&quot;;
if (rhs == null) rhs = &quot;&quot;;
char[] buff = lhs.toCharArray();
Arrays.sort(buff);
lhs = new String(buff);
buff = rhs.toCharArray();
Arrays.sort(buff);

