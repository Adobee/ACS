sb.deleteCharAt(sb.length()-1);
return LHS.toString() + &quot; -> &quot; + sb.toString();
}

@Override
public int hashCode() {
return false;
Production other = (Production) obj;
if (LHS != other.LHS)
return false;
if (RHS == null) {

