public PListItemAssert hasType(PListItem.Type type) {
isNotNull();

if (actual.getType() != type) {
public PListItemAssert isBoolean(boolean value) {
hasType(PListItem.Type.BOOLEAN);

if (value != actual.asBoolean()) {

