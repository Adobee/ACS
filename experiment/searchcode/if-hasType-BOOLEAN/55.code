List<Type> tList=new ArrayList<Type>();
for(Type t : constraint)
{
if(c.hasType(t))
{
tList.add(t);
}
}

types = tList.toArray(TypeUtil.emptyTypeArray);
public int getId()
{
return c.getId();
}

public boolean hasType(Type t)
{
return c.hasType(t);
}

public Type[] getAllTypes()

