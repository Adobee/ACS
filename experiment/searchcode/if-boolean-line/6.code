line=buff.readLine();
joueur.setExp(Integer.parseInt(line));

line=buff.readLine();
if(Boolean.parseBoolean(line))
joueur.setCasque(new Casque(nom,pA,estEquip,encombrement,solidite,degres));
}

line=buff.readLine();
if(Boolean.parseBoolean(line))
{
line=buff.readLine();

