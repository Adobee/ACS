public boolean parallelTo( Line incomingLine )
{
boolean isParallel = false;

if ( getSlope() == incomingLine.getSlope() )
public boolean coefficentEquality(Line incomingLine)
{
boolean isEqual = false;

if ( a == incomingLine.getA() &amp;&amp;

