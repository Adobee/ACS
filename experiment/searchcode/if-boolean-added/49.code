* @param layer layer that has been added or removed
* @param added true if the layer was added, false if removed
*/
@ThreadPolicy(ThreadPolicy.ThreadId.ANY)
void notifyLayerChange(Layer layer, boolean added);
}

