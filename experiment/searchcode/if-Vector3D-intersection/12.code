import javax.vecmath.Vector3d;

import model.BoundingBox;
import model.Intersection;
import model.Material;
return normal;
}

@Override
public void intersect(Ray ray, Intersection intersection) {
Vector3d v = new Vector3d(ray.getOrigin());

