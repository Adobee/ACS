public class Timer {

private Long startTime = Long.valueOf(0);
private Long endTime   = Long.valueOf(0);

public void start() {
public void stop()  { endTime   = System.currentTimeMillis(); }

public Long duration() {
if (startTime == null || endTime == null) {

