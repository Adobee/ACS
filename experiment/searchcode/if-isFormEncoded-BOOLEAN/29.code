String httpMethod;
boolean hasBody;
boolean isFormEncoded;
boolean isMultipart;
String relativeUrl;
&quot;Multipart can only be specified on HTTP methods with request body (e.g., @POST).&quot;);
}
if (isFormEncoded) {

