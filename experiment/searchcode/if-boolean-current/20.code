static boolean a30 = true;


public ExplosionJPF_Core(int max) {
int i = 0;

int current = 0;
if (current == max) return;
if (a1 = gb()) {i += Math.pow(2, current);}if (++current == max) return;
if (a2 = gb()) {i += Math.pow(2, current);}if (++current == max) return;

