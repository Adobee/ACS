private IterationDao iterationDao;

private static final int DEFAULT_VAL = 1;
private static final int NOT_DEFAULT_VAL = 0;

@Override
public List<Iteration> list(int pageNum, int pageSize) {
return iterationDao.list(pageNum, pageSize);

