while(iterate.data!=prev &amp;&amp; iterate.next!=null){
iterate = iterate.next;
}

//check if Node with given value is within the List
if(iterate.data!=prev){
return false;
}

Node toInsert = new Node(iterate,data,iterate.next);

