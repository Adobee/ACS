package math;

public class NumberTheory {

/**
* Returns true, if the given number can be divided by the given divisor, s.t. number/divisor is an integer. The divisor is not allowed to be 0 here!
public static boolean isDivisible(double number, double divisor) {
double erg = number / divisor;
double frac = erg - (int) erg;
return frac == 0;
}
}

