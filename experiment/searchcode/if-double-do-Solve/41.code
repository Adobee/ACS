* Solve a linear equation for:<br/>
* ax + b = 0
*/
@Override
public double[] solve(double... coefficients) throws IllegalArgumentException {
return new double[]{cbrt(div(-d, a))};
}

if (isZero(d)) {
double[] quadResults = Solvers.Quadratic.solve(c, b, a);

