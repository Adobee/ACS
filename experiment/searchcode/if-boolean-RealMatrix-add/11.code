.toArray(double[][]::new)
);
if (symmetric)
a = a.add(a.transpose()).scalarMultiply(0.5);
if (diagonalDominant)
public LinearSystem generateInteger(int vars, boolean symmetric, boolean diagonalDominant) {
RealMatrix a = MatrixUtils.createRealMatrix(

