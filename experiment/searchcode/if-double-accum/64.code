&quot;, points, time (secs), points/sec&quot;);

double totalAccum = 0.0;
double spanAccum = 0.0;
int spanCount = 0;
totalCount += groupSize;
spanCount += groupSize;
if (spanAccum > spanTime) {
writer.println(tree.pointCount()+

