while(sc.hasNext()){
long a = sc.nextLong();
long b = sc.nextLong();

long gcd = gcd(a,b);

long lcm = a*b/gcd;
System.out.println(gcd + &quot; &quot; + lcm);
}
}

private long gcd(long a, long b) {

