long n = in.nextLong();
long d = in.nextLong();
long m = in.nextLong();
long l = in.nextLong();
in.close();
long beginNextPlatform = (i + 1) * m;
x = (endPlatform / d) * d + d;

if (i == n - 1 || x < beginNextPlatform) {

