rank[sa[i]] = i;
}

int h = 0;
for (int i = 0; i < N; i++) {
if (rank[i] > 0) {
makeLCPArray(s, sa, LCP);
}

public static void makeSuffixArray(int[] s, int[] SA, int n, int K) {
int n0 = (n + 2) / 3;

