private SettingsCache settingsCache;

private class UserWeeks{
public User user;
public int weeks;
public static class UserWeeksComparator implements Comparator<UserWeeks>{

public int compare(UserWeeks user1, UserWeeks user2)
{
if(user1.weeks < user2.weeks)

