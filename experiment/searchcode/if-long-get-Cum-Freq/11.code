tree[idx] += val;
idx += idx &amp; (-idx);
}
}
int cumFreq(int idx, int [] tree) {
int ret = 0;
while(idx > 0) {
int binarySearch(int l, int r) {
if(l == r)
return l;
int mid = (l+r)/2;
int f = cumFreq(mid+1, singleTree);

if(f == mid+1)

