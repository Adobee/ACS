int binarySearch(int l, int r) {
if(l == r)
return l;
int mid = (l+r)/2;
int f = cumFreq(mid+1, singleTree);

if(f == mid+1)
arr = new int[K];
period = new int[K+1];

arr[0] = getInt();
manageFreq(arr[0] + 1, +1);
b = getLong();

