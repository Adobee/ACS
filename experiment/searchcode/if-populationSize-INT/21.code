public class Population<T extends Individual> {

/**
*  种群规模
* */
private int populationSize;

/**
*  种群成员
result.add((T) individual2.inoculate(individual1));
}
// 处理单数情况
if (populationSize % 2 == 1) {
int index = orderList.get(populationSize - 1);

