* manages game state
*/
public class Game {

static float TimeoutPeriod = 100;

// darkness timeout
float timeout;
timeout -= interval.next();
if (timeout < 0) {
reset();
}
}

/**
* get normalized timeout value

