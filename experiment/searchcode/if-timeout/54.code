package com.vibrantmedia.qacommon.browser;

/**
* A class to represent a Timeout.
*
* @author aknee
* @param timeoutInMilliseconds the number of milliseconds to wait until the given operation should timeout.
*/
public Timeout(long timeoutInMilliseconds) {
if (timeoutInMilliseconds < 0) {

