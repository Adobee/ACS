public Interval instantBefore() {
return this.minus(new Interval(1, Moment.NANOSECOND));
}

public boolean equals(Object other) {
boolean result;

if(this == other)
result = true;
else
if(other == null)

