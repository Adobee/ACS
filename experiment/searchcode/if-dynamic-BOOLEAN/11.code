import java.util.Map;

import org.bbly.server.bean.Dynamic;
import org.bbly.server.bean.Report;
import org.bbly.server.mapper.DynamicMapper;
mapper.deleteDynamic(dynamic);
}

public boolean checkReport(Report report)
{
int row = mapper.checkReport(report);
if(row > 0)

