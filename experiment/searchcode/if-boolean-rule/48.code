public boolean matchesResponse( MHttpResponse response, MResultCache resultCache )
{
boolean match = true;
int ruleCount = ruleList.size();
return false;
}
}

boolean ruleMatches = resultCache.getResult( rule );
if( ruleMatches == false )

