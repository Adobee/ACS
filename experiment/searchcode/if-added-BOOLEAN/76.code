int c = getColumn();
int r = getRow();
Position toBeAdded;
toBeAdded = new Position(c + 2, r + 1);
if (toBeAdded.isValid()) {
toBeAdded = new Position(c + 2, r - 1);
if (toBeAdded.isValid()) {
returned.add(toBeAdded);
}
toBeAdded = new Position(c - 2, r + 1);

