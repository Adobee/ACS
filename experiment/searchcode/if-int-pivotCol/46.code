//int pivotCol = colMostNegativeCost(tableau);
int pivotCol = firstIndexNegativeCost(tableau);

if(pivotCol == -1) { // optimal
int pivotRow = minRatioRule(pivotCol);

//int pivotRow = lexicographic_Rule(pivotCol);

if (pivotRow == -1) {

