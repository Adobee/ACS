return this.values[row][col];
}

public int pivotRow(int pivotCol) {
int pivotRow = 0;
double minValue = NON_ZERO_DIVISIBLE;

for (int row = 1; row < rows; row++) {
if (values[row][pivotCol] != 0) {

