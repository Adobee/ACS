long n = (curTime - lastSampleTime) / timeInteval;
if (n >= sampleCount) {
for (int i = 0; i < sampleCount; i++) {
public Sampler compareAndCreate(int sampleCount, int limit) {
if (this.limit != limit || this.sampleCount != sampleCount) {

