// FIXME  cimage is always null!!!
if (cimage == null)
{
bmpWithContours = Bitmap.createBitmap(imgMAT.cols(), imgMAT.rows(), Bitmap.Config.ARGB_8888);
// vertices to resultant sequence
if( maxCosine < 0.3 )
squares.add(approx);

