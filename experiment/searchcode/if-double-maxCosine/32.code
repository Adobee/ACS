Imgproc.isContourConvex(new MatOfPoint(approxContour.toArray())))
{
double maxCosine = 0;
double minCosine = 0;
for (int j = 2; j < approxContour.total()+1; j++)
Imgproc.isContourConvex(new MatOfPoint(approxContour.toArray())))
{
double maxCosine = 0;
double minCosine = 0;
for (int j = 2; j < approxContour.total()+1; j++)

