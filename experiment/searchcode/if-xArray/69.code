class GraphCanvas extends Canvas {

Color oldColor;

Color newColor;

static int[] xArray;
static int[] yArray;

public GraphCanvas() {
int nPoint = 10;
g.setColor(newColor);
g.drawPolyline(xArray, yArray, nPoint);
g.setColor(oldColor);

