weapon = null;
size = 0;
}

@Override
public void updateState(double delta) {
updateTimers(delta);
if (!paralyzed) {
public double getAngleToTarget(Vec2 target) {
Vec2 toTarget = target.sub(body.getWorldCenter());
double normalizedAngle = GameUtils.normalizeAngle(body.getAngle());

