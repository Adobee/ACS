private double getNormalizedAngleInDegrees(double angleInDegrees) {
double normalizedAngle = angleInDegrees;
while (normalizedAngle >= 360) {
return normalizedAngle;
}

private boolean isInFirstQuadrant(double angleInDegrees) {

