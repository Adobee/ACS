* parameter specifies how many tokens are produced by this actor. If the value
* of tokenLimit is a nonnegative integer, then the actor produces that many
* @exception NameDuplicationException
*                If the name of this actor or the tokenLimit parameter is
*                not unique within the container.

