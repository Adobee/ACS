int equationLength = equation.length();
int firstSpace = equation.indexOf(&quot; &quot;);
String firstFraction = seperateString(equation, firstSpace);
int divisor = fraction.indexOf(&quot;/&quot;);
int underscore1 = fraction.indexOf(&quot;_&quot;);
if(divisor != -1) {
if (underscore1 != -1) {

