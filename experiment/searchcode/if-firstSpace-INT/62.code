String lines[] = documentText.split(&quot;\\r?\\n&quot;);

for (int i = 0; i < lines.length; i++) {
String line = lines[i];
int firstSpace = line.indexOf(&#39; &#39;);

// line has no spaces
if (firstSpace < 0) {

