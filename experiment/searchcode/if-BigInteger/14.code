import java.math.BigInteger;

public class Criptografia {

public static BigInteger Totiente(BigInteger p, BigInteger q) {
BigInteger r;
//int E = e.intValue();

r = qe.mod(e);


if (r == BigInteger.ZERO) {
return (c.divide(e)).mod(qe.divide(e)); // retorna (c/a) % (b/a)

