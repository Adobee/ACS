for (String url : input) {
String title = TitleExtractor.getPageTitle(url);
boolean titleFound = false;
for (int i = 0; i < title.length() - 1; i++) {
if (Character.isUpperCase(title.charAt(i)) &amp;&amp; Character.isUpperCase(title.charAt(i))) {

