this.direction = direction;
this.map = map;
passed = new int[8][8];

}

public boolean findPath() {
Loc curLoc = new Loc();
for (int i = 0; i < path.size(); i++) {
Loc loc = path.get(i);
if ((loc.nextLoc - 1) == 0)

