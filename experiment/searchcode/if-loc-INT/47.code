String nextLine = in.nextLine();
for (int j = 0; j < X; j++)
{
char nextSpot = nextLine.charAt(j);
if (nextSpot == &#39;K&#39;)
locations.addLast(knight);
while (true)
{
Location currentLoc = locations.removeFirst();
if (currentLoc.y == hay.y &amp;&amp; currentLoc.x == hay.x)

