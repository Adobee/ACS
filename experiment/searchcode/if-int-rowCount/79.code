boolean swap = false;
if (rowCount > columnCount) {
swap = true;
int temp = rowCount;
rowCount = columnCount;
int curColumn = (rowCount - 1) / 2;
int curRow = 0;
int direction = 1;
if (rowCount % 2 == 0) {
for (int j = 0; j < rowCount / 2; j++)

