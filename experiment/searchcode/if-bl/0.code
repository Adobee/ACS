private List<BlTree> blList;

public void addToBlList(BlTree blTree) {
if (blList == null) {
blList = new ArrayList<BlTree>();
public List<Bl> getBlList(BlTree blTree, List<Bl> blList) {
blList.add(blTree.getBl());
if (blTree.getBlList() != null) {

