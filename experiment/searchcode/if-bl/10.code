public static int random(int n, int[] bl)
{
int r = rand.nextInt(n-bl.length);
if (bl.length==0 || r<bl[0]) return r;
int available_nums = (bl[m] - bl[left]) - (m - left);
if (available_nums < offset) {

