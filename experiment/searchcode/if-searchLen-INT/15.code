return search(8,4);
}
public SearchResult search(int skipCount,int searchLen)
{
if(searchLen<=0 || skipCount<=0)
kcur+=c0;
continue;
}
SearchResult sr=bs.greedySearch(this.key, kcur, kcur+searchLen);
if(sr.matchCount()==0)

