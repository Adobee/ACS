* (package private) constructor, BitSieve(BigInteger base, int searchLen). The length of the
* sieve generated by this constructor was chosen for performance; it controls a tradeoff
BitSieve(BigInteger base, int searchLen) {
/*
* Candidates are indicated by clear bits in the sieve. As a candidates nonprimality is

