return;
}if(str.equals(&quot;-&quot;)){
realPart = Fraction.ONE.negate();
imaginaryPart = Fraction.ZERO;
public double getArgument(){
if(realPart.getNumerator() == 0 &amp;&amp; imaginaryPart.getNumerator() == 0){

