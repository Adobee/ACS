* @param event     being offered to the conduit
* @param timeoutMs how long the offer stands in milliseconds
* @return whether or not the event was accepted
* @return an incoming event or <code>null</code> if one was not seen within the <code>timeoutMs</code>
*/
@Nullable
Event await(long timeoutMs);

}

