* one of them.
*/
static {
boolean success = dir.mkdirs();
if (success) {
System.out.println(&quot;Created the modelDB directory.&quot;);
// DbShutdownHook shutdownHook = new DbShutdownHook(env, store);
// Runtime.getRuntime().addShutdownHook(shutdownHook);
// addShutdownHook();

