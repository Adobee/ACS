if (smallLogFlag)
{
out = Math.log10(in) / LOG2;
} else {
boolean negFlag = (in < 0.0);
if (negFlag) in = -in;
if (smallLogFlag)
{
out = Math.log10(in);
} else {
boolean negFlag = (in < 0.0);
if (negFlag) in = -in;

