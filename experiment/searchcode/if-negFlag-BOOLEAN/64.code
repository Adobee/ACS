public double adjustedLog10(double val) {
boolean negFlag = (val < 0.0);
if (negFlag) {
public double adjustedPow10(double val) {
boolean negFlag = (val < 0.0);
if (negFlag) {
val = -val;
}
double res;

