if (smallLogFlag)
{
out = Math.log10(in);
} else {
boolean negFlag = (in < 0.0);
if (negFlag) in = -in;
if (smallLogFlag)
{
out = Math.log(in);
} else {
boolean negFlag = (in < 0.0);
if (negFlag) in = -in;

