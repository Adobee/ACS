private Stack<Tasks> listOperation = new Stack<>();

public Tasks getCurrentOperation() {
if (listOperation != null &amp;&amp; listOperation.size() > 0) {
return null;
}
}

public int remove() {
int nElement = 0;
if (listOperation != null &amp;&amp; listOperation.size() > 0) {

