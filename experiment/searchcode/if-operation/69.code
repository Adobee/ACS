ConstExpression g = ex.getLeft_Expr();
ConstExpression d = ex.getRigth_Expr();
String operation = ex.getOperation();

if (  operation.equalsIgnoreCase(&quot;+&quot;)){
valeur = g.executeOperation() + d.executeOperation();
}
else
{
if (  operation.equals(&quot;*&quot;) ){
valeur = d.executeOperation() * g.executeOperation();

