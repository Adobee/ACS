package ua.goit.logic;

import ua.goit.libs.Expression;
import ua.goit.libs.Operation;

import java.util.ArrayList;
String result = null;
for (Operation operation : operations) {
if (expression.getOperation().equals(operation.getSign())) {

