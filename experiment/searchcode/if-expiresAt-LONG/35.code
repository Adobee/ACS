public static String getRedisValue(int count, String userId, String existingIds,
long recordedEpochTime, long expiryTime) {
if (existingIds.isEmpty()) {
String userIdsString = splits[1];
long recordedEpochTime = Long.parseLong(splits[2]);
long expiresAt = Long.parseLong(splits[3]);

