case &#39;[&#39;:
int quoteLen = text.indexOf(&#39;[&#39;, 1);
assert quoteLen > 1;
if (openQuote == null)
return &quot;ERROR&quot;;

final int quoteLen = openQuote.length();

final int length = text.length();

