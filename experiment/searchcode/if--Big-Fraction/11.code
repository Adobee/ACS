public static final int MAXFRACTION = 1000;
public static final int EPS = 5;
int fraction;

BigDNum()
void correctFraction(BigDNum bdnum)
{
if(fraction > bdnum.fraction) {
bdnum.bnum.digit_shift(fraction - bdnum.fraction);

