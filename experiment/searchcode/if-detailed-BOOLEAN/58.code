private final List<DetailedTcpBench> benches = new ArrayList<>();

private final boolean detailedEnabled;

private volatile long totalTimeMillis;
public DetailedTcpBenchSet(boolean detailedEnabled) {
this.detailedEnabled = detailedEnabled;
}

@Override

