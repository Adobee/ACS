private void shotWall(Threshold th, double alpha) {
double xrange = range(V, alpha);
if (xrange < th.x) {
double currAlpha = -1;
for (int i = 0; i < M; i++) {
double alpha = alpha_th0[i];
if (alpha > currAlpha) {

