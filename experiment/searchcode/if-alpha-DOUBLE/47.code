public class ExponentialSignal implements Function {


private final Double alpha;
public ExponentialSignal(Double alpha) {
this.alpha = alpha;
Double[] result = (Double[]) Array.newInstance(Double.class, tArray.length);
for (int i = 0; i < tArray.length; i++) {
if(alpha == 0) result[i] = 1.0;

