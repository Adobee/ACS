package my.test.mq.utils;

import org.codehaus.jackson.map.ObjectMapper;



/**
* Jackson 单例模式获取全局唯一 ObjectMapper 对象
public static ObjectMapper getInstance(){
if(objectMapper == null){
synchronized(ObjectMapperInstance.class){
if(objectMapper == null){

