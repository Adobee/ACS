private OldState[] oldState = null;
private int maxOldSteps;
private int firstIndex, endIndex;
firstIndex++;
}
oldState[firstIndex].setOldScore(oldScore).setOldCardMap(cardMap);
if (firstIndex == endIndex) {

