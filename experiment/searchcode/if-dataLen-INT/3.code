else if(rankdataB == null)
return 1;

int l = (one.dataLen() < two.dataLen()) ? one.dataLen() : two.dataLen();
int off1 = one.dataOffset();
int off2 = two.dataOffset();

if(rankdataA[off1] >= 0 &amp;&amp; rankdataB[off2] < 0){

