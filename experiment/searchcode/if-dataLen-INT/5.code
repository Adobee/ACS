else if(rankdataB == null)
return 1;

int l = (one.dataLen() < two.dataLen()) ? one.dataLen() : two.dataLen();
return (rankdataA[off1 + i] &amp; 0xff) - (rankdataB[off2 + i] &amp; 0xff);
}
//		if(one.dataLen() != two.dataLen())
//			return one.dataLen() - two.dataLen();

