//		System.out.println(&quot;包长度=&quot;+packLength);
if (packLength<=MaxPackLong) {
buffList.add(buf);
}else{
int forTime=(int) packLength/MaxPackLong;
public IPack DeCode(IoSession session, IoBuffer buffer){
byte[] dst;
if (msgLength<0) {
//			System.out.println(buffer);
int bufferLimit=buffer.limit()-5;

