if (items.length == 0)
return objectString;
int bufferLimit = getBufferLimit();
for (int i = index; i < bufferLimit; i++) {

String item = items[i];
int num = i + 1;
objectString += &quot;\n&quot; + num + &quot;.&quot; + Application.tree.getNode(item).getTitle();

