// ñîçäà¸ì ïåðåìåííóþ VALUE òèïà double
private final double value;

// ñîçäà¸ì êîíñòðóêòîð
public Number (double num){
public int comp(Number number){
if(value<number.getValue()) return -1;
if(value>number.getValue()) return 1;
return 0;

