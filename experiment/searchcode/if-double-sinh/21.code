Function g = transform(f, min, max);
if(!x.containsKey(m)) computePointsAndWeights(m);
List<Double> X = x.get(m);
List<Double> W = w.get(m);
for(int k = 0; k < lim; k++)
{
double t = k * h,
X = Math.tanh(Math.PI / 2d * Math.sinh(t)),

