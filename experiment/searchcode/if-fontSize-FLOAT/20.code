Paint paint = new Paint();
int height = getHeight();
int width = getWidth();
int fontsize = 12;
if (width > 700) {
int nextParcent = batteries[i + 1].getParcent();
long nextTime = batteries[i + 1].getTime();

float xs = (fontsize * 1.5F) + (time - leftLimit) / rowScale;

