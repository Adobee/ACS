public void fillQueueFromFile() {
if (hits.size() == 0) {
boolean hasReachedEnd = !reader.readNextPageAsText();
public void fillQueueFromSma() {
if (hits.size() == 0) {

boolean isThereMoreHits = smaCollectLog.retrieveMoreHits();

