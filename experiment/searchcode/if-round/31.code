import org.springframework.util.Assert;

import repositories.RoundRepository;
import domain.Round;

@Service
public class RoundService {
Round round = null;
if(!rounds.isEmpty()){
round = rounds.get(0);
}
return round;
}

}

