throw new NotYourTurnException();

if ( throwStep == 1 &amp;&amp; (lastRoundStatus.equals(RoundStatus.SPARE) || lastRoundStatus.equals(RoundStatus.STRIKE)) ) {
this.roundScore += score*2;
}
else if( throwStep == 2 &amp;&amp; (lastRoundStatus.equals(RoundStatus.STRIKE))){

