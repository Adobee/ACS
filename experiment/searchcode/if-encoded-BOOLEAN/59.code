//falls es irgendwo keinen gibt, so ist ein Opfer nicht geschützt.
for (Weight w : v.wrapped.getWeights()) {
if (w.count(encoded) != 0) {
for (Relationable r : w.getFriends()) {
if (r.count(encoded) != 0) {
friend = true;
break;

