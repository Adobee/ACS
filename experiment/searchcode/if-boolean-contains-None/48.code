* @param Integer (intObj)
*/
public void add( Integer intObj )
{
boolean counter = false;

if (!model.contains(intObj))
{
if (model.size() == 0 || intObj >= model.get((model.size() - 1)) )
model.add( intObj );

