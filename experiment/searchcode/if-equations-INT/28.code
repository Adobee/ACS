private static int equations = 0;

private static void quicksort(int[] array, int low, int high) {
int i = low, j = high;
int pivot = array[low];
while (i <= j) {
Quicksort.setEquations(getEquations() + 1);
while (array[i] < pivot) // IF LOW < PIVOT

