OperatorExpression newEquation = (OperatorExpression)equation2.replace(f, equation.getRhs());
if (! newEquation.equals(equation2))
newEquations.add(newEquation);
List<Expressable> newEquations = new ArrayList<>();
List<Field> fields = extractAllFields(equations);
for (int i=0; i < 100; ++i) {

