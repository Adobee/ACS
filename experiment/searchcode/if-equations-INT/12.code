class EquationSystem
{
/** liczba równañ uk³adu */
protected int numEquations;

/** macierz wspó³czynników równania */
min_coef = i;

if(coefficients[min_coef][actual] != 0)
{
for(int i = 0; i < num_equations; i++)

