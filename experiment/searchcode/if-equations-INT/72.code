private int targetNum;
private int maxTargetNum;
private int numEquations;

private PuzzleElement[][] puzzleGrid;
this(seed, GameConfig.PUZZLE_SIZE, GameConfig.MAX_TARGET_NUM, GameConfig.MIN_EQUATIONS);
}

public PuzzleGenerator(long seed, int size, int maxTargetNum, int numEquations) {

