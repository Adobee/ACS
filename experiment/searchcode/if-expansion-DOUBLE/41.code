* base and height of the triangle
*/
private final int xExpansion;
private final int height;

/**
* constructor
if (!super.equals(obj)) return false;
Triangle other = (Triangle) obj;
if (xExpansion != ((Triangle)other).xExpansion) return false;

