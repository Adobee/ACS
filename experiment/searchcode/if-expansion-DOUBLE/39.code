/**
* compute the value of pi using series expansion
* pi = 4(1-(1/3+(1/5)-(1/7)+...)))
*/

public class e4computepi {
sum += (double)4 / denom;
} else if (denom % 4 == 3) {
sum -= (double)4 / denom;
} else {

