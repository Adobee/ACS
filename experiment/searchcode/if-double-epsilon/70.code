public class EpsilonGreedyFunction implements ActionProbabilityFunction {
private double epsilon;


@Override
ArrayList<Double> probabilities = new ArrayList<>();
double p = epsilon / (scores.size() - 1);
for (int i = 0; i < scores.size(); i++) {

