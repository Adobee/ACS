for ( int j = 0; j < bufferSize; j++ ) {

temp *= longArray[i+j];
}

if ( temp > productSum )
for( int i = 0; i < (longArray.length - bufferSize) ; i++ ) {

long temp = 1;

if ( i + bufferSize*50 < 1000 ) {

