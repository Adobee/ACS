
package AcopiTetris;

public class TestScript
{
public int currentLevel;

public TestScript()
currentLevel = 4;
}

public int getNextLevel(Boolean currentLevelPass)
{
if ((currentLevel==8 || currentLevel==5) || (currentLevel==3 || currentLevel==1))

