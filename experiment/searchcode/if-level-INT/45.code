public int calRemainExp(User user) {
int exp = user.getExp();
int level = calLevel(user);

if(level >= 5)
int exp = user.getExp();

int level;

if(exp < level_exp[0])
level = 0;
else if(exp < level_exp[1])

