Node<Anytype> newNode = new Node<Anytype>(data,null);
if(front == null) {
front = newNode;
back = front;
public void dequeue(){

if(isEmpty()) { return ; }
front = front.next;

}
public Node peek(){

