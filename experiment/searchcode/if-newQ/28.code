Queue.Node mergedResult = null;


while (head!=null) {
Queue.Node next = head.next;
if( (Integer)head.value < split) {
head = next;
}
// merge

if(lessList==null)
return moreList;

while (lessList.next!=null){

