int len1 = a.length();
int len2 = b.length();

if (len1 == 0) {
return b;
int digit1 = (int)(a.charAt(i) - &#39;0&#39;);
int digit2 = (int)(b.charAt(j) - &#39;0&#39;);
int digit = digit1 + digit2 + carry;

