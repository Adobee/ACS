public static InterActivityGestureListener getInstance(InterActivity interActivity){
if(instance == null){
instance = new InterActivityGestureListener(interActivity);
}
return instance;
}

@Override
public boolean onScroll(MotionEvent e1, MotionEvent e2, float distanceX,

