* A: There are some reasons for this decision:
* 1. If using lucene query classes directly, client application will be coupled to the search engine, while we may
public static SearchQuery termClause(String field, Serializable term, QueryRewrite rewrite) {
/*
* if a clause contains multiple terms (i.e. terms are delimited by blank) and its field is a tokenized fdt

