import com.pars.marc.index.Term;
import com.pars.marc.search.query.QueryRewrite;
import com.pars.marc.search.query.SearchEngineDocumentModel;
public SearchQuery termClause(QueryRewrite rewrite) {
if (rewrite == null) {
return SearchQuery.termClause(indexFieldName(), value);

