public String nextDay() { // method to calculate the nextdya
String message= &quot;&quot;;
int dayIndex = getDayIndex(this.day);
if (dayIndex>=0 &amp;&amp; dayIndex<6){ // condition to make sure we dont get a index error
String previousday =&quot;&quot;;
int dayIndex= getDayIndex(this.day);
if (dayIndex>0 &amp;&amp; dayIndex<=6){
previousday= days[dayIndex-1];

