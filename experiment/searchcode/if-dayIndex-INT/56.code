public boolean isLeap(int year) {
boolean result = false;

if ( year%4 == 0 ) {
if ( year%100 == 0 )
public int solve( int startYear, int endYear ) {
Days days = null;
Months months = null;

int dayIndex = 0;
int count = 0;

