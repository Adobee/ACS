// is Friday, which has an index of 5 in days[].
int dayIndex = 5;

// I&#39;d like to be able to do this instead (more user-friendly),
String firstDayOfYear = &quot;Friday&quot;;
int dayIndex = days[].getIndex(firstDayOfYear);
*/

for(int dayInYear = 1; dayInYear <= daysInYear; dayInYear++) {

