int consecutiveShiftsNum = 0;

int startingDay = 0;

// iterate days
for (int dayIndex = startingDay; dayIndex < taskInfo.getNumberOfDays(); dayIndex++) {
int shiftIndex = rand.nextInt(currShifts.size());
Shift shift = currShifts.get(shiftIndex);

if (dayIndex == 0) {

