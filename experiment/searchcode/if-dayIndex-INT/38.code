public String calculateDay(int numOfDays) {
String message = &quot;&quot;;
int dayIndex = getDayIndex(this.day);
int i=0;
dayIndex += numOfDays;
if (dayIndex != -1) {
while (dayIndex > days.length) {

