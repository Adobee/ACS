public int getDaysForAmonth(int month)
{
return months.get(month).size();
}

public WorkUnit getWorkUnit(int month, int dayIndex)
{
int monthSize = months.get(month).size();

if(monthSize == 0)
return null;

if(dayIndex < monthSize)

