long key = x + 100*y;

if (results.containsKey(key))
{
return results.get(key);
}

long count = 0;


if (y < size - 1)
count += rightCount;
}
else
{
count += 1;
}
if (x < size - 1)
{
long downCount = GetCount(x + 1, y);

