private int maxCacheSize = -1;
private Map<K, Long> timeMap = new HashMap<K, Long>();

public LruMap(int maxCacheSize) {
super();
this.maxCacheSize = maxCacheSize;
}

public LruMap(long maxCacheTime, int maxCacheSize) {

