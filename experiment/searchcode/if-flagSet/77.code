public final class FlagSet<T extends Flag> {

/**
* The current set Flags, as an {@code int}.
*/
private int flags;
return Integer.hashCode(flags);
}

@Override
public boolean equals(Object obj) {
if (obj instanceof FlagSet) {

