import org.metricstogo.base.layer.LayerObject;
import org.metricstogo.base.layer.implementation.PackageLayer;
import org.metricstogo.base.structure.elements.PropertyKey;
for (LayerObject layerObject : listOfLayerObjects) {
if(PackageLayer.class.isInstance(layerObject)){

