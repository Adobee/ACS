out.println(&quot;</steps>&quot;);

if (this.splits.size() > 0) {
XMLUtil.printIndent(out, indent++);
XMLUtil.printIndent(out, --indent);
out.println(&quot;</splits>&quot;);
}

if (this.joins.size() > 0) {

