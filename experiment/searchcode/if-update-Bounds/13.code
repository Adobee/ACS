bounds.y += yVel;


if (bounds.x + bounds.width / 2 >= gameScreen.getWidth()) {
System.exit(0);
} else if (bounds.x - bounds.width / 2 <= 0) {
System.exit(0);
}

if (bounds.y + bounds.height / 2 >= gameScreen.getHeight()) {

