public static String findRange(int[] a, int number, int start, int end)
{
if (start > end)
{
return &quot;&quot;;
}
int mid = (end + start) / 2;
String rangeStart = &quot;&quot;, rangeEnd = &quot;&quot;;

if (a[mid] > number)

