int[] res = new int[] { -1, -1 };

if (A == null)
return res;

int lower = 0;
int upper = A.length - 1;
int mid = 0;
while (lower < upper) {
mid = (int)(1.0 * (lower + upper) / 2 + 0.5);
if (A[mid] > target)

