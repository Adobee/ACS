return new Font(&quot;Arial&quot;, Font.PLAIN, fontHeight);
}
}

private byte[] hex2byte(String str) {
if (str == null)
int len = str.length();
if (len == 0 || len % 2 == 1)
return null;

byte[] b = new byte[len / 2];

