sb.append(s);
sb.append(&quot;&#39;,&#39;&quot;);
}
if (sb.charAt(sb.length() - 1) == &#39;\&#39;&#39;) {
sb.deleteCharAt(sb.length() - 1);
if(sb.charAt(sb.length() - 1) == &#39;,&#39;){
sb.deleteCharAt(sb.length() - 1);
}
}
return sb;
}
}

