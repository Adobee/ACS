public double Power(double base, int exponent) {
boolean b=false;
if(exponent<0)b=true;
if(exponent==0)return 1D;
if(exponent==1)return base;
exponent=Math.abs(exponent);
double temp=base;
for(int i=2;i<=exponent;i++){

