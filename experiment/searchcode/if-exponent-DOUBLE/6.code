public class Power {
public double calculate(double base, double exponent) throws IllegalArgumentException {
if (base == 0 &amp;&amp; exponent < 0) {
return 1;
} else if (isDoubleNotInt(exponent)) {
return Math.pow(base,exponent);
} else if (exponent < 0) {

