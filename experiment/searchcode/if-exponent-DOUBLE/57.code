double Power(double base, int exponent) {
if(base==0 || base==1)
return base;
if(exponent==0)
return (double)1;

double rst = exponent>0?power(base, exponent):-1* power(base, -1*exponent);
DecimalFormat dfDecimalFormat = new DecimalFormat(&quot;######.00000&quot;);

