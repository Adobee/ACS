output.print(valueOf(index));
}
}
}

private boolean multiplesOf5(int index) {
return multiplesOf(5, index);
}

private boolean multiplesOf3(int index) {
return multiplesOf(3, index);
}

private boolean multiplesOf(final int multiples, int index) {

