grids[bottom] = true;
}

private boolean isIndexOutOfBound(int i, int j) {
if (i < 1 || j < 1 || i > size || j > size) {
if (j < size)    union(index, getIndex(i, j + 1));
}

public boolean isOpen(int i, int j) {
if (isIndexOutOfBound(i, j)) {

