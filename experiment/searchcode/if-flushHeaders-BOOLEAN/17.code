public void setHeader(String name, String value, boolean append)
{
if (m_resp!=null)
{ // set the header directly
if (append)
public void setHeader(String name, Integer value, boolean append)
{
if (m_resp!=null)
this.setHeader(name,value.intValue(),append);

