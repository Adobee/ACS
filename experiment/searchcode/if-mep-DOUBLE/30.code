// specifique qui est parametre dans le fichier de config.
SiteMep site = sSiteMep;
if (ID_SITE_PAGE_AVEC_SECONDAIRE != -1)
// la zone.
if (zoneDM.getSiteMep() != -1)
{
site = FabriqueSiteMep.getSiteMep(zoneDM.getSiteMep());

