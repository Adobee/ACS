RatingBar jakeRate;

double inboxCount;
double outboxCount;
double meP = 0;
double themP = 0;
LayoutInflater inflater;
// works

// now for percents
double total = inboxA.get(g) + outBoxA.get(g);
meP = Math.ceil((inboxA.get(g) * 100) / total);

