Long days = duration.toDays();
Long hours = duration.toHours() - days*24;
Long mins = duration.toMinutes() - duration.toHours()*60;

if (0 != days){
durationString += days + &quot; dni, &quot;;
}

if (0 != hours){

