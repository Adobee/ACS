@RequestParam(value = &quot;encoded&quot;, defaultValue = &quot;true&quot;) Boolean encoded)
throws UserNotFoundException {

String decodedUsername = username;

if (encoded) {
String decodeUsername (String encodedUsername) {
String decodedUsername = encodedUsername;
if (Base64.isBase64(encodedUsername)) {

