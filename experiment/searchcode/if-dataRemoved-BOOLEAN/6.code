elementToDelete = findTop();
ArrayList<String> dataRemoved = new ArrayList<>();
for(Node data : elementToDelete) {
if(data.getName() != null) {
list.set(i, list.get(j));
list.set(j, tmp);

return list;
}
private boolean cmpWeight(Node i, Node j) {
if(i.getWeight() < j.getWeight()) {

