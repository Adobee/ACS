data[i] = b.data[i];
}
}

// Adds an element to the bag
public boolean add(E element) {
if (size == data.length){
data[index].equals(data[size-1]);
removeCounter+=2;
size--;
++removeCounter;
}
}

// Determines if the bag is empty
public boolean isEmpty() {

