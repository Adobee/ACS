} else if (lhs instanceof byte[]) {
append((byte[]) lhs, (byte[]) rhs);
} else if (lhs instanceof double[]) {
* @return Equals - used to chain calls.
*/
public EqualsResult append(final byte[] lhs, final byte[] rhs) {
if (!isEqual) {

