@Override
public int search( String txt ) {

int textIndex, textLength = txt.length();
int patIndex, patLength = pattern.length();
for ( textIndex = 0, patIndex = 0; textIndex < textLength &amp;&amp; patIndex < patLength; textIndex++ ) {
if ( txt.charAt( textIndex ) == pattern.charAt( patIndex ) )

