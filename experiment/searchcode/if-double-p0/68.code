Point p1 = line.p1;
Point p2 = line.p2;

double dx1 = p1.x - p0.x;
double dy1 = p1.y - p0.y;
double dx2 = p2.x - p0.x;
double dy2 = p2.y - p0.y;

if(dx1*dy2 > dy1*dx2 &amp;&amp; !(Math.abs(dx1*dy2 - dy1*dx2) < .005)){

