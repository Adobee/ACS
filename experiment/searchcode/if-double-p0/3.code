return p0.getDistance(p) > 0;
}

@Override
double perimeter() {

if (isCircle()) {
return 2 * Math.PI * p0.getDistance(p);
return 0;
}
}

@Override
double area() {

if (isCircle()) {
return Math.PI * Math.pow(p0.getDistance(p), 2);

