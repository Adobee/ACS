map.put(c.asKey(), playerName);
}

/**
* return true if and only if the player is the owner of the block
*
* @param event
* @return
*/
public boolean check(int dimension, int x, int z, String playerName) {

