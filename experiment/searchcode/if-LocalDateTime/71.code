package fede.util.time;

import java.time.LocalDateTime;
import java.time.temporal.ChronoUnit;
LocalDateTime from = intermediates.isEmpty() ? start : intermediates.get(intermediates.size()-1);
LocalDateTime to = LocalDateTime.now();
if(markIntermediate) {
registerIntermediate(to);

