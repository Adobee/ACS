return this.values[row][col];
}

public int pivotRow(int pivotCol) {
int pivotRow = 0;
double minValue = NON_ZERO_DIVISIBLE;
this.divide(pivot, pivotRow, i);
}

for (int row = 0; row < rows; row++) {
if (row != pivotRow) {
double coefPivotCol = this.values[row][pivotCol];

