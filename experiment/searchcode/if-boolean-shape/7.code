shapeChess[i][j][8] = 2000;
}
else if(this.isTo4L(i, j, shapeChess, shapeSpace))//是否实现活四2
private boolean isTo5L(int x, int y, int[][][] shapeChess, int[][][] shapeSpace)
{
boolean flag = false;

for(int d = 0; d < 2; d++)
{
if(shapeSpace[x][y][d]+shapeSpace[x][y][d+4]+1 >= 5)

