public class ShapeDrawer {

private static ShapeRenderer shapeRenderer;
private static boolean initialized;
private static ArrayList<BaseShape> shapesToRender;
ArrayList<BaseShape> toRemove = new ArrayList<BaseShape>();
for (BaseShape shape : shapesToRender) {
if(shape.shouldBeRemoved())

