public static PlayState create( State state ) {
if ( state == State.PlayingState) return Playing;
if ( state == State.PausedState) return Paused;
if ( state == State.BufferingState) return Buffering;
if ( state == State.StoppedState) return Stopped;

