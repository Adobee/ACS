package com.luohj.spring.designer.state;

/**
* 状态模式的切换类
*
* @author luohj
*
*/
public class Context {
this.state = state;
}

public void method() {
if (state.getValue().equals(&quot;state1&quot;)) {
state.method1();
} else if (state.getValue().equals(&quot;state2&quot;)) {

