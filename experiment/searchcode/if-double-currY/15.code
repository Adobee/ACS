System.out.println(&quot;Unknown kind: &quot; + i);
}
}

double currx = 0;
double curry = 0;
add(currx, curry);
currx += tx[i];
curry += ty[i];

double pdx = -dy[i];
double pdy = dx[i];
double kind = kinds[i];

