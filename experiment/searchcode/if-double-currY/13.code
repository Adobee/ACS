final int N = 5000;
int numInside = 0;
double currX, currY, radius, mypi;

for (int i=0; i<N; i++) {
currX = newRand(-1., 1.);
currY = newRand(-1., 1.);

/* check if throw inside circle */
radius = findRadius(currX, currY);

