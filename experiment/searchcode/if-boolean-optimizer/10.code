package volatility;

import optimizer.Optimizer;
import optimizer.Options;
import optimizer.Parameters;
super(data, params, options);
}

public boolean slide()
{
if (!((SlidingWindow) this.data).slide())

