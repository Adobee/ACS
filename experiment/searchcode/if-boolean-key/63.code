package core;

import org.lwjgl.input.Keyboard;

public class KeyboardManager
{
boolean[] keys, prevKeys;
prevKeys[key] = Keyboard.getEventKeyState();
}
}

public boolean checkKey(int key, boolean toggle)
{
/*
if( toggle &amp;&amp; prevKeys[key] )

