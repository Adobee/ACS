return false;
}
TreeSet<Integer> set = new TreeSet<>();
int size = k + 1;
if (size > nums.length) {
b = set.higher(set.first());
}
long diff = Math.abs((long) a - (long) b);
if (diff <= (long) t) {

