public class ProbeServiceImpl implements ProbeServiceI {

@Override
public void move(List<Probe> probes) {

for(Probe probe : probes){


if(!probe.isProcessed()){
String command  = Character.toString(motion.charAt(i));

if(command.equalsIgnoreCase(&quot;L&quot;)){
probe.turnLeft();

