for (int j = 0; j < numberStrs.length; j++) {
numbers[j] = Integer.parseInt(numberStrs[j]);
}
if (doesAliceToPlayWin(numbers, maxN)) {
private static boolean aliceWins(int[] numbers) {
if (numbers.length == _cacheLine) {
return isWinningPermutation(numbers);

