long reminder = million;
int quotient, index = 0, temp, tempIndex;

while (number > 1) {

factorial /= number;
number--;

quotient = (int) (reminder / factorial);
reminder = reminder % factorial;
if(reminder == 0){

