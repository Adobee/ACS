public class Solution {
public int largestRectangleArea(int[] height) {
if (height == null || height.length == 0)
stack.push(i++);

else{
int tempIndex = stack.pop();
int tempArea = stack.isEmpty()? height[tempIndex] * i : height[tempIndex] * (i-stack.peek()-1);

