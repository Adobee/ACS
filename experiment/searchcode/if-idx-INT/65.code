this.tree = new long[maxValue + 1];
}

long read(int idx){
if(idx == 0){
return 0;
return ret;
}
void add(int idx, long num){

while(idx < tree.length){
tree[idx] += num;
idx += -idx &amp; idx;
}
}
}

