* Checks that the necessary data is present in the project.
*
* @return boolean - true if all is OK to proceed, false if not.
// user confirmation
// -----------------------------------------------------
gui.busy (false, BUSY_OTHER);

boolean asNew = gui.isAsNew();

if (asNew) {

