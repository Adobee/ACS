public static final String CLIENT_OPTION_AS_NEW = &quot;asNew&quot;;
private static final ThreadLocal clientOptions = new ThreadLocal();
return paramReferenceResolver.getReference(this, paramResource, paramClass);
}

public boolean equals(Object paramObject)
{
if (!(paramObject instanceof RepoResource))

