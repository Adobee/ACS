for (int n = 0; n < N; n++) {
temp = sc.nextInt() % K;

if (!modCounts.containsKey(temp)) {
modCounts.replace(temp, modCounts.get(temp) + 1);
}
}

int numElements = 0;
for (int i = 1; i <= K >> 1; i++) {

