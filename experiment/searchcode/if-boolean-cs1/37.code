// testing to add null
assertFalse(cs1.insertPoint(0, (Point2d) null));
}

/*
* Class under test for boolean addPoint(Coord2D)
assertFalse(cs1.addPoint((Coord2D) null));
}

/*
* Class under test for boolean insertPoint(int, Coord2D)

