package wencakisa;
import java.time.DayOfWeek;
import java.time.LocalDate;
import java.util.Arrays;
// If the &quot;future&quot; date is before this moment, we swap them.
if(future.isBefore(now)) {
LocalDate temp = now;

