private int minutes;
private int seconds;

public Time(int hours, int minutes, int seconds) {
this.hours = hours;
minutes = (int) Math.floor(time);
time = time - minutes;
time *= 60;
seconds = (int) Math.ceil(time);

if (seconds != 0) {

