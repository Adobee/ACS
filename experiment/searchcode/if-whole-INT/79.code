return new WholeNumber(number.subtract(other.number));
}
@Override
public WholeNumber divide(WholeNumber other) {
if(compareTo(other)>0) {
throw new ArithmeticException(&quot;Other number too big!&quot;);
return number.doubleValue();
}
@Override
public int compareTo(WholeNumber other) {
return number.compareTo(other.number);

