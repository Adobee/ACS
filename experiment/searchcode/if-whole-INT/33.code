vo = (Menu) list.get(i);
subCode = Integer.parseInt(vo.getId().substring(2, 4));

if (subCode == 0)// 是一级菜单
wholeMenu.append(vo.getTitle());
wholeMenu.append(&quot;\&quot;,\&quot;&quot;);
wholeMenu.append(url);
if (url.indexOf(&quot;?&quot;) == -1) {

