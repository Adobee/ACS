public Fraction (int whole, int top, int bottom){
if (bottom == 0){
System.out.println(&quot;Invalid fraction because bottom is 0&quot;);
if (top > bottom) {
int m = top / bottom;
whole = m + whole;
top = top % bottom;
} else if (top==bottom){

