Scanner in = new Scanner(System.in);
long num = in.nextLong();
double tmp = Math.sqrt(num);
long gd = Math.round(tmp);
long tmp3 =0;
if(num-tmp2*gd>0) { tmp3 =12;}
for( long i = num - tmp2*gd; i > 0; i--)
{
tmp3+=8;

