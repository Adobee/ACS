private ArrayList<EctaDividendResult> exactaDividends = new ArrayList<EctaDividendResult>();

public EventDividend(long eventId) {
super();
}

/**
* Adds the dividend for a single outcome (ie runner) to the list of dividends for this event.
* @param dividend
*/
public void addDividend(DividendResult dividend) {
if (dividend.getDividendTypeId() == HorseRacingDividends.WIN) {

