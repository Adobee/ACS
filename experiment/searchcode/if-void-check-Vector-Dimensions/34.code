* Throws a {@link IncompatibleOperandException} with given message if the condition is not satisfied.
*
* @param condition
* @param message
*/
public static void check(boolean condition, String message) {
public static void checkDimensions(IVector first, IVector second) {
check(first.getDimension() == second.getDimension(), &quot;Vectors aren&#39;t of same dimensions.&quot;);

