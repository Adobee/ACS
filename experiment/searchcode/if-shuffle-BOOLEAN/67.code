Collections.shuffle(list);

if (QuickSelect.quickSelect(list, 0) != 1) {
success = false;
}

Collections.shuffle(list);

if (QuickSelect.quickSelect(list, 1) != 2) {
success = false;
}

Collections.shuffle(list);

