package org.kutsuki.service.type;

public enum EmployeeLevel {
ASSOCIATE(1),
OWNER(2);
String name = null;

for( EmployeeLevel employeeLevel : values() ) {
if( employeeLevel.getLevel() == level ) {

