return age;
}

private static Boolean isBirthMonth(Integer birthMonth) {
Calendar currentCalendar = Calendar.getInstance();
Integer currentDay = currentCalendar.get(Calendar.MONTH);

return (currentDay <= birthDay) ? Boolean.FALSE : Boolean.TRUE;

