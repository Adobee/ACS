import org.springframework.stereotype.Service;

import com.ocellus.lmep.dao.OrgEstimateCostMapper;
import com.ocellus.lmep.model.OrgEstimateCost;
List<OrgEstimateCost> list = orgEstimateCostMapper.search(params);
if(list.size() > 0)
orgEstimateCost = list.get(0);
return orgEstimateCost;
}
}

