package com.partner.zipdoc.estimate;

import android.content.Context;
import android.widget.FrameLayout;
estimate_type_text = (TextView) findViewById(R.id.estimate_type);
}

public void setEstimate(EstimateData e){
estimateData = e;
if(e.getIcon()!=null){

