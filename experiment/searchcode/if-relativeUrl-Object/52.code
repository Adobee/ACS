* a rootUrl, relativeUrl, and physicalFile which will be addressable via rootUrl+&#39;/&#39;+relativeUrl (called the Url).
* For example, if the rootUrl is &#39;/a&#39; and the relativeUrl is &#39;/file.gif&#39; then the physical file will be addressed
this.rootUrl = rootUrl;
this.relativeUrl = relativeUrl;
this.physicalFile = physicalFile;

if(!rootUrl.endsWith(&quot;/&quot;))

