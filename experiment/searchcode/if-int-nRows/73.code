return s;

if (nRows >= s.length())
return s;

int next = 2 * nRows - 2;

String result = &quot;&quot;;

for (int i = 0; i < nRows; i++) {
String result = &quot;&quot;;
if (nRows == 1)
return s;
for (int i = 0; i < nRows &amp;&amp; i < s.length(); i++) {
int inc = 2 * (nRows - i - 1);

