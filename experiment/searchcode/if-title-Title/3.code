String tmp = title;

if(title.isEmpty())
{
title = &quot;(No Title)&quot;;
}

if(title.contains(&quot;::&quot;))
{
title = title.substring(title.lastIndexOf(&quot;::&quot;)+2);

if(title.isEmpty())

