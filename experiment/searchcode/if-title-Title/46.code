String message;
int number;
int week;

String getTitle() {
if(number != -1) {
String titleString = String.format(Locale.TAIWAN, &quot;§@·~ %d&quot;, number);
if(!title.isEmpty()) {
titleString += &quot; : &quot; + title;

