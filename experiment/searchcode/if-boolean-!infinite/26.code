public static final boolean INFINITE = true;
// Constant for infinite valid interval.

public static final boolean FINITE = false;
return a0 * (1-t) + a1 * t;
}
}

public boolean isValidTime(float t)
{
if (isInfinite)
return true;
else
return (t >= t0) &amp;&amp; (t <= t1);
}

}

