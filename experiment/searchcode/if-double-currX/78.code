int length = (int) (Math.random() * 50 + 20);
if (currX + length > x) {
length = x - currX;
}
int mapType = (int) (Math.random() * CURR_MAPS);
//mapType = 0;
if (mapType == 0) {
hillMap(currX, currX + length);

