private void find(int[][] A, int row_s, int row_e, int col_s, int col_e, List<Integer> ans) {
int row_m = row_s + (row_e - row_s) / 2;
int col = findcol(A, row_m, col_s, col_e);
int col = start;
for (int i = start + 1; i <= end; i++) {
if (A[row][i] > A[row][col]) {

