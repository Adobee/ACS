public List<GHCommit> randomCommits(int numCommits) {
List<GHCommit> randomCommits = new LinkedList<GHCommit>();
int maxNum = (int) commits.count();
// If a number larger than the integers between min and max was required,
// return all numbers between min and max
if (numIntegers > maxNum - minNum) {
for (int i = minNum; i <= maxNum; i++)

