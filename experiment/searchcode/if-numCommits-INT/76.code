String diretorioAtual, caminhoVersaoAtual = &quot;&quot;;
String nomeSistema = &quot;elasticsearch&quot;;
int numArquivos, numCommits, numCommitsSistema;
arqSaida.write(&quot;,&quot; + numCommits);

if (numCommits == 1) { //caso o arquivo só tenha uma versão, não há como haverem breaking changes

