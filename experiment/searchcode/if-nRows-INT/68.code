public String convert(String s, int nRows) {
if (s == null || s.isEmpty() || nRows == 0) return &quot;&quot;;
else if (nRows == 1) return s;
StringBuffer sb = new StringBuffer();
int start = 0, j, max = (nRows - 1) * 2, step;

