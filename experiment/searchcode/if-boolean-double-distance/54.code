private double distance;

public Circle(Point center, double distance) {
this.center = center;
this.distance = distance;
}

public static boolean doTheyIntersect(Circle c1, Circle c2) {
boolean bool = true;
if ((c1.distance + c2.distance) < (Point.getDistance(c1.center,

