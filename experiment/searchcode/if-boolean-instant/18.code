* @param event
*/
public boolean addInstant(BObjectInstant event) {
// the prev to affect is the instant found before the add
// is there a next? to set the previous
if (getLast() != prev) {
boolean found = false;

