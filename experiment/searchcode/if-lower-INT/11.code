public int sqrt(int num) {
if (num <= 0)
return 0;
int lower = 0;
int upper = num;
while (lower < upper) {
int mid = (lower + upper) / 2;
if (mid == num / mid)
return mid;
else if (mid < (num / mid)) {

