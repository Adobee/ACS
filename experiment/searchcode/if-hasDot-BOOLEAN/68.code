/*
Validate if a given string is numeric.

Some examples:
&quot;0&quot; => true
&quot; 0.1 &quot; => true
&quot;abc&quot; => false
boolean dot = false, exp = false, digit = false, sign = false;
boolean hasdot = false, hasexp = false;

if (chs[pos] == &#39;+&#39; || chs[pos] == &#39;-&#39;)

