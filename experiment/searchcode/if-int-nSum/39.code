if (longStr.length() - shortStr.length() == 0) {

if (division != 0) {
nSum = division + nSum;
longStr = longStr.substring(0, longStr.length() - shortStr.length());

for (int i = 1; i <= longStr.length(); i++) {
nSum = (division + (longStr.charAt(longStr.length() - i) - &#39;0&#39;)) % 10 + nSum;

