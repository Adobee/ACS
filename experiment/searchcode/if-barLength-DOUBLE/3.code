double val = v.doubleValue();
if (val > max)
max = val;
dataModel.update(0, barLength);
}
else if ( height >= 80 &amp;&amp; height < 130 )
{
double x = arg0.getX();
double barLength =  ( x / ICON_WIDTH * max);

