int w = in.readInt();
if (!adj[v].contains(w)) {
adj[v].add(w);
throw new IllegalArgumentException(&quot;Vertex index &quot; + v + &quot; out of range&quot;);
} else if (w < 0 || w >= adj.length) {

