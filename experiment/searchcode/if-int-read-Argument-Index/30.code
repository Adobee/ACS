public ByteArrayInput(byte[] source) throws IllegalArgumentException {
this(source, 0, source.length);
}

public ByteArrayInput(byte[] source, int index, int length)
+ source.length + &quot;]&quot;);
}

public int read(byte[] buffer, int index) throws IllegalArgumentException,
IOException {
if (buffer == null)

