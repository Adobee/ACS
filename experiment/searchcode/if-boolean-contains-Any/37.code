* For a given array of integers, return true if any two numbers in the array can be added up
* to equal the largest number in the array.
*/

public static boolean containsTuple(int[] arr) {
Arrays.sort(arr);
int largest = arr[arr.length - 1];

