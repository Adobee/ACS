this.zone = zone;
}

public EZone getZone()
{
return zone;
}

@Override
public boolean equals(Object obj)
{
if(obj instanceof ZoneFilterData)
{
return getZone() == ((ZoneFilterData) obj).getZone();

