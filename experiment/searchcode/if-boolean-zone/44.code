public Zone(String[] zone, MapPos centre){
this.zone = zone;
}

public boolean isInZone(MapPos par1){
boolean isInZone = false;

int x = 0;
int y = 0;
boolean isFound = false;

for (y = 0; y < zone.length - 1 &amp;&amp; !isFound; y++)

