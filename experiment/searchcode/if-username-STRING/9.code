public static final String joinUsername(String username, String companyCode) {
if (StringUtils.isEmpty(companyCode)) {
return username + USERNAME_JOIN_CHAR + INFOSKY_COMPANY_CODE;
public static final String splitUsername(String username) {
if (!username.contains(USERNAME_JOIN_CHAR)) {

