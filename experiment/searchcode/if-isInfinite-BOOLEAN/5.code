// Alpha values at either end of time interval.

protected boolean isInfinite;
// Whether times outside t0 and t1 are valid.
return a0 * (1-t) + a1 * t;
}
}

public boolean isValidTime(float t)
{
if (isInfinite)
return true;
else
return (t >= t0) &amp;&amp; (t <= t1);
}

}

