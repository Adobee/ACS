public static double abs(double x) {
if (x > 0)
return x;
return -1.0 * x;
}

public static double logAdd(double logX, double logY) {
// make a the max
if (logY > logX) {
double temp = logX;
logX = logY;

