* Safely multiplies and adds number under a modulus to prevent overflow Operations may still
* overflow if modulus is greater than Long.MAX_VALUE / 2
*
* @author Lewin
*
*/
public class SafeArithmetic {
public static long mod_mult(long x, long y, long p) {
if (x == 0 || y == 0)

