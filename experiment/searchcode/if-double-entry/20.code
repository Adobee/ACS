for (Entry entry : entryList) {
if (entry.IsIncome())
total += Double.parseDouble(entry.GetValue());
for (Entry entry : entryList) {
if (!entry.IsIncome())
total += Double.parseDouble(entry.GetValue());

