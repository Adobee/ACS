public ComplexFraction add(ComplexFraction complexFraction) {
return new ComplexFraction(numerator.multiply(complexFraction.getDenominator()).add(complexFraction.getNumerator().multiply(denominator)),

