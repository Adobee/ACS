package expression;

public class Fraction
{
private int m, n;

// a = m/n

public Fraction(int a)
Fraction bb = new Fraction(1);
bb.m = -b.m;
bb.n = b.n;
return add(a, bb);
}

public static Fraction multiply(Fraction a, Fraction b)

