public static void main(String[] args) {

// test divide by zero - should print an error and exit
// new Fraction(1, 0);
// test multiply
Fraction n = new Fraction(2, 2);
// test multiply
if (!f.equals(g.multiply(h)))
System.out.println(&quot;Multiply failed&quot;);

