package task101;

/**
* @author Igor
*/
class Fraction {
long num, denom;

public Fraction(long num, long denom) {
if (b == 0) return a;
return gcd(b, a % b);
}

public Fraction multiply(Fraction fraction) {

