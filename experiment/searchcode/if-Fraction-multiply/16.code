divisor /= gcd;
}

private void multiply(Fraction fraction) {
checkDivisorSign();
numerator *= fraction.getNumerator();
Fraction result = new Fraction(numerator, divisor);
result.multiply(fraction);
return result;
}

public Fraction multiplication(int multiplier) {

