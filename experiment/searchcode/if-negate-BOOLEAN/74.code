* The Class Variable which defines a simple variable
* object with string value, boolean value that has to be set
* and boolean variable to define if this variable if negated.
* @return true, if is negate
*/
public boolean isNegate() {
return negate;
}

/**
* Sets the negate.

