point.x=intWidth;
label.setSize(point);
int intLabelHeight=point.y;

// Create text
if (type==TYPE.TEXTMULTI)
text = new Text(composite, TEXT_STYLE);

if (fontText!=null)
text.setFont(fontText);
text.setText(stringText);

int intTextHeight;

