package Code;

public class MatchRegExpn {
int match(char[] regexp, char[] text)
{
if (regexp[0] == &#39;^&#39;)
/* matchhere: search for regexp at beginning of text */
int matchhere(char *regexp, char *text)
{
if (regexp[0] == &#39;\0&#39;)

