int numThreads = 5;
iLinkedList list;
ListThread[] Threads;
int numAdds = 5000;
int numOps = 25000;

if(args.length != 2){
Threads[i].iterations = numAdds / numThreads;
Threads[i].list = list;
if(i == numThreads - 1){
Threads[i].iterations += numAdds % numThreads;

