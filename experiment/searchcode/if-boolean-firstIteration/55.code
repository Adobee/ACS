targetMap.put(temp, 0);
}

int start = 0;
int end = 0;
char next;
boolean firstIteration = true;
for (int j = 0; j < s.length(); j++) {
char c = s.charAt(j);
if (firstIteration) {
if (set.contains(c)) {

