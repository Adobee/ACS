StringBuilder strBld = new StringBuilder() ;

boolean firstIteration = true;
for (Object item: list) {

if (mapper != null) {
item = (item == null)? nullText: item;

if (firstIteration) {
strBld.append(item);

