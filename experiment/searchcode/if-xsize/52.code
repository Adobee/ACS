public boolean addRectangle(int x, int y, int xsize, int ysize) {
int xFar = x + xsize - 1;
int yFar = y + ysize - 1;

if (xSize <= 0 || ySize <= 0) {
return false;
} else if (x < 0 || x >= xSize || y < 0 || y >= ySize) {

