public class Camera
{
int x = 0,y = 0,xSize,ySize;

BufferedImage image;
Graphics g;
public Camera(int xsize,int ysize)
gfx.drawImage(image, -x, -y,xSize, ySize, null);
}

public void cameraUp()
{
if (y >= 10)
y -= 10;

