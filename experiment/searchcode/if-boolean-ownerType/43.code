StringBuilder buf = new StringBuilder();
if (ownerType != null) {
buf.append(typeName(ownerType));
buf.append(&quot;.&quot;);
return t.toString();
}
}
@Override
public boolean equals(Object obj) {
if (!(obj instanceof ParameterizedType)) {

