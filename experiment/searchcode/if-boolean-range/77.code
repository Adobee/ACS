private _UURange currentRange;
private boolean circular;

public UURangeList(boolean circular) {
this.circular = circular;
}

public UUList<_UURange> getRanges() {
if (circular &amp;&amp; currentRange != null &amp;&amp; ranges.size() > 1

