this.size = hi - lo;
}

public boolean liesWithin(Range range) {
return (this.lo < range.lo &amp;&amp; range.hi < this.hi);
}

public boolean isSuperSet(Range range) {
return (this.lo > range.lo &amp;&amp; this.hi < range.hi);

