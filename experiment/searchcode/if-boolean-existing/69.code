public void addDay(String weekNumber, String day, Boolean value) {
Map<String, Boolean> existingInner = data.get(weekNumber);
if (existingInner == null) {
existingInner = new HashMap<String, Boolean>();

