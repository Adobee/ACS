import org.hl7.fhir.instance.model.Resource;

import com.google.gwt.http.client.RequestBuilder;
import com.google.gwt.http.client.URL;
public class CustomFhirRequestBuilder extends RequestBuilder {

/**
* We need to config the base URL in some place common, like global variable

