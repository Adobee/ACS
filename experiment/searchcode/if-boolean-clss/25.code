dataType = dtype;
classLoader = cloader;
if (clssName != null)
loadClass(clssName);
}

// load class
public void loadClass(String clssName) throws Xcept {
if (m_getInfo != null) throw new Xcept(this,

