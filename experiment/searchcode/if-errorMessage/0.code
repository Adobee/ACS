List<ErrorMessage> error = new ArrayList<ErrorMessage>();
if (thread.getTitle().equals(&quot;&quot;)) {
ErrorMessage errorMessage = new ErrorMessage();
error.add(errorMessage);
}
if (thread.getText().equals(&quot;&quot;)) {
ErrorMessage errorMessage = new ErrorMessage();

