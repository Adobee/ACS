protected boolean esFilaModificableConModificarCategoriaCualificacion(RowKey rowKey) {
boolean es = rowKey != null;
if (es) {
es = this.esFilaRecursoActivo(rowKey, FUNCION_MODIFICAR_CATEGORIA_CUALIFICACION);
protected boolean esFilaValida(RowKey rowKey, long funcion) {
boolean es = false;
if (funcion == FUNCION_CREAR_CATEGORIA_CUALIFICACION) {

