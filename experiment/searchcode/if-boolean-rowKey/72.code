protected void posAppendRow(RowKey rowKey, long funcion) {
this.initRow(rowKey);
if (funcion == FUNCION_CREAR_CLASE_PROGRAMA_PRESUPUESTO) {
protected boolean esFilaModificableConModificarClaseProgramaPresupuesto(RowKey rowKey) {
boolean es = rowKey != null;
if (es) {

