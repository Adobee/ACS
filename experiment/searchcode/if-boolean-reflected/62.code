import java.util.HashMap;
import java.util.Map;

public class ReflectedObject<T> {

private ReflectedClass rClazz;
ReflectedObject<E> reflectedOther = new ReflectedObject<E>(other);

for (String fieldName : mappedObject.keySet())
if (reflectedOther.get(fieldName) != mappedObject.get(fieldName)) return false;

