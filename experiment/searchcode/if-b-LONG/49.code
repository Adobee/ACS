int length = array.length;

long greatestProduct = 0;
tempProduct = Long.parseLong(array[i + 0]) * Long.parseLong(array[i+ 1]) * Long.parseLong(array[i + 2]) * Long.parseLong(array[i + 3]) * Long.parseLong(array[i + 4]) * Long.parseLong(array[i + 5]) * Long.parseLong(array[i + 6]) * Long.parseLong(array[i + 7]) * Long.parseLong(array[i + 8]) * Long.parseLong(array[i + 9]) * Long.parseLong(array[i + 10]) * Long.parseLong(array[i + 11]) * Long.parseLong(array[i + 12]);
if( tempProduct > greatestProduct){

System.out.println(tempProduct);

