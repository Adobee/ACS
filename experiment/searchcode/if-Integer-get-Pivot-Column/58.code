private SimplexTableElement pivotElement = null;

private ArrayList<Integer> basisVariables = new ArrayList<Integer>(); //имена строк, по сути
private void exchangeVariablesInTable() {
Integer tmp;
tmp = basisVariables.get(pivotElement.row);

