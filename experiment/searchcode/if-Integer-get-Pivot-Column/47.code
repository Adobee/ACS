private void resetPivotRowCols() {
if(selectedPivotSource==null) return;

pivot.getRows().clear();
pivot.getCols().clear();
// system.out.println(&quot;cbPivotSources is null: &quot;+(cbPivotSources==null));

Integer selIndex=cbPivotSources.getSelectedIndex();
// system.out.println(&quot;selIndex: &quot;+selIndex);

