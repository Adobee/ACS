* Check for an ip range that contain an other or not.
* @param destRange
* @return true if it contains and false in the vice versa.
*/
public boolean contains(IpRange destRange) {
return this.hasIp(destRange.startRange) &amp;&amp; this.hasIp(destRange.endRange);
}

/**
* Check for an ip is in a range or not.
* @param ip
* @return true if in range and false in the vice versa.

