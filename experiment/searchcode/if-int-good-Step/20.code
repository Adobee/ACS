private static int go(int[] song, int step, int count, int delta) {
if (step == song.length) {
int used = 0;
for (int j = Math.max(step - delta, 0); j < step; j++) {
if (song[j] == i) {
good = false;

