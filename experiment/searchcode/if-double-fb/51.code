int n, side = 0;

/** starting values at endpoints of interval **/
double fb = f(b);
double fa = f(a);
c = (fb * a - fa * b) / (fb - fa);
System.out.println(&quot;c = &quot; + c);
if (Math.abs(a - b) < e * Math.abs(a + b))

