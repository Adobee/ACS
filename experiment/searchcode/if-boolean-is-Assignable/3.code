return instance;
}

public boolean isAssignable(Type from, Type to) {
if (to instanceof TypeVariable<?>) {
public boolean isAssignableWildcardType(WildcardType from, Type to) {
if (to instanceof Class<?> ||

