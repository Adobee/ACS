private SortedSet<QueuePosition> positions = new TreeSet<QueuePosition>(new QueuePositionComparator());
private Account account;
previousPosition.setNextPosition(nextPosition);
}

if (nextPosition != null) {

