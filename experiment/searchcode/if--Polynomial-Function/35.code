package com.spooky.crypt.checksum;

public class SpCRC32 implements Checksumer {
//	function crc(bit array bitString[1..len], int len) {
//	     remainderPolynomial  := polynomialForm(bitString[1..n])   // First n bits of the message

