Node newList, temp;
int carry, k;

if(num1 == null) return num2;
else if (num2 == null) return num1;
carry = (num1.data + num2.data + carry) / 10;

if(newList == null) {
temp = new Node(k);
newList = temp;

