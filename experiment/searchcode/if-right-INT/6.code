int pivotIndex = partition(A);

int left = 0;
int right = A.length - 1;

if (A.length < 2 * (pivotIndex + 1)) {
public int partition(int[] A) {
if (A == null || A.length == 0) {
return 0;
}

int left = 0;
int right = A.length - 1;

