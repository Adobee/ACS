public LinkedList<interval> FindDuring (Node node, interval interval, long minStart, long maxStart, long minEnd, long maxEnd, LinkedList<interval> list){
public boolean During (interval index, long minStart, long maxStart, long minEnd, long maxEnd)
{
if ((index.Start.before(CurrentInterval.Start) || index.Start.equals(CurrentInterval.Start))

