List<Integer> res = new ArrayList<>();
if (A == null || A.length == 0) {
return res;
minsta = sta;
minend = i;
gmin = lmin;
}
}
if (total - gmin > gmax &amp;&amp; (minsta != 0 || minend != A.length-1)) {

