public final long getSeed(final LocalDateTime seedContext) {

LocalDateTime initialFrom = this.getInitialFromDateTime(seedContext.getYear());
* on the given {@code year}.
*/
private LocalDateTime getInitialFromDateTime(final int year) {
if (year < 2000) {

