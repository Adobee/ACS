public interface ClosedList {

/**
* Returns if a stop is already contained in the closed list.
*
* @param stop
* @return true if stop is contained in closed list
*/
boolean contains(Stop stop);

/**
* Add a stop to the closed list.

