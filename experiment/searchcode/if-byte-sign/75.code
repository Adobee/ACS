* in the script&#39;s service structure if necessary
*
* @return
*/
public List<Byte> getPackedScript() {
List<Byte> packedScript = new ArrayList<Byte>();

if (script.getInfo() == Script.InfoTypes.MESSAGE.getValue()) {

