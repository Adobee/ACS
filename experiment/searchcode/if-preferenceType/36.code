for(PreferenceType preferenceType: PreferenceType.values()) {
if(!Preferences.isEnabled(player, preferenceType)) {
int index = preferenceType.getIndex();
String displayName = preferenceType.getName();

if(Preferences.isEnabled(player, preferenceType)) {

