import net.kbndz.jnarou.conf.NarouConfiguration;
import net.kbndz.jnarou.conf.RankingConfiguration;
import okhttp3.HttpUrl;
import org.junit.After;
private boolean equalsUrl(HttpUrl expected, HttpUrl actual) {
if (expected == null || actual == null) {
return false;

