* return the smallest character that is strictly larger than
* the search character.

If no such character exists, return the smallest character in the array.
int r = str.length()-1;
char ret = str.charAt(0);
while(l<r) {
int m = (l+r)/2;
if (str.charAt(m) > c) {

