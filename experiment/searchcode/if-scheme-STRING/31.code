public class Scheme implements SchemeI {

private Long id;
private String name;
private String city;
private SchemeType schemeType;
if (this == o) return true;
if (o == null || getClass() != o.getClass()) return false;

Scheme scheme = (Scheme) o;

