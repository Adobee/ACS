public static final SchemeDuplicator DUPLICATOR = new SchemeDuplicator<NormalizedScheme>(Builder::new);

private Map<String, RefScheme> links = new LinkedHashMap<>();

private NormalizedScheme(String name) {
super(name);

