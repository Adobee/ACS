import lti.alpha.lang.schemeobject.SchemeString;

public abstract class SchemeObjectWrapper extends SYNTAX {
protected SchemeObjectWrapper() {
return new Integer(schemeNumber.getIntValue());
}
else if (schemeObject instanceof SchemeString) {

