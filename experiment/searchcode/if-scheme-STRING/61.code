public Scheme save(Scheme scheme) {
return primary.save(scheme);
}

@Override
public Scheme remove(String schemeName) {
return primary.remove(schemeName);
}

@Override
public Scheme getOne(String schemeName) {

