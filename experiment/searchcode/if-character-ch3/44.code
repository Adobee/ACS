char ch2 = strPar.charAt(1); //Stores the second character in strPar
char ch3 = strIn.charAt(0); //Stores the first character in strIn
while ( ch3 != ch1 &amp;&amp; ch3 != ch2 || strIn.length() > 1) {
//Checks if ch3 is equal to both ch1 and ch2

