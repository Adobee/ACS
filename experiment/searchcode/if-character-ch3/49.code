for (int i = 0; i < s.length(); i++) {
Character ch = s.charAt(i);
if (hashTable.containsKey(ch)) {
for (int i = 0; i < s.length(); i++) {
Character ch = s.charAt(i);
if (charMap.containsKey(ch)) {
charMap.put(ch, plural);

