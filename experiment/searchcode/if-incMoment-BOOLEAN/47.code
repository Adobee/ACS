* be incremented or cleared.
*/
protected boolean incMoment;

/** Constructs a Mean. */
public Mean() {
incMoment = true;
public void increment(final double d) {
if (incMoment) {
moment.increment(d);

