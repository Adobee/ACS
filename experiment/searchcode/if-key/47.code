//text(mouseX + &quot;,&quot; + mouseY, mouseX, mouseY + 5);
if(keyPressed)
highlight(key);
}

int enters = 0;

public void keyPressed() {
if(key != CODED) {
if(key == ESC)
System.exit(0);
else if(key == RETURN || key == ENTER)

