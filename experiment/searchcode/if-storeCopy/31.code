tree subroot = new tree(data[root]);
if (subroot.data > parent.data) {
parent.setRight(subroot);
allPossibleRec(llcopy, storecopy, size);
}
if (storecopy.size() == size) {
while (!storecopy.isEmpty()) {

