* A state of the syllable parser. Can parse a string or a character and return the next state.
*
* @author jonathan
* @see Syllable
private ParserState parse(char character) throws ParseException {
ParserState nextState = this.process(character);

