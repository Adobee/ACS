public static void parseNumber() {
if (Character.isDigit(nextChar)) {
getNextChar();
//The following four lines were added to recursively parse multiple digit numbers
if (Character.isDigit(nextChar)) {

