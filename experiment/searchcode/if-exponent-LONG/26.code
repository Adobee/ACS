public static long power(int base, int exponent) {
if (exponent == 1) {        // Base case when exponent is 1
return base;
} else if (exponent == 0) {
return 1;
}

if (exponent % 2 != 0) {    // Raising to 0 power is 1

