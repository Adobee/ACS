public CnString(String s){
cn = s ;
}

/**
* 字符串的中文长度
* @return
*/
public double length(){
if(cnLen>-1){
return cn.substring(b, e);
}

//	输入实际字符的位置，返回对应的中文位置
private double cnPoint(int point){
if(point>=cn.length()){

