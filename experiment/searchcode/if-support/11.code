public Support next() {
Support support = null;
if (this.hasNext()){
support = this.supportStack.getStackList().get(index);
return support;
}

public void remove() {
Support support = null;
if (this.hasNext()){
support = this.supportStack.getStackList().get(index);

