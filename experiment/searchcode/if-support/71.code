else if (this.support < that.support)
return -1;
else if (this.support > that.support)
/*public int compareTo(Result that) {
if (this.support < that.support)
return -1;
else if (this.support > that.support)

