UI.invertRect(Math.min(xOld, lastX), Math.min(yOld, lastY), widtho, heighto);
if (State == 1)
UI.invertOval(Math.min(xOld, lastX), Math.min(yOld, lastY), widtho, heighto);
if (State == 2)
UI.invertLine(xOld,yOld,lastX,lastY);

