System.out.println(&quot;integrating from &quot; + from + &quot; to &quot; + to);

if (to < from) {
return 0;
}

double delta_x = (to - from)
/ (double) steps;

if (!withX) {
f1 = getMaxValue(from);
for (int i = 0; i < steps; i++) {

