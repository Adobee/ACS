public void removeWindow(AbstractWindow window) throws Exception {
int indexRemoveWindow = listWindow.indexOf(window);
if (indexRemoveWindow < 0) {
throw (new Exception(&quot;list of windows haven&#39;t this window &quot;));
}
if (indexRemoveWindow > 0) {

