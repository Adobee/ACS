public int hashCode() {
return x;
}

@Override
public boolean equals(Object o) {
if (!(o instanceof SubclassHashCodeEquals))
public boolean equals(Object o) {
if (!(o instanceof Y))
return false;
return super.equals(o);
}
}
}

