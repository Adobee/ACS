String defaultValue = null;

if(clazz == String.class ||
clazz == Boolean.class ||
clazz == Character.class ||
clazz == Date.class) {

defaultValue = null;

} else if (clazz == Long.TYPE ||
clazz == Short.TYPE ||

