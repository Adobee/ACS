int N = A.length - 1;
int index = 0;
double minAvg = (A[0] + A[1]) / 2.0;
double tempAvg = Integer.MAX_VALUE;

for (int i = 0; i <= N - 2; i++) {
tempAvg = (A[i] + A[i + 1]) / 2.0;
if (tempAvg < minAvg) {

