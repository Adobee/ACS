values = new CircularBuffer();
}

public RangedAVG(int size, double avgVal) {
rangeSize = size;
/// avg = new StdAVG(size, avgVal);
return calculate();

//		if ( avg.value > 10E6 ) {
//			avg.value = calculate();
//		}
//		return avg.value;

