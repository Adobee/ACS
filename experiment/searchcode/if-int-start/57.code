package search;

public class QuickSelect {
public int quickSelect(int[] A, int k) {
int start = 0;
int pivot = A[end];
int i = start - 1;
for (int j = start; j < end; ++j) {
if (A[j] < pivot)
swap(A, ++i, j);

