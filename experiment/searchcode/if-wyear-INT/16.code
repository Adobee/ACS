* typedef struct _SYSTEMTIME {
*     WORD wYear;
*     WORD wMonth;
*     WORD wDayOfWeek;
*     WORD wDay;
n.invoke(); // 调用方法
Long load_res = Long.parseLong(n.getRetVal());
if (1 == load_res.intValue()) {// 加载成功

