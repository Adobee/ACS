BigDecimal y = new BigDecimal(input.nextLine());
int quadrant = checkGivenPointsAndReturnQuadrant(x, y);
private static int checkGivenPointsAndReturnQuadrant(BigDecimal x, BigDecimal y) {
int quadrant = 0;

if (x.signum() == 0 &amp;&amp; y.signum() == 0) {

