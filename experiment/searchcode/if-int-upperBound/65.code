public boolean Find(int [][] array,int target) {

if (array.length == 0)
return false;
int groupLength = array.length;
lowerBound = curIn + 1;
} else if (array[curIn][0] > target) {
upperBound = curIn - 1;
}
}
for (int i = 0; i <= groupIndex;i++)

