// List<ProviderOf<DnsServerProvider>> dnsProviders = providers.listItemsProviding(DnsServerProvider.class);
// if (dnsProviders.size() == 0) {
// throw new OpsException(&quot;No DNS service providers registered&quot;);
// }
//
// if (dnsProviders.size() == 1) {
// return dnsProviders.get(0).get().createDnsRecord();

