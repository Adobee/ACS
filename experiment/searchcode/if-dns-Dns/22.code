return false;
if (getClass() != obj.getClass())
return false;
DNS_Server other = (DNS_Server) obj;
if (DNS_Server == null) {
if (other.DNS_Server != null)
return false;
} else if (!DNS_Server.equals(other.DNS_Server))

