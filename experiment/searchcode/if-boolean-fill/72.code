boolean[] used = new boolean[9];

// by row
for (int i = 0; i < 9; i ++) {
fill(used, false);
return false;
}
}
}
}
}
return true;
}

private boolean check(char ch, boolean[] used) {
if (ch == &#39;.&#39;) return true;

