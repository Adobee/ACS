private static String uniqCodeForRequest = &quot;default&quot;;
private static int number;

public static boolean shouldRetry(String encodedRequest) {
boolean statusRetry = false;

if (uniqCodeForRequest.equals(encodedRequest)) {

