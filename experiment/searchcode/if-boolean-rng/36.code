sb.append(vowels.charAt(rng.nextInt(vowels.length())));
sb.append(parts[rng.nextInt(parts.length)]);
if (rng.nextBoolean())
sb.append(parts[rng.nextInt(parts.length)]);
if (rng.nextBoolean() &amp;&amp; rng.nextBoolean())

