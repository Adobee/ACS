if (starts[i].y < miny) miny = starts[i].y;
if (starts[i].y > maxy) maxy = starts[i].y;
}

int I = 0;

for (int x=minx-1; x<maxx+1; x++) {
int x2 = starts[m+1].x;
int y2 = starts[m+1].y;

if (x1 > x2) {
int t = x2;
x2 = x1;

