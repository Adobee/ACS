package SZXY.model;

public class Complex {
private double x, y;
public Complex(double real, double imag){
public static Complex intersection(Complex a1,Complex a2,Complex b1,Complex b2){
Complex b = sub(b2, b1);
double d1 = cross(b,sub(a1, b1));
if(d1 < 0){
d1*=-1;

