final int length = this.partialDecoders.length;
if ( name == null ) {
throw new IllegalArgumentException( );
this.partialDecoders = partialDecoders;
this.byteArrayArray = new byte[ this.partialDecoders.length ][ ];
}

public Object get( final String name ) {

