for (int i = 0; i < WIDTH; i++) {
for (int j = 0; j < HEIGHT; j++) {

if (objectArray[i][j] == BLOCK) {
g.drawImage(imageBlock, i * TILE_SIZE, j * TILE_SIZE, TILE_SIZE, TILE_SIZE, null);
} else if (objectArray[i][j] == CLEAR) {
g.drawImage(imageBackground, i * TILE_SIZE, j * TILE_SIZE, TILE_SIZE, TILE_SIZE, null);

