private final List<Map<DataPatternElement, String>> parsedRules;

private final List<List<RuleOption>> ruleOptions;
pRules.add(rule.getDataPattern().parse(rule.getDefinedValue()));
rOpts.add(rule.getRuleOptions() != null ? rule.getRuleOptions() : null);

