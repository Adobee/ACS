int dim = dimension(parameters);
double[][] means = null;
if(computeMeans)
means = new double[observations.length][dim];
logLikelihood += logSum;

//resampling step
if(effectiveNumberParticles(w) < resamplingThreshold * (double) numParticles)

