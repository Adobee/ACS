setType = setType.substring(1,setType.length()-1);
}
int affectedRows = 0;

if (setType != null &amp;&amp; !setType.isEmpty()) {
UMonitor.getInstance().startMonitor(true);
affectedRows = 1;
} else if (&quot;OFF&quot;.equalsIgnoreCase(setType)) {

