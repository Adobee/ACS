private double getCenterOfRotationX()
{
double plusOrMinus = 1;
if(fieldDirection * sPar.chargeSlider.getSliderValue() > 0)
frequencyValue.setText(dFormat.format((double)1/actor.period)+ &quot; Hz&quot;);
if(actor.fieldDirection * actor.sPar.chargeSlider.getSliderValue() > 0)

