private static void printer1(long dividend, String leading, long preDivisor){
for(long divisor = dividend / 2; divisor > 1; divisor /= 2 ){
if(dividend % divisor == 0 &amp;&amp; preDivisor >= divisor){
long quotient = dividend / divisor;
if(divisor >= quotient){

