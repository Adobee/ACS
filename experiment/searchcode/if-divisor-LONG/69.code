return divideWithBitManipulation((long)dividend, (long)divisor);
}
private int divideStackOverFlow(int dividend, int divisor){
private int divideWithBitManipulation(long dividend, long divisor){
if(divisor == 0 || (dividend == Integer.MIN_VALUE &amp;&amp; divisor == -1)) return Integer.MAX_VALUE;

