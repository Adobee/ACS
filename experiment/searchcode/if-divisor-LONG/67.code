long divisorLong = (long) divisor;

boolean neg = false;

if(dividentLong < 0 &amp;&amp; divisorLong > 0 || dividentLong > 0 &amp;&amp; divisorLong < 0){
public long utilityDivide(long dividentLong, long divisorLong){
if(dividentLong == 0 || dividentLong < divisorLong){
return 0;

