public void Push(String item){
if(strings.length == n){
Resize(strings.length*2);
}

strings[n++] = item;
strings[n] = null;

if(strings.length > 0 &amp;&amp; n == strings.length / 4){
Resize(strings.length / 2);

