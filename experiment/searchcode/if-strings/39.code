package com.neaterbits.data.transform.runtime;

import com.neaterbits.data.reader.api.PackedStrings;
private PackedStrings strings;

@Override
public void init(PackedStrings strings) {
if (strings == null) {

