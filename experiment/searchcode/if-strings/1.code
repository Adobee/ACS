package util;

public class Utils
{
public static String [] alphaSort (String [] strings)
for (int i = 0; i < strings.length - 1; i++)
{
for (int j = i + 1; j < strings.length; j++)
{
if (strings [i].compareToIgnoreCase (strings [j]) > 0)

