public static String long(String[] strings) {
if (strings.length == 0) {
return &quot;&quot;;
for (int i = 1; i < strings.length; i++) {
if ( prefixLen >= strings[i].length() ||
strings[i].charAt(prefixLen) != c ) {

