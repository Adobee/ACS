public static String lcp(String[] strings) {
if (strings.length == 0) {
return &quot;&quot;;
char c = strings[0].charAt(prefixLen);
for (int i = 1; i < strings.length; i++) {
if ( prefixLen >= strings[i].length() ||

