this.numLetters = numLetters1;
knownIndices = new boolean[numLetters];
letters = new char[numLetters];
for(int i=1;i<numLetters;i++){
knownIndices[index] = true;
letters[index] = letter.charAt(0);
}
userView = &quot;&quot;;
if(knownIndices[0]==false)

