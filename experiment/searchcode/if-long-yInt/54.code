private String name;
private long id;
private Point coord;
private double scale;
private int[][] grid;
double slope = (double) (endY - startY) / (endX - startX);
double yInt = startY - slope * startX;
if (slope < 1 &amp;&amp; slope > -1) {

