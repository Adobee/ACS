public boolean checkItem(MoneyStockFlowDO item) {
float percent = item.getPercent();
if (increasePercent > 0 &amp;&amp; percent < increasePercent) {
return breakForeash();
}

if (increasePercent < 0 &amp;&amp; percent > increasePercent) {

