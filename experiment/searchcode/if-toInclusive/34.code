public static double diffSum(double[] array, int from, int toInclusive, Linear line) {
double diff = 0;
double increment = line.slope;
for (int i = from; i <= toInclusive; i++, currentExpectedValue += increment) {
double ydiff = Math.abs(currentExpectedValue - array[i]);

