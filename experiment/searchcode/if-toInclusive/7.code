to = new Date(value.getTime());
toInclusive = true;
} else if ( operator.equals( &quot;!=&quot; ) ) {
to = new Date(value.getTime());
toInclusive = false;
} else if ( operator.equals( &quot;<=&quot; ) ) {
to = new Date(value.getTime());

