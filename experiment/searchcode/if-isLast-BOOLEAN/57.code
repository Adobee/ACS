private final Long total;

private final boolean isLast;

private final Collection<User> users;

public GroupMembers(URI self, URI nextPage, Long maxResults, Long startAt, Long total, boolean isLast,

