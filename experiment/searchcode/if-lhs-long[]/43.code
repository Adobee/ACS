int result = Long.compare(lhs.lastModified(),rhs.lastModified());
if(result == 0){
try {
Log.d(&quot;&quot;, lhs.getName());
Log.d(&quot;&quot;, rhs.getName());
long lhsLong = Long.parseLong(lhs.getName().split(&quot;.jpeg&quot;)[0]);

