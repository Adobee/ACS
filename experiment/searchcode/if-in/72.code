package careercup;

public class SumToKInSortedIntArray {
/**
* find two elements sum to k in sorted array
while (s <= e) {
if (in[s] + in[e] > k) e--;
//			else (in[s] + in[e] < k) {
else {
if (in[s] + in[e] == k)

