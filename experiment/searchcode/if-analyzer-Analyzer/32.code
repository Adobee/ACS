Analyzer analyzer = null;
if (isLearning) {
if (SPANISH.equals(language)) {
analyzer = new AnalyzerLearningSpanish(luceneVersion);
} else if (ENGLISH.equals(language)) {
analyzer = new AnalyzerLearningSpanish(luceneVersion);

