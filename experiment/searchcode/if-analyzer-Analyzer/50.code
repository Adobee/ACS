switch(this) {
case MMSegComplex:
if (analyzer == null ||
!(analyzer instanceof MMSegAnalyzer) ||
analyzer = new MMSegAnalyzer(MMSeg.Complex, CCPrefs.userDictionaries);
}
return analyzer;

case MMSegSimple:
if (analyzer == null ||

