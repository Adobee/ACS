private String scheduleName;
private ArrayList<Week> weeks;
private int activeWeek;

public Schedule(String filePathInput, ArrayList<String> inputStrings) {
weekInputs.add(inputStrings.get(1));
weeks = new ArrayList<Week>();
for (int inx = 2; inx < inputStrings.size(); inx++) {

