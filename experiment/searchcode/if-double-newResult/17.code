this.labelList = labelList;
W = new double[featureNum];
if (C > 10) {
alpha = 0.0001;
} else if (C >= 1) {
double oldW = W[j];
Double attribute = vector.get(j + 1);
if (attribute == null) attribute = 0.0;

