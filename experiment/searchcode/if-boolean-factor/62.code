for (int factor = 2; factor < Math.sqrt(pnumber); factor++) {
if (pnumber%factor == 0) { //factor is factor of pnumber
ofactor = pnumber/factor;
if (isPrime(ofactor)) {
System.out.println(ofactor);

