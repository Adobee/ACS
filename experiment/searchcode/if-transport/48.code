public static int countOfCargoTransport(List<Transport> transport) {
int count = 0;
for (Transport t : transport) {
if (t instanceof CargoTransport) count++;
* @param transport, list of transport
* @param required,  transport, which we want to check
* @return true, if we found transport

