private MusicApp() {}

public static MusicApp getInstance() {
if (musicApp == null) musicApp = new MusicApp();
public void execute() {
AbstractAction nextAction = new StartAction();
while (nextAction != null) {
nextAction = nextAction.execute();
}
}
}

