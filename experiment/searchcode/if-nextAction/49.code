public enum NextAction {
BackToQueueAnyway(InnerIncreaser.getNextInt()),
BackToQueueIfNotExists(InnerIncreaser.getNextInt()),
for (NextAction req : NextAction.values()) {
if (req.getBit() == code) {
return req;

