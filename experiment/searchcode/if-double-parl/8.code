return ;
}
double sum2;
double parl = 0;
if ((rank) == (solvedCols)) {
for (int j = 0 ; j < (solvedCols) ; ++j) {
sum2 += s * s;
}
double correction = fp / (delta * sum2);
if (fp > 0) {
parl = java.lang.Math.max(parl, lmPar);

