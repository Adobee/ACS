FileObject [] replicatedFiles = nameserver.checkReplicatedFiles(clientIp);

for (int i = 0; i < replicatedFiles.length; i++) {
int randomPort = randomPort(8500, 65000);
replicatedFiles[i].fileSize,
randomPort, &quot;files/replicated/&quot;);
if(ipWhereToReplicate != nameserver.findIpNode(originalNodeHash)){

