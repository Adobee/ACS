elementList.add(element);
}

public int getFreePrimaryKey() {
Integer maxPrimary = 0;
for (ContentElement element : getContentList()) {
Integer val = element.getPrimaryKeyValue();
if (val > maxPrimary)

