private int frameLength;

public Animation(SpriteSheet sheet, int speed, int frameLength, int start, int end) {
return getFrame(currentFrame).getImage().getWidth(null);
}

public void update(long elapsedTime) {
if (frames.size() <= 1) return;

