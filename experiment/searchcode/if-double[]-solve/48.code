private double getD_element(int pos) {
double solve = A.get(pos, pos);
for (int i = 0; i < pos - 1; i++)
A.set(ry, i, swap);
}
}

private double getS_element(int x, int y) {
double solve = A.get(x, y);

