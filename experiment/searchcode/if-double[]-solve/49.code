long x, y, z, t1, t2, t3, t4; int count = 0;
out: for (long m = 2; m <= 250000; m++)
{
t1 = solveQuadraticP(1, 4 * m, 1 - m * m);
return (-1 * b + (long)Math.sqrt(b * b - 4 * a * c))/(2*a);
}
public static double solveQuadraticM (long a, long b, long c)
{
return (-1 * b - (long)Math.sqrt(b * b - 4 * a * c))/(2*a);
}
}

