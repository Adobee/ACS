* 			vertexC => true,
* 			...
* 		},
* 		...
* }
*/
public class AdjacencyMatrixGraph implements Graph {
private Map<Vertex, Map<Vertex, Boolean>> adjacencyMatrix;
Set<Vertex> neighbors = new HashSet<Vertex>();
for (Map.Entry<Vertex, Boolean> entry : this.adjacencyMatrix.get(vertex).entrySet()) {
if (entry.getValue()) {

