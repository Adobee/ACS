try {
tag.writeTo(appendable, forceBuild);
if (body != null) {
appendable.append(body);
tag.writeTo(appendable, forceBuild);
if (body != null) {
appendable.append(body);
}
appendable.append(&quot;</style>&quot;);

