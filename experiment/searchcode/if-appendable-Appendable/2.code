package org.srostov.utils;

import java.io.IOException;

public class IndentedAppendable implements Appendable {
return appendable;
}

private void ensureLineStarted() throws IOException {
if (atLineStart) {
appendable.append(indent);

