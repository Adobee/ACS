ArrayList<Double> list = new ArrayList<Double>();

void addToEnd(double value) {
list.add(value);
}
void addToStart(double value) {
list.remove(list.size()-1);
}
boolean checkIfContains(double value){
if (list.contains(value)) {

