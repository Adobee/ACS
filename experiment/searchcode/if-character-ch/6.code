scanner.unread();
return token;
}
scanner.unread();
// assert ch == &#39;0&#39;;
} else if (!Character.isDigit((char) ch)) {
ch = scanner.read();
++unreadCount;
}
if (Character.isDigit((char) ch)) {

