protected Map<U, T> mIdSet = Maps.newLinkedHashMap();

/** type for creating objects **/
protected Class<? extends U> mIdType;
/** translator for translating the combo box to the target type**/
super(new MigLayout(&quot;ins 0&quot;,&quot;[grow,fill]&quot;));

mSession = session;
mClass = clazz;
mIdType = idType;
mTranslator = translator;

