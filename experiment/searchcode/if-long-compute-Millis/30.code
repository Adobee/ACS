long start = System.currentTimeMillis();
long fileSize = compute(new File(FOLDER));
long end = System.currentTimeMillis();
System.out.println(&quot;Folder size is &quot; + fileSize);

}

public static long compute(final File file) {
if (file.isFile()) {

