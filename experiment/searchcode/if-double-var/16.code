int rows = o.length;
int cols = o[0].length;

double[] var = new double[m.length];
Arrays.fill(var, 0.0);
if (rows == 1 ) {
Arrays.fill(var, 0.0);
if (rows == 1 ) {
return var;
}

for (int j = 0; j < rows; ++j) {
double[] row = o[j];

