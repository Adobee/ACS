JDialog parent;

JButton confirm;

JButton abort;

private boolean confirmed = false;

public SimpleDialogListener(JDialog d, JButton confirm, JButton abort) {
confirmed = true;
} else if (source == abort) {
confirmed = false;
}

parent.setVisible(false);

