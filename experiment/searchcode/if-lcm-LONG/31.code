static long lcm(long a, long b) {
long g = gcd(a, b);
long ag = a / g;
if (Long.MAX_VALUE / ag < b)
long gcd = 0;
long lcm = 1;
for (int i = 0; i < N; i++) {
K[i] = scan.nextLong();
gcd = gcd(K[i], gcd);
lcm = lcm(K[i], lcm);
}


}
}

