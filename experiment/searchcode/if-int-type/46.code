int best = Integer.MIN_VALUE;
for(int i = 0; i < taste.length; ++i) {
if(!tasted[type[i]]) {
done = false;
y++;
a += taste[index];
tasted[type[index]] = true;
int result = y * a;

if(max < result) {

