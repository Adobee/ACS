package check.please.utils;

public enum MenuItemTypeEnum {

FOOD(1), DRINK(2);
private int type;
public static MenuItemTypeEnum getType(int type){
for(MenuItemTypeEnum itemType : MenuItemTypeEnum.values()){
if(itemType.type == type)

