//if all variables were not used the chromosome was thrown out and the original chromosome is maintained
if(chromosomes.size() == 1){
//if all variables were not used the chromosome was thrown out and the original chromosome is maintained
if(chromosomes.size() == 1){
chromosomes.add(originalChrome1);

