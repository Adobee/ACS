import optimization.evolutionary.geneticalgorithm.gene.Gene;

/**
* Represents the role of a chromosome in genetic algorithms.
* <p>A {@link Chromosome} object provides methods for accessing its fitness and {@link #clone()}ing mechanism.</p>
for (int i = this.genes.size(); --i >= 0;) {
if (!this.genes.get(i).equals(chromosome.genes.get(i))) {

