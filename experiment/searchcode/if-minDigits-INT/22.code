int b = Integer.parseInt(st.nextToken());
in.close();

int minDigits = 0;
int temp = a;
while (temp > 0)
num += pow;
}
}
}
}
if (minDigits != maxDigits)
{
d = maxDigits;
pow = (int) Math.pow(10, d / 2);

