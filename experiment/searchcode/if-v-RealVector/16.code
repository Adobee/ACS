public Boolean Overlap(RealVector point){
//Checks if any point overlaps with a box
for (BoxFeature box : boxes) {
current = point;
}
}
if (current == goal){
List<RealVector> temp = reconstruct_path(came_from, current);

