package recog.separation;

import java.util.Arrays;

public class RealVector {

private double[] coords;
private static void assertEqualLength(RealVector v1, RealVector v2) {
if (v1.coords.length != v2.coords.length) {
throw new IllegalArgumentException(&quot;vectors have different length: &quot;

