public static String format(int value)
{
//      if (nf == null)
//      {
//         nf = NumberFormat.getIntegerInstance(Locale.getDefault());
public static String format(int value,Locale locale)
{
if (nf == null)
{
nf = NumberFormat.getIntegerInstance(locale);

