Set<String> returnVal = new HashSet<String>();
if( !s.varType.isInteger() &amp;&amp; !s.varType.isBoolean() )
returnVal.add ( s.name );
private ASTStatement previous = null;

private boolean done = true;
private boolean first = true;

