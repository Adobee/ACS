y = c.getMY();
current = new Boolean[x][y];
previous = new Boolean[x][y];

for(int i = 0; i < x; i++) {
//c.setPixelText(i, j, 50,Color.AQUAMARINE, Integer.toString(surrounding));
if(current == previous)
{
System.out.println(&quot;ERROR! current == previous&quot;);

