// System.out.println(str);
if (str.length() < 13)
{
continue;
}
int maxStartIndex = str.length() - 13;
for (int i = 0; i <= maxStartIndex; i++)
{
long temp = multiplyFromString(str.substring(i, i + 13));

