resRamlID = resources.getIdentifier(&quot;config_BLRameRate&quot;, &quot;array&quot;, &quot;android&quot;);

if (resRamlID != 0) {
resRampItemArray = resources.getStringArray(resRamlID);
resbright_Rate = targetMap.get(BRIGHT_RAMP_RATE);
resdark_Rate = targetMap.get(DARK_RAMP_RATE);

// check go if can use resource config ;

