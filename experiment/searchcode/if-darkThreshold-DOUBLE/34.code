DigitChunk chunk = getChunkAt(c, r);
if (chunk.getAverageGrayLevel() < darkThreshold()){
matrix[r][c] = 1;
} else {
DigitChunk chunk = getChunkAt(c, r);
if (chunk.getAverageGrayLevel() < darkThreshold()){
matrix.append(&quot;#&quot;);

