import com.zuehlke.carrera.console.ConsoleContext;
import com.zuehlke.carrera.razor.ObservableType;
import org.springframework.beans.factory.annotation.Autowired;
context.turnAllDisplayOff();
}
int axis = 0;
if ( tokens.length >= 2 ) {
ObservableType observableType;

