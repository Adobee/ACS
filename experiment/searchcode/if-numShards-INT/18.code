Args optionalArgs;

private int numShards;
private Coordinator coordinator;
private ShardingScheme shardingScheme;
public NewDomainSpec(Coordinator coordinator, ShardingScheme shardingScheme, int numShards, Args args) {
if (numShards <= 0) {

