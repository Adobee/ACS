/** minimum length of a word to be counted */
int minWordLength = DEFAULT_MIN_WORD_LENGTH;

/** array holding the individual sentenceDelimiter characters */
* @param minWordLength The minimum word length.  Passing a number less than or equal to zero
* will reset the word length to the default.
*/
public void setMinWordLength(int minWordLength) {
if (minWordLength <= 0) {

