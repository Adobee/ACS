* Adds `element` to this iterable. If this iterable does not permit duplicates and already contains `element` it
* Removes `element` from this iterable, if it is present. If this iterable does not contain `element` it does

