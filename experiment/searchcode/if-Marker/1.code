Set<Marker> markers = new HashSet<Marker>();
markers.addAll(fullMarkerSet);
if(markers != null){
for (Marker m : markers) {
Marker marker = fullMarker;
if(marker != null){
marker.setGroup(null);
}

return marker;
}
}

