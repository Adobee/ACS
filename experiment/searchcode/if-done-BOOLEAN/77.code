private Scanner scaner;
private boolean done;
public ClientReader(Scanner scanner) {
this.scaner = scanner;
this.done = done;
}

@Override
public void run() {
while(!done){
if(scaner.hasNextLine()){

