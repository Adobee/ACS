String text;
boolean isDone;

public Item(String text, boolean isDone) {
this.text = text;
String done = &quot;&quot;;
if (isDone) {
done = &quot;(done)&quot;;
}
return String.format(&quot;%s %s&quot;, text, done);
}
}

