String text;
boolean isDone;

public ToDoItem(String text, boolean isDone) {
this.text = text;
String done = &quot;&quot;;
if (isDone){
done = &quot;[X] done&quot;;
} else { done = &quot;[ ] it ain&#39;t done&quot;;}
return String.format(&quot;%s %s&quot;, text, done);
}
}

