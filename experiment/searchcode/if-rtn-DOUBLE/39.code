a.set(j,tmp);
}

public static boolean isListEmpty(final List<?> lst)
{
boolean rtn=false;
if ( lst==null )
for ( Double temp : listTwo )
{
if ( !ListTools.isInList(listOne,temp.doubleValue()) )
{
rtn=false;

