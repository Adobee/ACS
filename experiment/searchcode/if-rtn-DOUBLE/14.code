public double asFahrenheit() {
double rtn = temperature;
if ( scale != FAHRENHEIT ) {
rtn = (9.0/5) * temperature + 32;
}
return rtn;
}

public double asCelsius() {
double rtn = temperature;
if ( scale != CELSIUS ) {

