List<JavaInfo> result = new ArrayList<JavaInfo>();
for (String javaPath : javaExecs)
{
if (!new File(javaPath).exists())
return ((JavaInfo) javas.get(i)).path;
}
return null;
}

public static JavaInfo parseWinJavaVersion()
{
if (prefered != null)

