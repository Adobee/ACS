public void setBeta(double beta) {
this.beta = beta;
}

public double getFuzzy(double t) {
double A_t = -1;
if (a - alpha <= t &amp;&amp; t <= a)
double A_t = -1;
if (a - alpha <= t &amp;&amp; t <= a)
A_t = 1 - (a - t) / alpha;

