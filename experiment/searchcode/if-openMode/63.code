Directory dir = FSDirectoryFactory.getInstence().create(dirPath);

OpenMode openMode = null;

if (openModeString.equals(&quot;create&quot;)) {
openMode = OpenMode.CREATE;

} else if (openModeString.equals(&quot;append&quot;)) {

