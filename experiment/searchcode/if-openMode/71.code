List<Event> eventList=eventListAll.subList(i*num, (i+1)*num);
buildEventIndexImpl(eventList,OpenMode.CREATE_OR_APPEND);

}

if(eventListAll.size()%num>0){
List<Writer> writerList=writerListAll.subList(i*num, (i+1)*num);
buildWriterIndexImpl(writerList,OpenMode.CREATE_OR_APPEND);
}

if(writerListAll.size()%num>0){

