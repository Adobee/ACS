Fraction result;

Fraction f1 = parseFraction(a);
Fraction f2 = parseFraction(b);

//Test auf Gleichheit von Bruechen
result = calc(f1, operator, f2);

if(!a.matches(Fraction.REGEX)||!b.matches(Fraction.REGEX)){
return Double.toString(result.doubleValue());

