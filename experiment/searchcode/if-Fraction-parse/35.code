/**Compares the fraction with another.
*
* @return
*/
@Override
public boolean equals( Object obj ){
if( obj instanceof Fraction ) {
public Fraction power(int exp) throws FractionConstructionException{
if(exp < 0){
Fraction temp = Fraction.createFraction(this.mDenominator, this.mEnumerator);

