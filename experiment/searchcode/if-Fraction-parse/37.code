public Fraction recursiveReduce(Fraction inFrac){
Fraction nonreducedFraction = new Fraction(inFrac);
if(inFrac.getNumerator() == 1){
return inFrac;
* @return : If reducible - returns reduced form of the original Fraction.
* If irreducible - returns the original Fraction
*/
public Fraction reduce(){

