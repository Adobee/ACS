this.denominator = Integer.parseInt(fullFrac[1]);
this.fractionCount = 1;
}

public Fraction(Fraction copy){
this.denominator = denom;
this.fractionCount = 1;
}

/**
* reduce():
* @return : If reducible - returns reduced form of the original Fraction.

