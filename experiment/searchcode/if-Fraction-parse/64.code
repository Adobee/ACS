this.denom = 1;
}
public Fraction(long numerator, long denominator) {
if (denominator == 0) throw new ArithmeticException(&quot;Fraction has a zero denominator&quot;);
if (tokens<1||tokens>2) throw new NumberFormatException(&quot;Fraction is in improper format&quot;);
if (tokens == 1) {
this.num = Long.parseLong(frac.nextToken());

