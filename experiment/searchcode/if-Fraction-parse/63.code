public Fraction(String string) {
if (string.indexOf(&#39;/&#39;)==-1){
numerator = Integer.parseInt(string.trim());
numerator=Integer.parseInt(string.split(&quot;/&quot;)[0].trim());
denominator = Integer.parseInt(string.split(&quot;/&quot;)[1].trim());
if(string.split(&quot;/&quot;).length>2)

