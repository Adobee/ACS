public static String formatContent(String content, String delimiter) {
String formattedContent = content.trim();
if (content.startsWith(delimiter)) {
formattedContent = formattedContent.substring(delimiter.length());
}
if (content.endsWith(delimiter)) {

