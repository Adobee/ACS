DynamicValue(int initValue, int maxValue) {
if(initValue < 0 || initValue > maxValue) {
throw new IllegalArgumentException();
}
value = initValue;
this.maxValue = maxValue;
}

/**
*
* @return -1 if value <= 0. 0 if not.

