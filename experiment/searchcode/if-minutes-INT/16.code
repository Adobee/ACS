NEVER(0), EVERY_MINUTE(1), EVERY_5_MINUTES(5), EVERY_10_MINUTES(10), EVERY_15_MINUTES(15), EVERY_30_MINUTES(30), EVERY_HOUR(
60), EVERY_3_HOURS(180);

private final int minutes;

private TransmissionInterval(int minutes) {
this.minutes = minutes;
}

public int getMinutes() {
return minutes;
}

public static TransmissionInterval byMinutes(int minutes) {

