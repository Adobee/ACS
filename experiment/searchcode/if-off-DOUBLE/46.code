private static void sort1(double x[], int off, int len, double y[]) {
// Insertion sort on smallest arrays
if (len < 7) {
// Choose a partition element, v
int m = off + (len >> 1); // Small arrays, middle element
if (len > 7) {

