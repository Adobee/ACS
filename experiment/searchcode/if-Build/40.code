* and open the template in the editor.
*/

package lolbuilder.calculation;

import java.util.ArrayList;
import lolbuilder.build.Build;
import lolbuilder.mainClasses.ItemBuildGenerator;
public BBtadThread(ArrayList<Build> builds,Build bestBuild) {
super(builds, bestBuild);
}

public void run() {
if (bestBuild==null) bestBuild=builds.get(0);

