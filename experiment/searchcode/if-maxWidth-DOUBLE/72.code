public BasketObject(GameObject parent, int number, double maxWidth, double[] fillColour, double[] lineColour) {
super(parent, new double[]{-maxWidth / 4, -maxWidth / 2,
maxWidth / 4, -maxWidth / 2,

