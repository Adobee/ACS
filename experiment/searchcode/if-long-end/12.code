in.close();

long x = 0;
for (long i = 0; i < n; i++) {

long endPlatform = i * m + l;
long beginNextPlatform = (i + 1) * m;
x = (endPlatform / d) * d + d;

if (i == n - 1 || x < beginNextPlatform) {

