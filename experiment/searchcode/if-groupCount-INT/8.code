this.groupCount = groupCount;
}

public int getGroupCount() {
if( groupCount>0 )
return groupCount;
if( groups!=null )
this.groups = groups;
//		if(null != groups)
//			this.groupCount = ((Integer)groups.size()).shortValue();

