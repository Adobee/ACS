public static double convert(double degree, ScaleType currentScale) {
if (currentScale.equals(ScaleType.CELSIUS)) {
return degree * 1.8 + 32;
}
if (currentScale.equals(ScaleType.FAHRENHEIT)) {
return (degree - 32) / 1.8;

