import nedhyett.scarlet.entity.s2d.EntityTracker2D;
import nedhyett.scarlet.util.ScarletLog;
import nedhyett.scarlet.vermillion.v2d.trajectory.StraightTrajectory;
ScarletLog.severe(&quot;Entity %s does not have a valid constructor or caused an exception when it was created.&quot;, entityID);
return;
}
}

@Override
public boolean validate() {
if (entityID == null) {

