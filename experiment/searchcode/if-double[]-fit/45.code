assert sol != null;
double fit = this.getFitness(sol);
S bestSol = sol;
double bestFit = fit;
this.setDurationInitialization(
assert newSol != null;
final double newFit = this.getFitness(newSol);

if (this.accept(sol, fit, newSol, newFit, t)) {

