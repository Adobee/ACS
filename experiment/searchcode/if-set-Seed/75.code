/** Get the seeds in pit of current player, put it in own kalah and set seed to 0 */
public void receive(int amountOfSeed) { //Capture en change turn in afzonderlijke methodes voor overzichtelijkheid, getopposite().capture()
if (amountOfSeed == 1 &amp;&amp; this.getSeed() == 0) { //&amp;&amp; dubbele bespaart wat energie

