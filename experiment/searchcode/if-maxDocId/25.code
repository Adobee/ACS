isSameDoc = false;
}
maxDocId = Math.max(iDocId, maxDocId);
}
if (isSameDoc) {
private void addDocPos(ArrayList<QryResult> resultList,int[] docPos, int maxDocId){
for (int i = 0; i < args.size(); i++) {
if (resultList.get(i).invertedList.postings.get(docPos[i]).docid != maxDocId) {

