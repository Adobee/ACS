Color c = new Color(200, 109, 172);

//StandRight
if(state == 0)
{
g2.setColor(c);
g2.fillRect(x+2*s, y+0, 2*s, 1*s);
//StandLeft
if(state == 1)
{
//Plume
g2.setColor(c);
g2.fillRect(x+7*s, y+0, 2*s, 1*s);

