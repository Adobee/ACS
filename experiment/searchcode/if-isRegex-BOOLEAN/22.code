@Column(name = &quot;isRegex&quot;)
private boolean isRegex;
@Column(name = &quot;toUpper&quot;)
private boolean toUpper;
@Column(name = &quot;toLower&quot;)
public StringFix(String match, boolean isRegex) {
this.setMatch(match);
this.setIsRegex(isRegex);

