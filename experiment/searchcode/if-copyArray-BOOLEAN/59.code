public static boolean reverse(int[] A, Stack<int[]> record) {
boolean isStep = false;
if(check(A)) {
isStep = true;
if(isStep) break;
}
}
}
if(isStep) {
int[] tmp = copyArray(A);
record.push(tmp);
}
return isStep;

