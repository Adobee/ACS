* recursive case: split current string into two parts, the prefix takes the first 1, 2, and 3 digits,
* and the remaining goes to tail.
* Check if prefix is Valid and recursively process tail. If valid prefix, copy tail results to prefix res.
firstDigitZero = true;

// cannot be all zeros with more than one zero
boolean allZeros = false;

