* backwards from there.
*
* Will fail if i == Long.MIN_VALUE
*/
static void getChars(long i, int index, char[] buf) {
buf[--charPos] = IntToString.digits[r];
i2 = q2;
if (i2 == 0) break;
}
if (sign != 0) {

