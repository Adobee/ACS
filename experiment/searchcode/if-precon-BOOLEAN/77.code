PreCon.positiveNumber(index);
PreCon.isValid(index < 4);

if (_sign == null)
return &quot;&quot;;
PreCon.positiveNumber(index);
PreCon.isValid(index < 4);
PreCon.notNull(line);

if (_changeEvent != null) {

