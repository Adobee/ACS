return capturePoints.get(scanOrder[currentStage]).get(currentPointNum+1);
else
if(currentStage+1 != numBands)
return capturePoints.get(scanOrder[currentStage+1]).get(0);
return capturePoints.get(scanOrder[currentStage]).get(currentPointNum);
}

public float[] step(){
if(currentPointNum + 1 != capturePoints.get(scanOrder[currentStage]).size()) {

