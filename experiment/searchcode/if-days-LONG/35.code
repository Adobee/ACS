public static String getDurationString(long ms){
long mins = ms / 60000;
long hrs = mins / 60;
long days = hrs / 24;
if (days != 0)
return days + &quot; days&quot;;
if (hrs != 0)
return hrs + &quot; hours&quot;;
return mins + &quot; minutes&quot;;
}
}

