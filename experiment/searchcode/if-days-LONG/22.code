return Math.abs(deviation);
}

long days() {
return getAbsoluteDeviation() / (24*60*60);
private String dayString() {
long days = days();
if (days == 1)
return days + &quot; day, &quot;;
if (days > 1)

