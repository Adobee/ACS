long totalHours = totalMinutes /60;
int currentHour = getCurrentHour (totalHours);

long totalDays = totalHours / 24;
public static int getCurrentYear (long totalDays)   {
int days = 0;
int currentYear = 1969;

