log.info(&quot;=====Compute Predict Accuracy&quot;);
int totalNum = 0;
int predictNum = 0;
&amp;&amp; (predictLabel = brPredict.readLine()) != null) {
if (label.equals(predictLabel)) {
predictNum++;

