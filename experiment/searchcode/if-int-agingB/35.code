return x[0];
}

int nbPoints;
int signChangeIndex;
if (y[0] * y[1] < 0) {

// reduce interval if it brackets the root
double yB    = y[signChangeIndex];
double absYB = FastMath.abs(yB);
int agingB   = 0;

// search loop

