this.max = max;
}

public int generateRandom()
{
final int number = createRandom();
/*
* if (number < min) { number = generateRandom(); System.out.println(&quot;REGENERATING&quot;); } else if (number >= min) { return number; } return number;

