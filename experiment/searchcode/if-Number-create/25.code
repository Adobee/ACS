for (long i = 1; i < number; i++)
{
createSequence(i);
if (govno > sequenceNumber)
govno = 0;       // обнуляем говно
}
}
private long createSequence(long number)         // рекурсивно вычисляем кол-во членов цепочки

