public class MixedSound extends CombinedSound {

double compression = 1;

public MixedSound (AudioFormat audioFormat, final Sound... sounds) {
val += fval[i];
}
//    val /= compression;
//    if(val > 0.7f) {
//      compression *= 1.001;

