return res[0];
}

private void helper(int n, int row, int[] columnForRow, int[] res) {
if (row == n) {
res[0]++;
return;
}
for (int i = 0; i < n; i++) {
columnForRow[row] = i;
if (check(row, columnForRow))

