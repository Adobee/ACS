return null;
} else if (s.length() == 0 || s.length() == 1) {
return s;
}

int N = s.length();
int maxLen = 1;
int maxRow = 0;
int maxCol = 1;
boolean [][] dp = new boolean [N][N];
for (int lastRow = N - 1; lastRow >= 0; --lastRow) {

