for(int i = 0; i < a.length; i++) {
A += a[i] * funcVal[i];
B += a[i];
}
if(Math.abs(B) < 1e-6)
* @return
*/
public double mf_d1(double d1, int kind_size) {
switch (kind_size) {
case SMALL:
if (d1 < 3)

