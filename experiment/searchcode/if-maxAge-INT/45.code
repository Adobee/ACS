int n = scanner.nextInt();

int maxAge = 0;

for (int i = 0; i < n; i++) {
System.out.println(i + 1 + &quot;人目&quot;);
String name = firstName + &quot; &quot; + lastName;

System.out.print(&quot;年齢：&quot;);
int age = scanner.nextInt();

if (age > maxAge) {
maxAge = age;

