public class Age implements Activity
{
private final int maxAge;	// Animal dies if this old

/**
* Constructor
* @param maxAge
* @return species max age
*/
public int getMaxAge()
{
return maxAge;
}

/**
* Animal gets older, and dies if max age is reached

