* @param minAge
* @param maxAge
* @return
*/
public OldList getSubList(final int minAge, final int maxAge) {
for (Person p : this) {
if (p.getAge() >= minAge &amp;&amp; p.getAge() <= maxAge) {
list.add(p);
}
}

return list;
}
}

