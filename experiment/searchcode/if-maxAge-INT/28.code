Pattern pattern = Pattern.compile(&quot;[0-9]+&quot;);
int maxAge = Integer.MIN_VALUE;
int maxIdx = 0;
for (int i = 0; i < data.length; i++) {
int age = 0;
if (m.find()) {
age = Integer.parseInt(m.group());

