private Graphics g;
private String[][] markers = new String[10][10];
private boolean notFirst = false;
for (int j = 0; j < markers[i].length; j++)
{
double r = Math.random();
if (r < 0.4)
markers[i][j] = &quot;grass&quot;;

