import org.apache.commons.math.distribution.PoissonDistributionImpl;

import com.github.traviolia.simlib.core.report.DiscreteDistPoissonReporter;
if (p == 1.0) return Long.MAX_VALUE; // should be infinity, can&#39;t get closer

PoissonDistributionImpl poi = new PoissonDistributionImpl(this.getMean());

