* This validator takes care of validating the edittext. The input will be valid
* only if the number is a valid credit card.
digit = Integer.parseInt(cardNumber.substring(i, i + 1));
if (doubled) {
addend = digit * 2;
if (addend > 9) {

