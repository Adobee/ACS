int mid = (begin + end)/2;
currentPos = mid;
if(words[mid].equals(&quot; &quot;))
{
for(int i = mid + 1; i < words.length; i++)
break;
}
}

if(currentPos == -1)
{
for(int i = mid - 1; i >= 0; i--)
{
if(!words[i].equals(&quot; &quot;))

