for (Period period : periods) {
if (period.getPeriodType().isMainPeriod() &amp;&amp; inRunning == period.getPeriodType().isInRunning()) {
LongRequest periodByIdRequest = new LongRequest(periodId);

try {
PeriodsResponse periodsResponse =

