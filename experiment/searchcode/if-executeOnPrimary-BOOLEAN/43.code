* decide to weaken the compatibility contract.
* @since 6.5
*/
public boolean isCompatible(Pool p) {
if (p == null) return false;
public synchronized void basicDestroy(boolean keepAlive) {
if (!isDestroyed()) {
this.destroyed = true;
if (getLoggerI18n().configEnabled()) {

