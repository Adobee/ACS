Integer exponent;

public Exponent(int exponent) {
this.exponent = exponent;
}

@Override
public double apply(double argument) {
if (exponent == 0) {
return 1;
} else if (exponent == 1) {
return argument;

