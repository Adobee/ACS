/* Number, and exponent input. */
double number = readInt(&quot;Please enter a number: &quot;);
int exponent = readInt(&quot;Please enter an exponent: &quot;);
private double raiseToPower(double base, int exponent) {
double baseCounter = base;

if (exponent > 0)

