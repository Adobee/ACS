
public class Term implements Comparable<Term>{
private double coefficient;
private int exponent;
setExponent( exponent );
}

@Override
public int compareTo(Term term) {

if ( term.getExponent() == this.exponent ) return  0;

