package welcome.enums;

import java.time.LocalTime;

/**
* This enum designed to represents all possible
private static LocalTime NIGHT_START = LocalTime.of(23, 0);

public static DayPeriod getMoment(LocalTime time){
if (isInRange(time, MORNING_START, DAY_START))

