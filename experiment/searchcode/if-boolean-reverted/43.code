import javax.persistence.*;

/**
* The primary key class for the number_of_reverted_revisions database table.
this.dayNumber = dayNumber;
}

public boolean equals(Object other) {
if (this == other) {
return true;
}
if (!(other instanceof NumberOfRevertedRevisionsPK)) {

