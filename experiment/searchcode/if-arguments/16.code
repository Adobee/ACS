System.exit(1);
} else if (maximumArguments == -1) {
System.err.println(
&quot;The variable argument must be the last one.&quot;);
else
minimumArguments++;
if (argument.isVariable()) {
maximumArguments = -1;

