public static void NormalizeView(View... views){
if(views == null || views.length <=1){
return;
}
int maxWidth = 0;
int maxHeight = 0;
for (View view : views) {
int width = view.getWidth();
int height = view.getHeight();

