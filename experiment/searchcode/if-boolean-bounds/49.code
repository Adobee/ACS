private boolean debugging;
private boolean bounds_set = false;
private boolean non_debug_code_hit = false;
new Rectangle(originalBounds.x, originalBounds.y,
originalBounds.width, originalBounds.height);

if (debugging)
{
originalBounds = new Rectangle(5, 5, 5, 5);

