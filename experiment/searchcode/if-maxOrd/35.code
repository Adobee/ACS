minOrd = ord + 1;
}
}

final long maxOrd;
if (upperVal == null) {
maxOrd = values.getValueCount() - 1;
final long ord = values.lookupTerm((BytesRef) upperVal);
if (ord < 0) {
maxOrd = -2 - ord;
} else if (includeUpper) {

