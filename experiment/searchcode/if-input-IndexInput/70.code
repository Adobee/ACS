package lucene;

import org.apache.commons.io.IOUtils;
import org.apache.lucene.store.IndexInput;
* @param input2 IndexInput with the (hopefully) same content
* @return <code>true</code> if both have the same content, <code>false</code> else

