public static int TYPE_BY_ROAD = 3;

protected List<Double> criterionValues;
protected LVertex destination;
criterionValues.add(v);
}

public double getCriteriaValue(int index) {
if(index >= 0 &amp;&amp; index < criterionValues.size()) {

