Boolean isEn = CharUtil.isEnChar(ch);
Boolean isCn = CharUtil.isChineseChar(ch);
Boolean isBlank = CharUtil.isBlankChar(ch);

if ((isEn || isCn || isBlank) == false) {
result = false;
break;
}
}
return result;
}



}

