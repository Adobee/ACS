int increment=2;

do {
for (int i=0;i<4;i++) {
if (isPrime(curr)) {
primeCount++;
}
totalCount++;
curr+=increment;
}
increment+=2;
} while ((primeCount/totalCount)>=0.10 &amp;&amp; (primeCount/totalCount)!=0.0);

