for(Shape shape: shapes) {
double area = shape.getArea();
if (area < smallestArea) {
smallestArea = area;
for(Shape shape: shapes) {
double area = shape.getArea();
if (area > biggestArea) {
biggestArea = area;

