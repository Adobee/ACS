len = text.length();
this.delimiter = delimiter;
parsePosition = 0;
parseNext();
}

public boolean hasNext() {
next = 0;

if (parsePosition == len) {
hasNext = false;
return;
}

while (parsePosition< len) {

