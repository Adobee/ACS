this.num = BigInteger.valueOf(num);
this.den = BigInteger.ONE;
}

public Rational(double numb) {
BigInteger den = BigInteger.valueOf(temp1);

if (!den.abs().equals(BigInteger.ONE)) {
BigInteger gcd = num.gcd(den);

