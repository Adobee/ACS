long num = this.num;
long den = this.den;
if(den < 0) { num = (-1)*num; den = (-1)*den; }
}

private void reduce()
this.num /= g;
this.den /= g;
}

private long gcd(long a,long b)
{
long g;
if(b == 0) { return a; }

