//Create fraction with num n and den d
public Fraction(int n, int d) {
if (d < 1) {
throw new IllegalArgumentException();
reduce(f3.num, f3.den);
return f3;
}

//Test to see if two fractions are equal
public boolean equals(Fraction f2) {

