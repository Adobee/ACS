for (int i = 0; i <= limit; i++) {
if (i % 2 != 0) {
double x_pow = pow(x, i);
double divident = fac(i);
for (int i = 0; i <= limit; i++) {
if (i % 2 == 0) {
double x_pow = pow(x, i);
double divident = fac(i);

