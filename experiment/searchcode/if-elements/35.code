@Override
public int[] sort(int[] elements) {
if(elements == null || elements.length == 0) return new int[0];
public int[] quickSort(int[] elements, int min, int max) {
if(max <= min) return elements;
int i = min;

