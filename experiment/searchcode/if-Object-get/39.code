changeRightDoor(upperDoor);
}
} else if (doorClicked.getRotation() == 1) {
RSObject westDoor = getDoor(object.getId() - 3, object.getX() - 1, object.getY(), object.getZ());
RSObject eastDoor = getDoor(object.getId() + 3, object.getX() + 1, object.getY(), object.getZ());
if (westDoor != null) {

