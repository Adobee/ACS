public static ReckoningTypeEnum getOpenCode(boolean open, boolean closed) {
if (open &amp;&amp; closed) {
return OPEN_AND_CLOSED;
} else if (open) {
return OPEN;
} else if (closed) {
return CLOSED;
}

return null;
}
}

