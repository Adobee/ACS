String value = extractValue(partes[i]);
httpRequest.setSoapAction(value);
}else if ( partes[i].contains(&quot;Content-Type&quot;) ){
private HttpRequest extractBody(HttpRequest httpRequest, String bodyIn){

if(httpRequest.getMethod().equals(&quot;POST&quot;)){

