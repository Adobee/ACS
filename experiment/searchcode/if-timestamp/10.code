this.timestampSec=logLine.substring(6,8);;

}

public boolean lowerTime(LogEntry e){

if (this.timestampHour.compareTo(e.timestampHour)<0) return true;

if (this.timestampHour.compareTo(e.timestampHour)==0 &amp;&amp; this.timestampMinute.compareTo(e.timestampMinute)<0) return true;

