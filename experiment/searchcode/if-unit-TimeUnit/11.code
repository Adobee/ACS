final class TimeUtil
{
public static String time(long utime)
{
if(TimeUnit.SECONDS.toHours(utime) != 0)
TimeUnit.SECONDS.toSeconds(utime) - TimeUnit.MINUTES.toSeconds(TimeUnit.SECONDS.toMinutes(utime))
);
if(TimeUnit.SECONDS.toMinutes(utime) != 0)

