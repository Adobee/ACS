// Check for invalid input values

if (np <= 0.0) {
return -1;
}

double rm = np + par;
m = (int) rm;                            // mode, integer
p4 = p3 + c / lr;
}
}

double U;
int K;
if (np < 10) {                                      //Inversion Chop-down

