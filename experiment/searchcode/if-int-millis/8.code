private double detMillis;

public ClusterShell(int w, int h, double x, double y, double millis) {
super(w, h, x, y);
public boolean countdownDet(double millis)
{
detMillis -= millis;

if (detMillis <= 0)
return true;
else
return false;
}

}

