private int denominator; //分母

public MixedNumber(int intNum, int numerator, int denominator)
{
if(denominator == 0)
else
return a;
}

@Override
public String toString() {
if(intNum == 0)
return numerator + &quot;/&quot; + denominator;

