int len = str.length();
double ret = 0;
if (len == 0) {
return 0;
}
boolean flag = false;// 判断是否开始读取数字
int neg = 1;
int noNeg = str.indexOf(&quot;+&quot;);
int isNeg = str.indexOf(&quot;-&quot;);
int begin = 0;
if (noNeg != -1 &amp;&amp; isNeg != -1) {

