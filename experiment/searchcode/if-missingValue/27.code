private final DateTime missingValue;

/**
* Ignore input field value, always return the current system time.
// this value should never be set for production data
@JsonProperty(&quot;missingValue&quot;) DateTime missingValue

