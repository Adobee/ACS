while (!(startpos.equals(goalpos)))
{
if (startpos.getFloor() < goalpos.getFloor())
{
startpos.setFloor((startpos.getFloor())+1);
mymaze.setPosition(startpos, 0);
}
else if (startpos.getFloor() > goalpos.getFloor())

