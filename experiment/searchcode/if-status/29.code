public int compareTo(StatusWrapper statusWrapper) {
if (statusWrapper == null || statusWrapper.getStatus() == null || this.status == null) {
return String.format(&quot;%s&quot;, status.getRetweetCount());
}

@Override
public boolean equals(Object obj) {
if (obj == null) {

