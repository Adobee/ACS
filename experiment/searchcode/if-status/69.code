public static <T> Status<T> createSearchStatus(List<T> result) {
Status<T> status = new Status<T>();

if (!result.isEmpty()) {
Status<T> status = new Status<>();

if (row >= 1) {
status.setCode(201);
status.setStatus(true);

