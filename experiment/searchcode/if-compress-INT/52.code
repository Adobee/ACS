char[] arr = str.toCharArray();
int  chCounter = 0;
char chCompress = arr[0];
StringBuilder out = new StringBuilder(str.length());
for (int i = 0; i < arr.length; i++) {
if(arr[i]==chCompress){
chCounter++;

