public int compare(Entry<String, Double> o1, Entry<String, Double> o2) {
if ((null != o1.getValue()) &amp;&amp; (null != o2.getValue())) {
percentedItems = new ArrayList<>(percentedItems.subList(0, lastSignificantItem));

double coveredPercents = 0;

for (Entry<String, Double> entry : percentedItems) {

