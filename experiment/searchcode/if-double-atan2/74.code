import java.util.*;
import java.lang.*;

class Vertex {
public double x;
public double y;
return Math.sqrt((x*x) + (y*y));
}

public double angle() {
double result = Math.atan2(y, x);
if (result >= 0) {

