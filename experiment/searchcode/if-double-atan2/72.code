double[][] Pos = matrix(v[0], v[1], v[2], v[3], v[4], v[5]);
double[][] goal;
if(base==null)
goal=Pos;
else
goal= mul34(base, Pos); // in WORLD frame BASE*POS(in BASE)= GOAL
double[][] T6;
if (tool == null) {

