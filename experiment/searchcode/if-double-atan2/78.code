double dx1 = cx - sx;
double dy1 = cy - sy;
double a1 = Math.atan2(dy1, dx1);
double dx2 = x - cx;
double dy2 = y - cy;
double a2 = Math.atan2(dy2, dx2);
double a = Math.abs(a2 - a1);
if (a > Math.PI / 2) {

