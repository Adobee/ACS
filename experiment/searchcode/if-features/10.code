public void selectFeatures(AccumulatorDataSet accumulator) throws Exception
{
// if there is a selector, use it to select the features.
// else, return all the features.
if (featuresSelector != null)

