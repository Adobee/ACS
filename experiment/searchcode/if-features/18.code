package com.l2k.module.entitylinking.elu.features;

import com.google.gson.JsonArray;
import com.google.gson.JsonObject;
public Entity_svm_features(JsonObject features) {
this.entity_features=new JsonArray();
this.nonentity_features=new JsonArray();

if(!(features==null)){

