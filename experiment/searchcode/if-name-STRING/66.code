public Recipent(String raw)
{
if (raw.contains(&quot;<&quot;))
{
String info[] = raw.split(&quot;<&quot;);

name = info[0].substring(0, info[0].length() - 1);
public String getAddress()
{
return address;
}

public String toString()
{
if (name.equals(&quot;&quot;))
return &quot;Recipent(address:&quot; + address + &quot;)&quot;;

