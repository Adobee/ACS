Percent(&quot;百分比&quot;),
Fixed(&quot;固定值&quot;);

private String desc = &quot;&quot;;

private PortletSizeMode(String desc){
this.desc = desc;
}

public static PortletSizeMode getMode(String typeStr){
for(PortletSizeMode type : values()){

