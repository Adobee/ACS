import com.cheddy.storage.RefactoredClass;

public class Refactoring {

private Refactoring() {
}

public static String refactorDesc(String desc) {
for (int i = 0; i < types.length; i++) {
if (desc.endsWith(types[i])) {
desc = desc.replaceAll(types[i], refactoredTypes[i]);

