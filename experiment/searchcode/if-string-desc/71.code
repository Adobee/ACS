private static final int MAX_SIZE = 2;

private String number;

private String desc;
String[] str = source.split(StringUtil.SMALL_SEPARATOR);
if (str.length == MAX_SIZE)
{
number = str[0];
desc = str[1];

