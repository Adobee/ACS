public abstract int maxX();

public abstract double getData(int i);

public double maxY() {
if (maxX() == 0) return 0;
maxY = Math.max(maxY, getData(i));
}
return maxY;
}


public double minY() {
if (maxX() == 0) return 0;

