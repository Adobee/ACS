double[][] points, int currentDegree, int amount) {
double new_coef = 0;

for (int i = 0; i < amount; i++) {
double sum = 0.0;
currentError = currentError / (2 * numberOfPoints);
if (Data.regularization) {
for (int j = 1; j <= currentDegree; j++) {

