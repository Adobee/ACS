flipLayout.setCameraDistance(8000 * getResources().getDisplayMetrics().density);
}

public int getCurrentPosition() {
return Math.round(currentDegree/180f);
float value = ((Float) (animation.getAnimatedValue())).floatValue();

if(Math.floor(currentDegree/90f) != Math.floor(value/90f) &amp;&amp;

