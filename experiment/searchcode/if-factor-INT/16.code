while (x != 0) {
int right = x % 10;
int left = x / factor;

if (left != right) {
return false;
int left = x / (int)factor;

if (left != right) {
return false;
}

//x /= 10;
x = (x % (int)factor) / 10;

