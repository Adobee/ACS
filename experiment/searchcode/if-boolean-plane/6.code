public boolean setPlanePosition(int _planeNumber, int _x, int _y)
{
Plane plane;
if((plane = (Plane)(planes.get(new Integer(_planeNumber)))) == null)
return true;
}

// スプライトプレーンの表示切替 On/Off
public boolean setPlaneView(int _planeNumber, boolean _view)
{
Plane plane;
if((plane = (Plane)(planes.get(new Integer(_planeNumber)))) == null)

