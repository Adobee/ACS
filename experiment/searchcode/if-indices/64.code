ArrayList<Integer> indices = new ArrayList<Integer>();
indices.add(-1);
indices.add(-1);
if (A == null || A.size() == 0) return indices;
private void search(ArrayList<Integer> A, int l, int r, int target, ArrayList<Integer> indices) {
if (l > r) return;
int m = l + (r - l) / 2;
if (A.get(m) == target) {

