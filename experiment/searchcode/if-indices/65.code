ArrayList<Integer> indices = new ArrayList<Integer>();
indices.add(-1);
indices.add(-1);
if (A == null || A.size() == 0) {
private void find(ArrayList<Integer> A, int target, int l, int r, ArrayList<Integer> indices) {
if (l <= r) {
int m = l + (r - l) / 2;

