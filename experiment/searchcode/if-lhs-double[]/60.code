public static double operation(String operator, double lhs, double rhs) {
double result = 0;
if (operator.equals(&quot;+&quot;)) {
result = operation(operations.pop(), lhs, rhs);
numbers.prepend(result);
} // While

double resultD = numbers.pop();
if (resultD % 1 == 0)

