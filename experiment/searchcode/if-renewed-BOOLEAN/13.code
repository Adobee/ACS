String fileURL = &quot;http://update.lovinglemoore.com/renewed-pos.jar.update.gpg&quot;;

try {
CodeSource codeSource = Main.class.getProtectionDomain().getCodeSource();
thisDir + &quot;\\temp\\renewed-pos.jar.update.gpg&quot;);
if (valid) {
System.out.println(&quot;Verified!&quot;);

