public class TimePeriod {
private Date begin;
private Date end;
private int liveDuration;
for (TimePeriod timePeriod : timePeriods) {
if (df.format(now).equals(df.format(timePeriod.begin)))
sum += timePeriod.getLiveDuration();

