public ColumnCostData(int type, int length, int numOfCols, int floors){
if (type == 1)
cost = SCCost(length, numOfCols, floors);
else if (type == 2)
cost = PCCCost(length,numOfCols, floors);
}

double SCCost(int length, int totalCols, int floors){

