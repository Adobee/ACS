Matrix1D colMatrixIndex, Matrix1D layerMatrixIndex, Matrix3D matrix) throws ModelException {
if (!checkData(rowMatrixIndex, matrix, 3) ||
!checkData(colMatrixIndex, matrix, 2) ||
if (o instanceof IndexedMatrix3D) {
IndexedMatrix3D imf = (IndexedMatrix3D) o;
boolean ok = super.equals(o);
if (this.layerMatrixIndex != null)

