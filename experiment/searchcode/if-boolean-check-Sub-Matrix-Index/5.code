int matrix[][];
if(parts.length==0)
return -1;
sub=parts[0].split(&quot;@&quot;);
m=parts.length;
n=sub.length;
matrix=new int[m][n];
array=new String[m][n];
for(i=0;i<m;i++)
{
if(parts[i].length()==0)

