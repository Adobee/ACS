if (frequency[1] == 0) {
frequency[1] = sum;
firstChar = lastChar;
}

else if (frequency[0] == 0) {
secondChar = firstChar;
firstChar = compareChar;
}

else if (sum > frequency[0]) {
frequency[0] = sum;

