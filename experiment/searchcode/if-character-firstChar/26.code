* @throws IllegalArgumentException se todos os caracteres se repetem.
*/
public static char firstChar(Stream input) {
final int ALL_CHARS_SIZE = Character.MAX_VALUE + 1;
if (counts[i] == 1 &amp; positions[i] < position) {
firstChar = i;
position = positions[i];
}
}
if (firstChar == -1) {
throw new IllegalArgumentException();
}
return (char) firstChar;
}
}

