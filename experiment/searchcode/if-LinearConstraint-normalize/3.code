import java.util.Set;


/**
* A LinearConstraint represents an equation or inequality. All termSet are on the left-hand side and
Term cancelingTerm = leavingConstraint.getTermFor(zeroOut);

if (cancelingTerm == null) {
throw new IllegalStateException(&quot;The LinearConstraint provided does not contain a &quot;

