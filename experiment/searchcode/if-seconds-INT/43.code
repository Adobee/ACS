package com.xetanai.rubix.entities;

public class Length {
private int raw_seconds;

public Length(int seconds) {
seconds -= hours*60*60;

int minutes = seconds/60;
seconds -= minutes*60;


String ret = &quot;&quot;;

if(weeks > 0)

