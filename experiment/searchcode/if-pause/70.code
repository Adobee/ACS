public void testPausing() throws Exception {
//test that the idle pause controller will not pause if there are less than 60 points that are &quot;idle&quot; and
//will pause if more than 60 points are &quot;idle&quot;
IdlePauseController idlePauseController = new IdlePauseController(new DoNothing(), new DoNothing());

