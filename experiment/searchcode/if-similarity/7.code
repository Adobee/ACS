public int compareTo(ArtistSimilarity o) {
if (similarity < o.similarity)
return -1;           // Neither val is NaN, thisVal is smaller
if (similarity > o.similarity)
return 1;            // Neither val is NaN, thisVal is larger

