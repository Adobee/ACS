public double data = 0;
}

// 检查计算的参数是否合法，也可以用于过滤一些重复的表达式
private boolean checkjisuan(double a, double b, int op) {
if (op == &#39;/&#39; &amp;&amp; b < 1e-6 &amp;&amp; b > -1e-6)
return false;
if (op == &#39;-&#39; &amp;&amp; b < 1e-6 &amp;&amp; b > -1e-6)

