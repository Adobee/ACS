int i = 0;
for (Diff aDiff : diffs) {
String text;
if (escapeSourceHtml)
//escape html tags
.replace(&quot;>&quot;, &quot;&amp;gt;&quot;).replace(&quot;\n&quot;, &quot;&amp;para;<BR>&quot;);
else
text = aDiff.text;

if (aDiff.operation == Operation.DELETE

