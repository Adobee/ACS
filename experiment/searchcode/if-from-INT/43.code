int N = gas.length;
int sumFromZero = 0;
int sumFromStart = 0;

int[] diff = new int[N];
for (int i = 0; i < N; i++) {
for (int count=0, i=start; count<N; count++) {
sumFromZero+= diff[i];
sumFromStart+=diff[i];
if ( sumFromStart< 0) {

