from = value;
fromInclusive = false;
} else if ( operator.equals( &quot;>=&quot; ) ) {
from = value;
fromInclusive = a.fromInclusive;
} else if ( a.from == null ) {
from = b.from;
fromInclusive = b.fromInclusive;

