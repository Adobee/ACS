} else if ( operator.equals( &quot;>&quot; ) ) {
from = new Date(value.getTime());
fromInclusive = false;
} else if ( operator.equals( &quot;>=&quot; ) ) {
from = a.from;
fromInclusive = a.fromInclusive;
} else if ( a.from == null ) {
from = b.from;

