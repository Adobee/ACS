public class LongParameter extends ElementParameter {

protected boolean mIsAmountLimited = false;
protected long mMinAmount = Long.MIN_VALUE;
mLongValue = Long.parseLong(mValue);

if (mIsAmountLimited &amp;&amp;
(mLongValue<mMinAmount ||

