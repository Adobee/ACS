public static boolean verifyColumn(String source){
boolean bool = false;
if(source == null || source.length() == 0){
bool = true;
}
return bool;
}

/**
* 比對兩個欄位資料是否相同
* @param one 欄位1
* @param two 欄位2
* @return boolean

