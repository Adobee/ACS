public static double simpson(Function f, double start, double stop, int n,
double tol) {
double step = stop - start;
int sign = (step < 0) ? -1 : 1;
if (sign < 1) {
public static double trapezoidal(Function f, double start, double stop,
int n, double tol) {
double step = stop - start;
int sign = (step < 0) ? -1 : 1;
if (sign < 1) {

