super(RigidBody.DYNAMIC);
}

public void initialize(double strength, double delay, Vector3d position,
super.stepPosition(timeStep);
if (super.getAge() > delay) {
double innerRadius = 0.4;
double outerRadius = 100d;

