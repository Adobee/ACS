import com.kickmogu.lib.core.constants.LabeledEnum;

/**
* 商品取得タイプ
*/
public enum FillType implements LabeledEnum<FillType, String>{
for (FillType element : values()) {
if (code.equals(element.code)) {
return element;

