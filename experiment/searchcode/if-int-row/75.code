private void QueenHelper(int n, int row, int[] colForRow, int[] count)
{
if (row == n)
private boolean check(int row, int[] colForRow)
{
for (int i = 0; i < row; i++)
{
if (colForRow[i] == colForRow[row] || Math.abs(colForRow[i] - colForRow[row]) == (row - i))

