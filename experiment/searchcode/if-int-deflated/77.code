final short[][] inflated = new short[sizeY][];
final short[] deflated = new short[sizeY];
int packed = 0;
for (int y = 0; y < sizeY; y++) {
packable = false;
break;
}
}
if (packable) {
deflated[y] = first;

