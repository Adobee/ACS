return metaLevel;
}
public int hashCode(){
return name.hashCode();
}
public boolean equals(Object o){
if(o == null)
return false;
if(o instanceof ExplainMetaName){
return this.name.equals(((ExplainMetaName)o).name);

