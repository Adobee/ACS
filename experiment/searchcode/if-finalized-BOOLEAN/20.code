FinalizedId finalizedIdByKey = finalizedIdsByKey.get(key);
if (finalizedIdByKey != null) {
if (Time.isTimedout(finalizedIdByKey.getFinalizedOn(), finalizedIdRetentionSeconds)) {
// Returns true if it finds the event id in the map data structure
public boolean isDuplicate(EngagementEvent event) {

