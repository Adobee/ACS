import java.util.List;

public class BooleanFlagItem<T> implements Comparable <BooleanFlagItem<T>>
{
private T item;
return createdList;
}

public int compareTo(BooleanFlagItem<T> o)
{
if(Integer.valueOf(item.toString()) < Integer.valueOf(o.getItem().toString()))

