public void expand (AABB aabb, double x, double y, double z) {
set(aabb);

if (x < 0) aabb.minX += x;
if (x > 0) aabb.maxX += x;
if (x > 0 &amp;&amp; bb.maxX <= this.minX) {
double amount = this.minX - bb.maxX;
if (amount < x) {

