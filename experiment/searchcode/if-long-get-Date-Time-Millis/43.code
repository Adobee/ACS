private static final Log logger = (Log) LogFactory.getLog(DatePostData.class);

private Long timeMillis;

protected DatePostData() {
public Object getUntypedValue() {
return getValue();
}

public Date getValue() {
if (timeMillis == null) {

