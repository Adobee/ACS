// Parse the input file.
if (inputFile != null){
numFrames = parser.parseNumFrames(inputFile);
accessOrder = parser.parseTest(inputFile);
System.out.print(&quot;\nSimulating ARB...&quot;);
runARB.simulate(numFrames, accessOrder);
}
else if(LRU){

