int threeSumClosest (int[] num, int target) {
int n = num.length, closest = 0;
boolean flag = false; //if or not closest has been initialized
mid = num[j];
sum = max + mid + min;
if (flag == false) {
closest = sum - target;
flag = true;

