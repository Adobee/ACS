private boolean isBuyBook;

public Order findClosestSellMatch(int price) {
if (orders.isEmpty())
// rest of the orders are too expensive
return closest;
else if (p > cp)
closest = o;
}
return closest;
}
}

