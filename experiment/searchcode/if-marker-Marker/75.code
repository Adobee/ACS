import com.google.android.gms.maps.model.MarkerOptions;

import net.apunch.scorecard.activity.HoleActivity;

import java.util.Collection;
// Track all other existing markers
for (AnimatedMarker marker : markers) {
if (!marker.getIdentifier().equals(getIdentifier())) {

