String getNextURL();

/**
* adds URLs to the read list
* @param array of URLs
* @return true if successful
*/
Boolean putURLs(String[] URLs);
* @return true if successful
*/
Boolean putEmails(String[] Emails);

void beginScanning();

}

