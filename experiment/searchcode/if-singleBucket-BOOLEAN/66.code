for (List hash1 : hash)
{
List<KeyValuePair<K,V>> singleBucket = hash1;
if (singleBucket != null)
List<KeyValuePair<K, V>> singleBucket = hash1;  //make a bucket of the current item
if (singleBucket != null) // if theres something in the bucket

