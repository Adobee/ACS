return i == digits.length() ? &quot;0&quot; : digits.substring(i);
}

private boolean isValid(String digits) {
if (digits == null || digits.length() == 0) {
res.append(singleBucket(num, i == 0));
}
return res.toString();
}

private String singleBucket(int num, boolean isFirst) {
StringBuilder curr = new StringBuilder(String.valueOf(num));

