public class MyCanvasScaleListener extends SimpleOnScaleGestureListener {

private float mScaleFactor = Config.MIN_ZOOM;
private float focusX;
private float focusY;
focusY = detector.getFocusY();

if (lastFocusX == -1)
lastFocusX = focusX;
if (lastFocusY == -1)
lastFocusY = focusY;

