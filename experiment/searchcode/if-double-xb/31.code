private double getAvg(ArrayList<Double> input){
if(input == null){
return (y.size() - 1)/2;
return (sum / input.size());
}
}

private void calculate(){
double xb = getAvg(x);
double yb = getAvg(y);

