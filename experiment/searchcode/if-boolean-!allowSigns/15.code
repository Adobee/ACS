public static String toHexString(byte[] data, int off, int length, String separate, boolean uppercase)
{
if (off < 0 || off > data.length || length > (data.length - off))
* @return <code>true</code> if the string is a correctly formatted number
*/
public static boolean isNumber(String s)
{
if (StringUtils.isBlank(s))

