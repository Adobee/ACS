return arrayOfType;
}

private Type b(Type paramType)
{
if (paramType == null) {
return null;
}
return a(paramType);
}

Type a(Type paramType)
{
g.a(paramType);
if ((paramType instanceof Class)) {}

