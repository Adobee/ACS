public static float angle(Vector3 a, Vector3 b) {
double vDot = dot(a, b) / (a.magnitude() * b.magnitude());
if (vDot < -1.0) {
return mul(-1);
}

public Vector3 normalize() {
float length = magnitude();
if (length != 0.0f) {

