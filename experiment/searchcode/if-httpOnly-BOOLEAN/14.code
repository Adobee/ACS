private boolean secure = false;
private boolean httponly = false;
private int maxage = 0;
private Date expires;
setSecure(true);
} else if (sec.equals(&quot;HttpOnly&quot;)) {
setHttponly(true);
} else {
int eqPos = sec.indexOf(&#39;=&#39;);

