public static void deleteFile(File file, File topDirectory, boolean ignoreFailure) throws IOException {
if (file.getParent().startsWith(topDirectory.getPath()))
public static void cleanDirectory(File directory, File topDirectory, boolean ignoreFailure) throws IOException {
if (directory.getParent().startsWith(topDirectory.getPath()))

