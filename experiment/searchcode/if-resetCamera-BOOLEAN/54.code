cam = (OrthographicCamera) stage.getCamera();
resetCamera();

new ZoomPan(stage, 1.04f, 0.6f);
if (breakLoop) break;
}
}

public void loadMultiHint() {
screen.sceneManager.resetCamera();
boolean breakLoop = false;

