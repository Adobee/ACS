private final String RESET_CAMERA = &quot;Reset camera&quot;;
private boolean resetCamera;

public ReloadFileChooser(AkuShaperSimulationFrame simFrame) {
super.actionPerformed(e);
if (e.getActionCommand().equals(RESET_CAMERA)) {
resetCamera = !resetCamera;

