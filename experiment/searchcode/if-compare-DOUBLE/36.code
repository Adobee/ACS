Summ,Dif,Mul,Div,Min,Max;

public double getElement(double a, double b){
if (compareTo(Summ)==0) return a+b;
if (compareTo(Min)==0) return Math.min(a,b);

if (compareTo(Max)==0) return Math.max(a,b);

return Double.NaN;
}
}

