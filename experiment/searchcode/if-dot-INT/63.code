int tx = (int) ((Math.random() * 1000) % COL);
int ty = (int) ((Math.random() * 1000) % ROW);

if(getDotPoint(tx, ty).getStatus() == DotPoint.STATUS_OFF) {
private int getDistance(DotPoint dot, int dis) {
int distance = 0;

if(isInEdge(dot))
return 1;

