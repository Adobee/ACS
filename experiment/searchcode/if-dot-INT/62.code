ArrayList<Dot> leftSide = new ArrayList<Dot>();
for (int i = 0; i < this.dots.size(); i++) {
Dot dot = this.dots.get(i);
private void getHull(Dot a, Dot b, ArrayList<Dot> field) {
int insertPosition = hull.indexOf(b);
if (field.size() == 0)

