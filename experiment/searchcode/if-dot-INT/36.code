setSize((int) (5 * Units.em), (int) (5 * Units.em));
}

@Override
public void update(Object changed) {
if (changed instanceof Integer) {
g.setColor(Color.BLACK);
int dotWidth = (int) (getWidth() / 5.0);
int dotHeight = (int) (getHeight() / 5.0);

