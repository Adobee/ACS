int allDotHeight = dotRadius * 2;

// 偏移量
int offsetWidth = 0;
int offsetHeight = 0;
if (canvasHeight >= allDotHeight) {
offsetWidth = 0;
}

// 开始画点
for (int i=0; i<dotNumber; i++) {
if (i == dotSelected) {

