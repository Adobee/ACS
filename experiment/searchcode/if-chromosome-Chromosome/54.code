cumulativeProbability += chromosome.getRouletteWeight();
if (rand <= cumulativeProbability) {
selection = new Chromosome(chromosome);
break check;
}
}
return selection;
}


//determines if there is a solution in the specified population

