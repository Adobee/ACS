package GA.mutation;


import GA.engine.chromosome.Chromosome;

/**
* Меняет местами два соседних гена. Выбранный (случайно), и слева от него
int index = (int)(Math.random() * (chromosome.getGeneticСode().length - 1));
int tmp;
if (index == 0){

