* Muta un cromosoma usando un esquema de mutaci—n de un solo punto, usando
* probabilidad fija
*
* @param chromosome
*            El cromosoma a mutar
for (int i = 0; i < chromosome.getChromosome().length; i++) {
value = Math.random();
if (value < mutationProbability) {

