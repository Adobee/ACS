synchronized (mapCallableList) {
ArrayList<Callable<?>> callableList = mapCallableList.get(tag);
if (callableList == null) {
ArrayList<Callable<?>> callableList = mapCallableList.get(tag);
if (callableList != null) {
for (Callable<?> callable : callableList) {

