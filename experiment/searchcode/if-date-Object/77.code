package huebener.gameday.object.util;

import java.text.DateFormat;
import java.util.Calendar;
public static String dateStr(Date date) {
if (null == date) {
return &quot;&quot;;
}
return DateFormat.getDateInstance(DateFormat.SHORT).format(date);

