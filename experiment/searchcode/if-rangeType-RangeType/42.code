* 包括：等于、不等于、包含、不包含、大于、大于等于、小于、小于等于、起止范围
* @author Orion
*
*/
public enum RangeType {
/* 等于 */
EQUAL,
/* 不等于 */
UNEQUAL,
/* 包含 */
INCLUDE,
RangeType[] rangeValues = RangeType.values();
for (RangeType rangeType : rangeValues) {
if (rangeType.name().equals(value))
return rangeType;
}
return null;
}
}

