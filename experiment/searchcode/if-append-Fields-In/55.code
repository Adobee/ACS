for ( int i=0; i<fields.length; i++ ) {

buffer.append( fields[i] );

if ( i < fields.length - 1 ) {
buffer.append( &#39;?&#39; );

if ( i < numFields - 1 ) {
buffer.append( &#39;,&#39; );
}
}

buffer.append( &quot;);&quot; );

