Integer[] xSamplesCopy = new Integer[numVisibleNodes + 1];

for (int i = 1; i <= numVisibleNodes; i++)
xSamplesCopy[i] = xSamples[i];
for (int i = 1; i <= numVisibleNodes; i++) {
if (i%2 == 0) {
xSamples[i] = 1;

