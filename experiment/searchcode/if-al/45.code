public void push(t obj)
{

al.add(obj);

}

public t pop()
{
t obj = null;
if(!al.isEmpty())
{
obj=al.get(al.size()-1);
public t peek()
{
t obj = null;
if(!al.isEmpty())
{
obj=al.get(al.size()-1);
return obj;

