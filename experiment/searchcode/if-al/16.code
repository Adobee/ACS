public static void mergeSort(int[] al,int p,int r)
{
if (p<r)
{
int q=(r+p)>>1;
mergeSort(al, p, q);
mergeSort(al, q+1, r);
merge(al,p,q,r);
int[] temp=new int[r-p+1];

while (i<=q&amp;&amp;j<=r)
{
if(al[i]<al[j])
temp[k++]=al[i++];
else

