for (int k = 1; k < 100; k++){
for (int i = 1; i < 5000; i++){
if(isPan(k, i, k*i))
al.add(k*i);
String s = &quot;&quot;;
s+=a+ &quot;&quot; + b + &quot;&quot;+ c;
if (s.length()!=9){
return false;
}
Set<Integer> al = new HashSet<Integer>();
al.add(1);

