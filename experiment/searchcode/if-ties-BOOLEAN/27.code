super.clear();
ties.clear();
}

@Override
public E peek() {
if (ties.isEmpty()) {
return super.peek();
public E poll() {
if (ties.isEmpty()) {
return super.poll();
} else {
return ties.remove(ties.size() - 1);

