private int maxSize;

Queue(int maxSize){
this.maxSize = maxSize;
list = new int[maxSize];
front = rear = -1;
}
public void enqueue(int entry){
if ((front == maxSize) &amp;&amp; (rear == maxSize)){

