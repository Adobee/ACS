/*
* AnimationBoost
*
* Version 1.0
*
* */

package by.epam.epamlabs.beans;

public class AnimationBoost {
delay = MIN_ANIMATION_DELAY;
} else if(animationBoost != ZERO_SPEED) {
delay = (int)Math.round(MAX_ANIMATION_BOOST / animationBoost);
}
return delay;
}
}

