import GameObject.GameObject;
import PowerUps.GameBoost;

/*
* This class will update and render all game objects.  It does this by looping
this.boost.remove(boost);
}

public boolean containsHealthBoostObject() {
for (GameBoost b: boost) {
if (b.getID() == ID.HealthBoost) {

