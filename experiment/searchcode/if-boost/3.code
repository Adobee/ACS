import com.game.basic.Boost;
import com.game.main.Game;

public class BoostController {

private LinkedList<Boost> b = new LinkedList<Boost>();
for(int i=0; i<b.size(); i++){
tempBoost = b.get(i);
if(tempBoost.getBoostTimer() > 2000)

