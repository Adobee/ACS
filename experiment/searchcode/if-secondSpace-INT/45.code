public void setNames(String line) {
int firstSpace = line.indexOf(&quot; &quot;);
int secondSpace = line.indexOf(&quot; &quot;,firstSpace+1);
if (secondSpace != -1) {
setLastName(line.substring(secondSpace+1));
} else {

