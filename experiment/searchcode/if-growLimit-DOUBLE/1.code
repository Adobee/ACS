int baiduViewGrowlimit = getParameterInt(&quot;baiduViewGrowlimit&quot;, 0);


double view_weight = viewWeight.doubleValue();
double view_wiggle = viewWiggle.doubleValue();
if (view_weight <= 0) {
return adminErrorResolution(&quot;站内目标系数不能小于等于0&quot;);

