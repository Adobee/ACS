viewTotal = new BigDecimal(String.valueOf(viewTotal)).setScale(3, BigDecimal.ROUND_HALF_UP).doubleValue();
int viewGrowlimit = videoViewRankCache.getVideoElementGrowLimit(weekTimeCode, videoId);
repasteTotal = new BigDecimal(String.valueOf(repasteTotal)).setScale(3, BigDecimal.ROUND_HALF_UP).doubleValue();
int repasteGrowlimit = videoRepasteViewRankCache.getVideoRepasteGrow(weekTimeCode, videoId);

