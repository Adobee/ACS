package fragment.submissions;

public class Fragment implements Combinable
{
private String fragment;
public int overlapNumber(Combinable other)
{
String otherFragment = ((Fragment)other).fragment;

if (this.fragment.contains(otherFragment))

