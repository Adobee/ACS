private FragmentManager mFragmentManager;

/** old fragment **/
private String oldFragment = &quot;&quot;;
/** if tag equals to active fragment than don&#39;t commit fragment **/
String activeFragment = fragment.getClass().getSimpleName();
if(!activeFragment.equals(oldFragment)) {

