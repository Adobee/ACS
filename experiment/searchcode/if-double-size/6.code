public static double mean(ArrayList<Double> list) {
int n = list.size();
double s = 0;
for (double a : list) s += a;
int n = list.size();
double m = mean(list);
double s = 0;
for (double a : list) s += (a - m) * (a - m);

