private int trials;
private int dim;

public PercolationStats(int N, int T) {
dim = N;
trials = T;
if (N <= 0 || trials <= 0) {
throw new IllegalArgumentException(&quot;Not a possible simulation&quot;);

