public void painter(char[] paint,char col)
{
if(paint.length==0)
return;
if(paint.length==1)
{
if(paint[0]==col) return;
for(int i=0;i<paint.length;i++)
{
for(int j=paint.length-1; j>i;j--)
{
if(paint[i]==paint[j] &amp;&amp; paint[i]!=col)

