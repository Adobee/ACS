import java.awt.*;

public class Line {

float gradient;
boolean vertical = false;

public Line(Point one, Point two){
if (two.x - one.x != 0) gradient = (two.y - one.y) / (two.x - one.x);
else vertical = true;
}

}

