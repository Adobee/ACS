Point d = new Point(b.x, a.y);

// 判断C点是否有元素
if (map[c.x][c.y] == -1) {
boolean method1 = horizon.horizon(a, c) &amp;&amp; vertical.vertical(b, c);
return method1;
}

// 判断D点是否有元素
if (map[d.x][d.y] == -1) {

