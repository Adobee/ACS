if (DAYS[i].equalsIgnoreCase(possibleDay)) day = DAYS[i];
}
int calDay = getCalendarDay(day);

if (calDay > -1) {
extractedDate = givenTime.getTime();
int stopAt = timeOfDay.length() + 1;
if (day != null) stopAt += day.length() + 1;

