int q = points.length - 1;
while( h != q ) {
System.out.println(h+&quot; &quot;+q);
if (points[h].x > points[q].x) {
swap(points,h,q);
printPoints(points);
q = q - 1;
if (points[h-1].x > points[h].x) {

