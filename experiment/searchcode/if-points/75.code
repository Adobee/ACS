for (q = p + 1; q < points.length; q++) {
for (r = q + 1; r < points.length; r++) {
if (0 == points[p].SLOPE_ORDER.compare(points[q],
for (s = r + 1; s < points.length; s++) {
if (0 == points[p].SLOPE_ORDER.compare(
points[r], points[s])) {

