int low = 0, high = nums.length - 1;
while (low <= high) {
int mid = (low + high) / 2;
if (target < nums[mid]) {
return low;
} else {
return low;
}
}
int mid = low + (high - low) / 2;
if (target < nums[mid]) {

