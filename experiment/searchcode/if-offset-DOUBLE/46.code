protected final int perm[] = new int[512];
protected double offsetX;
protected double offsetY;
protected double offsetZ;
protected float persistence = 0.175f;

public static int floor(double x) {
return x >= 0 ? (int) x : (int) x - 1;

