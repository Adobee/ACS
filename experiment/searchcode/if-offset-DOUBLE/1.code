drawDiagram(selection, 0.5 / N, 1.0 / N * i, 1.0 / N * j);
}
}
}
}

public static void diagram1(double r, double offsetX, double offsetY)
StdDraw.filledSquare(1.75 * r + offsetX, 1.75 * r + offsetY, 0.25 * r);
}

public static void drawDiagram(int i, double r, double offsetX, double offsetY)
{
if (i == 1)			diagram1(r, offsetX, offsetY);

