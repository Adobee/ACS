path = path.substring( path.lastIndexOf(&quot;/&quot;) + 1 ); // get file part
int extIndex = path.indexOf(&quot;.&quot;); // file extension index
if( extIndex == -1 ) { extIndex++; } // return empty
int extIndex = path.indexOf(&quot;.&quot;); // file extension index
if( extIndex == -1 ) { return &quot;&quot;; }

