int extIndex = fileName.lastIndexOf(&quot;.&quot;);
StringBuilder builder = new StringBuilder();
if (extIndex == -1) {
return builder.append(fileName)
if (StringUtil.isEmpty(fileName)) {
return &quot;&quot;;
}
int extIndex = fileName.lastIndexOf(&quot;.&quot;);
String newFileName = fileName;
if (extIndex > 0) {

