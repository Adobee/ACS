return null;
}
int extIndex = path.lastIndexOf(&#39;.&#39;);
if (extIndex == -1) {
int extIndex = path.lastIndexOf(&#39;.&#39;);
if (extIndex == -1) {
return path;
}
int folderIndex = getFileSeparatorIndex(path);

