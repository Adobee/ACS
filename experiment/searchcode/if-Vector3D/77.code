double length=Math.sqrt(Math.pow(x,2)+Math.pow(y,2)+Math.pow(z,2));
if(length==0){
return new Vector3D(0,0,0);
}else{
return new Vector3D(x*l/length,y*l/length,z*l/length);
}
}

public Vector3D fixValueX(double t){
if(x==0){

