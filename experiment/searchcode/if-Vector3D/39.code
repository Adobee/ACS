public Vector3d sumVector(Vector3d vector3d)
{
if (vector3d != null)
{
return new Vector3d(this.x + vector3d.getX(),this.y + vector3d.getY(), this.z + vector3d.getZ());
return null;
}
}

public int scalarProductVector(Vector3d vector3d)
{
if (vector3d != null)

