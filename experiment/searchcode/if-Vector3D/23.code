package engine.util;
import java.util.Vector;

public class Vector3d {
public double x, y, z;
new Vector3d(z * v.x, z * v.y, z * v.z));
}

public Vector3d normalize() {
double l = length();
if(l == 0)

