public abstract class IntegerNumber extends Number implements Integral
{
public abstract int intValue();

//	public abstract Number getValue();

public long longValue()
//      int y = x.intValue();
//      if (intValue() < y) return -1;
//      if (intValue() == y) return 0;
//      return 1;
//   }
}

