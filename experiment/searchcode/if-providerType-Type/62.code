import br.net.woodstock.rockframework.core.utils.Strings;
import br.net.woodstock.rockframework.crypto.ProviderType;
import br.net.woodstock.rockframework.crypto.store.KeyStoreType;
keyStore = KeyStore.getInstance(this.keyStoreType.getType(), this.provider);
} else if (this.providerType != null) {
keyStore = KeyStore.getInstance(this.keyStoreType.getType(), this.providerType.getType());

