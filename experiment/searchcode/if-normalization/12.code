return path;
}

Deque<String> queue=new LinkedList<String>();
String normalization=path;

if(normalization.length()>1&amp;&amp;normalization.charAt(normalization.length()-1)==&#39;/&#39;)
normalization=normalization.substring(0,normalization.length()-1);
}

if(path.length()<=0)
{
return path;
}

if(normalization.charAt(0)==&#39;/&#39;)

