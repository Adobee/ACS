private FloatingCategories categories = new FloatingCategories(80);

private final SectionCategory[] history = {
for ( SectionCategory category : history ) {
categories.shift(category);
if ( index++ < 6 ) continue;

