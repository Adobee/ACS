return;
}
--position;
int itemCount = getItemCount(section);
if (position < itemCount) {
return getSectionHeaderViewType(section);
}
--position;
int itemCount = getItemCount(section);
if (position < itemCount) {

