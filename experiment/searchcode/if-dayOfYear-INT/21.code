public class DayOfYear implements Temporal<DayOfYear> {
private int year;
private int month;
private int day;
return !isBefore(other) &amp;&amp; !equals(other);
}

public int compareTo(DayOfYear other) {
if (isBefore(other)) {

