public boolean isKeyUp(String key) {
if (type != 3) {
return !localState.contains(key);
}
return !localState.contains(key);
}

public boolean isKeyDown(String key) {
if (type != 3) {
return localState.contains(key);

