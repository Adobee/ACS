* Output number, followed by string, followed by &quot;s&quot; if number == 1.
* @param n
* @param singular
return nn + &quot; &quot; + singular + extension;
}

/**
* Output number, followed by string, followed by &quot;s&quot; if number == 1.

