/** The cols. */
protected int cols;

/** The fuzziness. */
protected double fuzziness;

/** The heaviside. */
case SIGMOID:
double exp = Math.exp(-val / fuzziness);
return ((1 - exp) / (1 + exp));
case SIN:
if (val > fuzziness) {

