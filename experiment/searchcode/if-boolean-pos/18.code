{ posX = x; posY = y; }

public boolean isAt(double x, double y)
{
{ if(x < posX || x > posX + width) return false;
if(y < posY || y > posY + height) return false; return true; }
}

public boolean isColliding(Bounds2D b)

