package matching.subscriptionStorer.clusterStrategy;

import matching.subscriptionStorer.Subscription;
* Store subscription in cluster vector
*
* @param newSubscription the new subscription
* @return -2 if subscription added is a new one, x >= 0 (subscription id) if the subscription is a duplicate

