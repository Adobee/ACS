for(int j = 0; j < clusterRow.giveNumberOfClusters(); j++){
if(i != j &amp;&amp; clusterMethod.calculateDistance(clusterRow.giveCluster(i), clusterRow.giveCluster(j)) < min){
int clusterDepth = clusterRow.giveNumberOfElements() - clusterRow.giveNumberOfClusters();
for(int i = 0; i < clusterRow.giveNumberOfClusters(); i++){
if(clusterRow.giveCluster(i).giveDepth() == clusterDepth){

