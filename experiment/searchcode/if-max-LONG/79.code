public static void main(String [] args) {
Scanner key = new Scanner(System.in);
long n = key.nextInt();
long k = key.nextInt();
mid = min + (max-min) / 2;
long sum = sum(k,mid);
if (sum + mid >= n) {
v = mid;

