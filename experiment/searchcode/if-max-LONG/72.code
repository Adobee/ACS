long x = X;
for (int i = 1;; i++) {
long y = (x + ((n + x - 1) / x)) >> 1;
if (y == x)
return i;
return (long) Math.floor(t * t);
}

static long search(long a, long b, double d) {
if (a == b)
return f(a) > d ? a - 1 : a;

