double H, S, L;
double delta, minval, maxval;

minval = Math.min (R, G);
minval = Math.min (minval, B);
// Calculate Lightness
L = (maxval + minval) / 2.0;

if (minval == maxval) {
S = 0;
H = Undefined;

