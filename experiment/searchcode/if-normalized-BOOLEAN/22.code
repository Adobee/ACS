super(kernelCode, shape);
if(normalizedX == null || normalizedY == null) throw new NullPointerException();
this.normalizedX = normalizedX;
result = prime * result + ((normalizedY == null) ? 0 : normalizedY.hashCode());
return result;
}

@Override
public boolean equals(Object obj) {
if (this == obj)

