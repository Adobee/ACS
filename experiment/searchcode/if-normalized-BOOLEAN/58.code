int normalizedCol = 0;
int normalizedRow = 0;

public Player(int row, int col, boolean onGoal) {
return normalizedRow;
}

private void dfs(final int x, final int y) {
if (normalizedCol < y) {
normalizedCol = y;

