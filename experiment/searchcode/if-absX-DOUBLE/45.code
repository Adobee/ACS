} else {
double absx, c, p, q;
absx = Math.abs(x);
if(absx<=0.5) {
private static double nonexperfc(double x) {
double absx, c, p, q;
absx = Math.abs(x);
if(absx<=0.5) {
return Math.exp(x*x)*errf(x);

