double delta = slope-currentSlope;
double min = -1000;
double max = 1000;

if (delta<0) {
min = currentSlope-getMaxSlopeChange();
protected double slopeCorrection(double slope) {
if (!isOverwriteSlope())
return slope;

String formula = getSlopeFormula();

