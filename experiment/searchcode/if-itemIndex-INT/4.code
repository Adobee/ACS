int itemIndex;
//背包容量的下标，也就是行
int capacityIndex;
int itemLastIndex = weight.length - 1;
for (capacityIndex = 1; capacityIndex <= CAPACITY; capacityIndex++) {
if (capacityIndex < weight[itemIndex]) {
maxValue[itemIndex][capacityIndex] = maxValue[itemIndex - 1][capacityIndex];

