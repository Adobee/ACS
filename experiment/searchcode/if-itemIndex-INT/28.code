kappa2[userIndex] = q + N;
}

for(int itemIndex = 0; itemIndex < N; itemIndex++) {
tau1[itemIndex] = f * (1 - ratio + 2 * rand.nextDouble() * ratio);
delta += Math.abs((kappa1[userIndex] - k0) / kappa2[userIndex]);
}
}
for(int itemIndex = 0; itemIndex < N; itemIndex++) {
if(itemBias[itemIndex] != 0) {

