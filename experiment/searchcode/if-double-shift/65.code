public double compute (double[][] args, int i) {
data = args[0][i];
scale = args[1][i];
index = args[2][i];
shift = args[3][i];
if (data > shift) {
return Math.log(index/scale) +
(index-1) * Math.log((data-shift)/scale) -

