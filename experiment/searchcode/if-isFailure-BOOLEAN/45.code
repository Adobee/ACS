* the result of isPass
*/
public final boolean isPass() {
return( (! isFailure()) &amp;&amp; (! isError()) );
}

public boolean isFailure() {
return(failure != null);
}

public final boolean isError() {
return(error != null);

