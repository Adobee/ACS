double totalTermFrequency = termTotalFrequencyMap.get(term);

if (termList.containsKey(term)) {
termFrequency = termList.get(term);

double okapiForTerm = getOkapiTF(termFrequency,
double okapiBM25ForTerm = getOkapiBM25(documentFrequency,
termFrequency, documentLength, averageCorpusLength);
okapiBM25 += okapiBM25ForTerm;

if ((docNo.equals(&quot;AP891109-0261&quot;))

