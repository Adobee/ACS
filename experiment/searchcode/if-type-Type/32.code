package parameter;


public enum ParameterType {
Common(0),
Number(1);

private int mType;
for (ParameterType type : ParameterType.values()) {
if (type.getType() == id)
return type;
}

return ParameterType.Common;
}
}

