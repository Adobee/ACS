package figure;


public enum FigureType {
Undefined(0),
Line(1),
Ellipse(2),
Pie(3),
for (FigureType type : FigureType.values()) {
if (type.getType() == id)
return type;
}

return FigureType.Undefined;
}
}

