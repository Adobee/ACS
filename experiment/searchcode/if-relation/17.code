public enum Relation {
FAMILY(1), FRIEND(2), COWORKER(3), OTHER(4);

private int relationValue;
for (Relation relation : Relation.values()) {
if (value == relation.relationValue) {
return relation;
}
}
return OTHER;
}
}

