getAccess().getConsignmentTransportDb().deleteAllByConsignment(cId);

if ( _import.hasConsignmentTransports() )
{
int consignmentId = _import.getConsignment().getId();
AcFlight f,
AcPropertyRegistry pr)
{
int minutesOffset = pr.getReceiveMinutesBeforeDepart();

