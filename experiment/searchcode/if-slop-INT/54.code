public class Fast {
private static void merge(SlopInfo[] arr, SlopInfo[] aux, int lo, int mid, int hi) {
SlopInfo current = slops[0];
for(int j = 1, colinear = 1; j < slops.length; ++j) {
if( slops[j].slop == current.slop ) {

