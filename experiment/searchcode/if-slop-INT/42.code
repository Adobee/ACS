double slop = (points[j].y - points[i].y) / (points[j].x - points[i].x);
if(slopmap.containsKey(slop)){
slopmap.put(slop, slopmap.get(slop) + 1);
for(double k : slopmap.keySet()) {
slopmap.put(k, slopmap.get(k)+dup);
}
for(int v : slopmap.values()){
if(v > max){

