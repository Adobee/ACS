private ArrayList<DestinationButton> destButtonList;
private JPanel destPanel;
private boolean closed = false, dataChanged = false;
newDest.addActionListener(e -> {
Destination d = enterDestination();
if(d==null)
return;

