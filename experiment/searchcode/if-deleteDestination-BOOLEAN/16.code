mTripsListView.setItemChecked(position, true);
}
}
}
}).start();
}
}


private void deleteDestination(Long key){
if(mListener.deleteDestination(key)){
public void setSelectedDestination(Long key);

public Long addDestination(String name);

public boolean deleteDestination(Long key);

