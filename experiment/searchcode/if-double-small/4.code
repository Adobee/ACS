public static double get_max_interval(double[] in)
{
if (in.length <= 0)
return 0.0;

double small = in[0];
double large = in[0];
double interval = large - small;

for(int i = 1; i < in.length; ++i)

