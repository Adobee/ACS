for (int i = 0; i < _clusterValues.size(); i++)
{
if (solution.get(j)._index == _clusterValues.get(i)._index)
{
domainMatch[j] = i;
boolean domainMatches = true;
for (int j = 0; j < solution.size(); j++)
{
if (domainMatch[j] == -1)

