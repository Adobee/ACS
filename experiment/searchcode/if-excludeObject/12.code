*            The normal of the intersecting surface if an intersection is
*            found.
* @param excludeObject
*            If set, dynamic surfaces from this object will be ignored.
endPoint, tempHitPoint, tempHitNormal, movementDirection,
excludeObject)) {
if (hit) {
// Check to see whether this collision is closer to the one

