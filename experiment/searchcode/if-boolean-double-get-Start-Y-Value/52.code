return 0;

if(d <= max.doubleValue())
return this.getAnimationSteps()-1;

//start at 1 since 0 has been tested above
return 0;



if(d >= ((Number)this.getMax()).doubleValue())
return this.getAnimationSteps()-1;

//start at 1 since 0 has been tested above

