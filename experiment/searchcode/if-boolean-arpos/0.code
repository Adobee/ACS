public String[] dfs(String start)
{
int arpos = 0;
String[] dfs_array = new String[vertices.length];
Boolean[] visited = new Boolean[vertices.length];
//System.out.println(vertices[getPos(start)]);
dfs_array[arpos] = vertices[getPos(start)];
arpos++;

Boolean childfound = false;

