public void insert( long j ) {   //put items at rear of queue
if( rear == maxSize - 1 )
rear = -1;                 //後面加不下加到前面
public long remove() {
long temp = queArray[ front++ ];
if( front == maxSize )         //後面刪完刪前面

