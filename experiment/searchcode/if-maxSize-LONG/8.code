arr = new long[maxSize];
elems = 0;
font = 0;
end = -1;
}

// 插入数据
public void insert(long value) {
if (end == maxSize - 1) {
// 移除数据
public long remove() {
long tmp = arr[font++];
if (font == maxSize) {
font = 0;

