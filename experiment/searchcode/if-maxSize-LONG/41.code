nItems = 0;
}

/**
* 方法中并没有判断 队列满了的情况
* @param j
*/
public void insert(long j) {
if (rear == maxSize - 1) {
public long remove() {
long temp = queArray[front++];
if (front == maxSize) {
front = 0;
}
nItems--;
return temp;

