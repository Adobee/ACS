public class ResponseCode
extends DynamicEnum
{

private int m_nCode;

public static final ResponseCode CONTINUE = new ResponseCode(100);
for (ResponseCode loCode : loCodes)
{
if (loCode.getCode() == tnCode)

