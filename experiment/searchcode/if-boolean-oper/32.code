private Operator leftOper, rightOper;
private boolean infix, paren;

public BinaryOperator(String operation, String function, boolean infix, float x, float y, boolean paren) {
rightOper.addOperator(oper);
}

@Override
public boolean hasRoom() {
if (leftOper == null)
return true;

