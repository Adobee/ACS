int nicknameIndex = contact.indexOf(nicknameStr);
int emailIndex = contact.indexOf(emailStr);

// if nickname exist do this.
return EMPTY;
}
int smallestIndexOfDiff = indexOfDifference(strs);
if (smallestIndexOfDiff == -1) {

