int index;
int tempLength;
int returnLength = 0;
byte[] tempArray;

if (inputString == null) {
if (strs == null || strs.length == 0) {
return EMPTY;
}
int smallestIndexOfDiff = indexOfDifference(strs);
if (smallestIndexOfDiff == -1) {

