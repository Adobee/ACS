@Override
public boolean include( String element, String filter ) {

if ( element == null || filter == null ) return false;
CONTAINS {

@Override
public boolean include( String element, String filter ) {

if ( element == null || filter == null ) return false;

