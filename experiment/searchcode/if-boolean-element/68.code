private final Element element;

public ElementInfo(Element elem) {
if (elem == null)
throw new IllegalArgumentException(&quot;The given Element is invalid!&quot;);
return element;
}

public boolean hasSameElement(ElementInfo info){
return info.element.equals(this.element);

