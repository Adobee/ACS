uid = String.format(UID_FORMAT, trigger.getClientId(), trigger.getClassId());
//			boolean shouldPut = false;
//						log.error(&quot;Failed at removeMessageByCoorelationId for: {} from {}&quot;, uid, queueName);
//				}
//			}

boolean shouldPut = true;
if (shouldPut) {

