synchronized(trackPlaces) {
totalTrackedNum++;
boolean shouldPut = true;
if(Globals.traceElimination.get()) {
if(trackPlaces.size() > N) {
break;
}
}
}
}
if(shouldPut) {
trackedNum++;
for(int i = 0; i < N; i++) {
Place p = new Place();

