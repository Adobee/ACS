double d_lambda;
double [][] lambda_matrix = null;
double oldObjective;

boolean cache = true;

public SOGradPKL()
double[][] matrix = sck.getKernelMatrix(l);
if(traceNorm)
{
//compute trace
double trace = 0.;

