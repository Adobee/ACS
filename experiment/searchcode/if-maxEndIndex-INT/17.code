int maxEndIndex = 0;
int tempEndIndex = 0;

for (int i = 0; i < integers.length - 1; i++) {
tempEndIndex = i + 1;
}
}

int startIndex = maxEndIndex - maxCount + 1;
System.out.print(&quot;Longest: &quot;);

