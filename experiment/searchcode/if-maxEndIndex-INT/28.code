int maxCount = 0;
int tempCount = 1;
int maxEndIndex = 0;
int tempEndIndex = 0;

for (int i = 0; i < integers.length - 1; i++) {
maxCount = tempCount;
maxEndIndex = tempEndIndex;
}
int startIndex = tempEndIndex - tempCount + 1;

