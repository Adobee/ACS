copy = new LinkedList<Serialize>(members);
}
Serialize prev = null;
for (Serialize d : copy)
{
if (prev != null)
private List<Serialize> _invocationList = new ArrayList<Serialize>();
public static Serialize combine(Serialize a, Serialize b) throws Exception {
if (a == null)

