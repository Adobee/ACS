public static boolean intersecting(Line2f a, Line2f b)
{
Vector2f p1 = a.p1;
Vector2f p2 = a.p2;
float d = (p4.y - p3.y) * (p2.x - p1.x) - (p4.x - p3.x) * (p2.y - p1.y);
if (d == 0)
return false;
float inv_d = 1.0f / d;

