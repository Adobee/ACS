dy = dx + 3;
checkAndAdd(dx, dy);
dy = dy * -1;
checkAndAdd(dx, dy);
}
else if (dx > 0) {
checkAndAdd(dx, dy);
}
}
}

private void checkAndAdd(int dx, int dy) {
int x = position.getX()+dx;
int y = position.getY()+dy;

