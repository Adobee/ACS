ArrayList<Integer> ret = new ArrayList<Integer>();

for (int i = 2; i < n; i++) {
if (checkIfPrime(i)) {
ret.add(i);
}
}

return ret;
}

private static boolean checkIfPrime(int n) {
int dividers = 0;

