// This keeps track of the current pile
int currentPile = 0;
// Loop and check each pile. If possible add
while (currentPile < boxes.size() &amp;&amp; !added) {
// Increment pile counter
currentPile++;
}
// If not added check if there&#39;s space on the bottom and add
if (!added)

