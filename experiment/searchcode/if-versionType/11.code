import com.kickmogu.lib.core.constants.LabeledEnum;

/**
* バージョンタイプです。
*/
public enum VersionType implements LabeledEnum<VersionType, String>{
* @return
*/
public static VersionType codeOf(String code) {
for (VersionType element : values()) {
if (code.equals(element.code)) {

