for(VersionType type : values()){
if(type.id == id){
return type;
}
}
return PC;
}

public static VersionType getType(String typeStr){
for(VersionType type : values()){
if(type.toString().equalsIgnoreCase(typeStr)){

