public static int getScore( String[] sscore, int n ) {
int[] a = new int[ n ];
int ts = 0;
for ( int i = 0; i < n; i++ ) {
a[ i ] = geti( sscore[ i ] );
}
int i = 0;
while ( i < n ) {
if ( ( i + 3 ) == n &amp;&amp; ( ( a[ i ] + a[ i + 1 ] ) == 10 ) ) {

