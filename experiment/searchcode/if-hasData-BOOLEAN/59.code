public class CachedNetworkData extends Observable {

private boolean isActive;
private boolean hasData;
&quot;Skipping notifyObservers for API call: &quot; + type);

}
}

public void sendCachedData() {
if (hasData) {

