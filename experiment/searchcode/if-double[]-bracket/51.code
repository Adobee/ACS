*            Factor by which to expand bracket interval on each iteration.
*
* @return True if bracket successfully found The new bracket will be placed
*         in &quot;bracket&quot;.
MonadicFunction function, int maxIter, double expansionFactor) {
// Check for valid initial bracket.

if ((bracket == null) || (bracket.length < 2)

