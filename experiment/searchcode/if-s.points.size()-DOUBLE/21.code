this.L = L;

points.clear();

double l = L;
if ( hold ) l = lastL;
R[1][1] =  (double)Math.cos(angle);

Vector<double[]> rotated = new Vector<double[]>();
for ( int i = 0; i < points.size(); i++ ){

