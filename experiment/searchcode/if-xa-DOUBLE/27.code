protected enum Direction {
UP, DOWN, LEFT, RIGHT
}

protected Direction dir;

public void move(double xa, double ya) {
if (xa > 0)
dir = Direction.RIGHT;
if (xa < 0)
dir = Direction.LEFT;
if (ya > 0)

