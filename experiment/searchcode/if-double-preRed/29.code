int i, cg_iter;
double delta, snorm;
final double one = 1.0;
double alpha, f, fnew, prered, actred, gs;
delta = euclideanNorm(g);
final double gnorm1 = delta;
double gnorm = gnorm1;

if (gnorm <= eps * gnorm1)

