int i, cg_iter;
double delta, snorm, one = 1.0;
double alpha, f, fnew, prered, actred, gs;
int search = 1, iter = 1;
delta = euclideanNorm(g);
double gnorm = delta;

// CHANGED BY bchen: END

if (gnorm <= eps * gnorm1) search = 0;

