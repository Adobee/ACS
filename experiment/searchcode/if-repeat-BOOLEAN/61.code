protected String timezone;//done

protected int repeatType;//done
protected boolean repeatFinished;//done
try {
o = (BaseRepeatObject) super.clone();
if (startTime != null) {
o.startTime = (Date) startTime.clone();

