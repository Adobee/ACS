public OtherRelation(boolean bDelSrc, boolean bSepFolder)
{
m_repeatList = new StringList();
m_bDelSrc = bDelSrc;
String repeat = null;
if (index < m_repeatList.size())
repeat = (String)m_repeatList.get(index);

