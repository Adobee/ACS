private static final int _DEFAULT_REPEAT_COUNT = 0;

private boolean repeatForever = true;

private int repeatInterval = _DEFAULT_REPEAT_INTERVAL;
builder.withIntervalInHours(getRepeatInterval());
if(isRepeatForever())
builder.repeatForever();
else

