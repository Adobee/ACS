int maxLength = -1;
int maxStart = -1;
for (int n = 1; n < BOUND; n++)	{
long current = n;
int length = 1;
length++;
}
if (length > maxLength)	{
maxLength = length;
maxStart = n;
}
}
System.out.println(&quot;Max length: &quot; + maxLength);

