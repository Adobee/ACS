hp += toRead;
return toRead;
}

protected int skipHead(long n) {
int i = headHead();
if (i == -1) return i;
return toRead;
}

protected int skipTail(long n) {
int i = tailTail();
if (i == -1) return -1;
int toSkip = tail.length - tp;

