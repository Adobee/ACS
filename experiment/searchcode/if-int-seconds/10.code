SECOND (1);

private final int seconds;   // in seconds
Type(int seconds) {
this.seconds = seconds;
public static String secondsConversion(int seconds) {
String secondsToDate = &quot;&quot;;

if(Type.DAY.getSeconds() <= seconds) {

