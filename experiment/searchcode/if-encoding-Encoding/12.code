private static Map<Character, Integer> encodingMap = null;

/**
* If encoding map is not initialized, initializes the map then returns the
* decoded value of the passed character
* @return decoded value of the character
*/
public static Integer get(Character c) {
if (encodingMap == null) {

