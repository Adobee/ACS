package universe;


import utilities.statemachine.StateMachine;

public class ProtocolRole extends Object{
ProtocolRole tempRole = (ProtocolRole)_pRole;
if(tempRole.getProtocol().getName().equals(this.getProtocol().getName()) )

