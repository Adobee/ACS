int maxColTop = 0;
for (int i = 0; i < State.COLS; i++) {
if (top[i] > maxColTop) {
maxColTop = top[i];
int[][] field = s.getField();

int numHoles = 0;
for (int i=0; i<State.COLS; i++) {
for (int j = 0; j < top[i]; j++) {

