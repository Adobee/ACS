//			int oldLevel, newLevel;
//			Object o = child.getUserDatum(MINIMUMLEVELKEY);
//			if (o != null)
//				oldLevel = ((Integer) o).intValue();
int oldLevel, newLevel;
Object o = child.getUserDatum(MINIMUMLEVELKEY);
if (o != null)
oldLevel = ((Integer) o).intValue();

