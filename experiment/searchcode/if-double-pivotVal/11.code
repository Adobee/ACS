public static <T> void copy(double[] fill, T[] array, DoubleProcedure<T> processor) {
if (fill.length != array.length) {
throw new IllegalArgumentException(&quot;The lengths of the 2 arrays must be the same!&quot;);
// NOTE: do not store key - pivotVal because of overflows
if (key > pivotVal) {
min = pivot + 1;
max--;
} else if (key == pivotVal) {

