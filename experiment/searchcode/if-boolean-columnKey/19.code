market.add(new CorrectScoreMarket(correctScoreMarketId, prices));
}
}

return market;
}

private boolean hasValidPrice(Map<ColumnKey, Double> prices) {
private boolean hasRequiredCols() {
return (colMap.containsKey(ColumnKey.CORRECT_SCORE_00));

