return Objects.hash(type, name);
}

@Override
public final boolean equals(Object obj) {
if (obj == this) {
return true;
}
if (obj instanceof ColumnKey) {
final ColumnKey<?> that = (ColumnKey<?>) obj;

