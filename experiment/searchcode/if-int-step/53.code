int ans = solve(n, m);
System.out.println(ans);
}

static int solve(int n, int m) {
if (n >= m) {
static int BFS(int n, int m) {
int step[] = new int[m * 2 + 5];
Arrays.fill(step, -1);
Queue<Integer> Q = new ArrayDeque<>();

