baos.write(check_summ);
baos.write(etx);
baos.close();
if(is_debug){
System.out.println(&quot;Send string data: &quot;
baos.write(password);
baos.write(separator);
if (command_data != null &amp;&amp; command_data.length > 0) {

