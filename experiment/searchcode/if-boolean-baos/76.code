* @return
*/
public boolean checkWord(String dir, String word){
FileInputStream fis = null;
while ((length = is.read(buff))!= -1) {
baos.write(buff, 0, length);
if(baos.toString().contains(word));{

