public <T extends Comparable<T>> void sort(T[] array, int firstIndex, int lastIndex) {
int indexLowerElement = firstIndex;

for (int i = firstIndex + 1; i <= lastIndex; i++) {
if (array[indexLowerElement].compareTo(array[i]) > 0) {

