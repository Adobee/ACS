fi.ave = sum / K;
fi.firstIndex = 0;
fi.lastIndex = K - 1;
for (int i = 0; i < a.length - K + 1; i++) {
sum += a[j];
}
sumfi.ave = sum / K;
sumfi.firstIndex = i;
sumfi.lastIndex = i + K - 1;
if (fi.ave < sumfi.ave) {

