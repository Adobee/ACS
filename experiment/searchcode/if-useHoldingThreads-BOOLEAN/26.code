specifier|final
name|Lock
name|lock
decl_stmt|;
comment|/* a per thread boolean indicating the lock is held by it. only works when assertions are enabled */
name|useHoldingThreads
operator|=
literal|true
operator|)
assert|;
if|if
condition|(
name|useHoldingThreads
condition|)

