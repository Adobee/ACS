* naive solution
* 时间复杂度为O(n^2)
*
* 超时
*/
public int largestRectangleArea(int[] height) {
if(height == null || height.length == 0)
for(int i = 0; i < N; i ++){
int cur = height[i];
int count = 1;

for(int j = i -1; j >= 0; j --){
if(height[j] >= cur)

