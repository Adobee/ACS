import ua.com.ukrelektro.flight.enums.ListenerType;
import ua.com.ukrelektro.flight.ws.interfaces.WSResultListener;

public class Registrator {
mapListeners.put(listenerType, list);
}

if (!list.contains(listener)) {
list.add(listener);

