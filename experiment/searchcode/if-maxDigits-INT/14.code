public static void printGist(int[] arr) {
int[][] res = toPrintable(arr);
int maxDigits = maxDigits(arr);
for (int i = 0; i < res.length; i++) {
private static void printHeight(int i, int maxDigits, boolean left) {
if (left) {
int digits = maxDigits(i);

