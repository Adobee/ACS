private int karatsubaRecursive(int num1, int num2, Metric m){
if(displayCall) System.out.format(&quot;f(%d, %d)\n&quot;, num1, num2);
return num1 * num2;
int maxDigits = Math.min(Maths.floorLogN(num1, 10), Maths.floorLogN(num2, 10));
//int maxDigitsMid = (int) Math.floor(maxDigits); //1 op

