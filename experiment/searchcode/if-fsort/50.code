return commodityDao.findAllWithCode(key);
}else if(&quot;3&quot;.equals(type)){
return commodityDao.findAllWithFsortId(Integer.parseInt(key.trim()));
return commodityDao.findForPageWithCode(key, start, pageSize);
}else if(&quot;3&quot;.equals(type)){
return commodityDao.findForPageWithFsortId(Integer.parseInt(key.trim()), start, pageSize);

