//度数ソート
import util.java.Scanner;

class Fsort{

//度数ソート(配列要素の値は0以上max以下)
static void fsort(int[] a,int n,int max){
}while(x[i]<0);
}
int max=x[0];
for(int i=1;i<nx;i++)
if(x[i]>max) max=x[i];

fsort(x,nx,max);

