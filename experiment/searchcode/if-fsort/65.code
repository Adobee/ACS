Sort fsort = sortService.findById(commodity.getFsort());
if(fsort!=null){
fsort.setType(&quot;1&quot;);
sortService.update(fsort);
}
Sort ssort = sortService.findById(commodity.getSsort());
if(ssort!=null){
ssort.setType(&quot;1&quot;);

