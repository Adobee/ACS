public void setFilterProvider( FilterProvider filterProvider ) {
mFilterProvider = filterProvider;
if ( null != mFilterListener ) {
mFilterProvider.setFilterListener( mFilterListener );

if ( null != mFilterSet ) {

