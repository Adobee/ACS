input = input.replaceAll(&quot; &quot;,&quot;&quot;);
if (isHex(input)) num = input;
}

public Hex(long num) {
if (isHex(num)) this.num = &quot;&quot; + num;
}

//check if it can be a Bin
public static boolean isHex(String input) {

