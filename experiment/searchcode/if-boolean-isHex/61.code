private boolean valid(String input) {
if (&quot;&quot;.equals(input)) return true;

boolean ret = true;
it = new StringCharacterIterator(input);
char t = ci.first();
if (c != t) return false;

int start = col;
boolean ret = true;

