ImageView imageView = ((ImageView) trackView.findViewById(R.id.trackViewImage));
Image optimalImage = getOptimalImage(track.album.images);
if (optimalImage != null) {
* Pick the Optimal Image for the TrackAdapter Item.
* An Optimal Image is exactly 200 PX in Width, or it
* at is least 200 PX Width in Size. If no images are

