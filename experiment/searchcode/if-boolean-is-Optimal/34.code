for (int i = 0; i < n; i++)
input[i] = in.nextInt();

// trivial case:
if (isSorted(input))
System.arraycopy(a, br3, b, br3,         end-br3);
if (direction == FWARD)
{
if (isSorted(b))
{
optimalSteps = n;

