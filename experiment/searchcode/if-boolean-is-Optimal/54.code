boolean itemAdded = false;

// if there are no more remaining items, cart could be an optimal solution
// and is compared to the currently best solution in optimalCart
if (remainingItems.isEmpty()){
optimalCarts.updateOptimalCarts(cart);

