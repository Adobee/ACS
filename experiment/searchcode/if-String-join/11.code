* @param join
*            join
* @param alias
*            alias
*/
public Join(JoinType joinType, String join, String alias) {
* Generates the Jpql join part.
*
* @return Jpql join part
*/
public String toJpql() {
if (getJoins().isEmpty()) {

