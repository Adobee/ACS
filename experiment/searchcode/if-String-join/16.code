StringBuilder builder = new StringBuilder();
for (Join join : data)
{
if (builder.length() > 0)
private String buildJoin(Join join)
{
StringBuilder builder = new StringBuilder();
if (join.getJoinType() != null)

