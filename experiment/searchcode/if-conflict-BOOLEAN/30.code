this.tightness=tightness;
if (conflict) this.conflictMatrix=generateMatrix();
}
public Conflicts(boolean conflict,int numOfValues,boolean[][] conflictsMatrix){
public boolean hasConflict(int myValue,int otherAgentValue,boolean inverse){
if (hasConflict()){
if (inverse)
return conflictMatrix[otherAgentValue][myValue];

