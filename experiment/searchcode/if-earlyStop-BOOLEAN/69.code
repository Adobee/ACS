public enum MutType {MISSENSE, FRAMESHIFT, EARLYSTOP, INDEL, SILENT};

/*--------------------------- member variables --------------------------*/
after = AminoAcid.XXX;
type = MutType.EARLYSTOP;
} else
if(s2.indexOf(&quot;_&quot;) > 0) {
// insertion/deletion

