int[][] goldlbids;

int numframes;
int len;

SRLFeatureData sfd;
boolean addLoss;

double bestScore;
if (unchangedRuns >= converge)
stopped = true;

if (addLoss &amp;&amp; unchangedRuns >= earlyStop &amp;&amp; bestScore >= goldScore + 1e-6)

