int dIncX = prevX + 2; // double decrement
int decrX = prevX - 1;
int decrY = prevY - 1;
int dDecX = prevX - 2; // double decrement
dIncX = prevX;
}
if (prevY + 1 > 7) {
incrY = prevY;
}
if (prevX - 1 < 0) {
decrX = prevX;
}
if (prevX - 2 < 0) {

