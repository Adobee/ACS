private void choseLocationInArraysAndAppendToResult(StringBuilder result) {
if (random.nextBoolean()) {
result.append(third[random.nextInt(third.length - 1)]);
} else if (random.nextBoolean()) {
result.append(first[random.nextInt(first.length - 1)]);

