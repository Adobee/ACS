private double mu;
private int m;

private double rho;

private double L;
private double Lq;
d += C.fact[m] / C.fact[(m - i)] * Math.pow(lamda / mu, i);
}
return 1.0D / d;
}

public double Pn(int n) {
if (0 <= n &amp;&amp; n <= m) {

