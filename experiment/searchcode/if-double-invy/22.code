stopGrief.add(w);
}
}
}
}

if (!config.isDouble(&quot;RideSpeed&quot;)) {
rideSpeed = config.getDouble(&quot;RideSpeed&quot;, 0.6D);
if (!config.isDouble(&quot;Lifetime&quot;)) {
saveChanged = true;

