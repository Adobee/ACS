double[] ab = LinearRegression.bestFit(CameraAuto.imageTargetRatioData, CameraAuto.distanceData, CameraAuto.NumberOfCameraDataPoints);
double a = ab[0];
double b = ab[1];

if (!Double.isNaN(a)) {
// first pass: read in data, compute xbar and ybar
double sumx = 0.0, sumy = 0.0, sumx2 = 0.0;

