this.upperArm = l1;
this.lowerArm = l2;
}

public void doCalculations(double targetx, double targety)
{
//finds the distance to the target point
double distance = Math.sqrt((targetx * targetx) + (targety * targety));

