public static long GetN() {
return (long)1e9;
}

static final long DELTA = -1234567 + 2; // MUST be odd

public static long rotate(long i, long delta) {
public static long GetLeftNeighbour(long index) {
if (index % 2 == 0) {
return rotate(index, +DELTA - 2);

