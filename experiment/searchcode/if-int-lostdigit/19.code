result.shiftRight();
result.mant[mant.length-1] = rh;
final int excp = result.round(lostdigit);
if (excp != 0) {
rh = r / RADIX;
result.mant[i] = r - rh * RADIX;
}

int lostdigit = 0;
if (rh != 0) {

