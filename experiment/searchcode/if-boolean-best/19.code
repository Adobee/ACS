int m = g.vertices.length;
int[] path = new int[m];
boolean[] used = new boolean[m];
used[0] = true;

for (int i = 1; i < m; i++) {
int best = -1;
for (int j = 0; j < m; j++) {
if (!used[j] &amp;&amp; (best == -1 || g.distanceBetween(path[i-1],j)

