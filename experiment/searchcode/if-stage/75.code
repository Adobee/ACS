import javafx.stage.Stage;
import org.jetbrains.annotations.NotNull;
import org.jetbrains.annotations.Nullable;
@NotNull
Stage getStage() {
if (stage == null) {
throw new ZeusRuntimeException(&quot;JFXBaseLayout: Stage was not set&quot;);
}
return stage;
}
}

