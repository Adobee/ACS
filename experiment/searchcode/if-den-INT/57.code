private int num;
/**
* Dénominateur de la fraction
*/
private int den;

/**
* Constructeur par défaut 0/1
* @param num
* @param den
*/
public Fraction(int num, int den){
this.setNum(num);

if(den != 0)

