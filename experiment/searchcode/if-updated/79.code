public byte[] getBlob(int i) {
checkPosition();
synchronized (this.mUpdatedRows) {
if (isFieldUpdated(i)) {
synchronized (this.mUpdatedRows) {
if (isFieldUpdated(i)) {
String str = (String) getUpdatedField(i);

