* @return category, a root category, has not any values but a list of sub category and items if any
*/
public static CategoryData getParsed( String xml) {
// cat null means its an Item else its a category
if(cat == null) {
categoryData.items.add(getItemDataParsed(element, categoryData.id));

