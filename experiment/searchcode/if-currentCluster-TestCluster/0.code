private void getSolution(ArrayList<ConfiguredValue> solution, DefaultMutableTreeNode currentNode, String[] solutionString)
{
Cluster currentCluster = (Cluster) currentNode.getUserObject();
if (currentCluster._clusterDomains.size() > 0)
for (int j = 0; j < otherCluster.length; j++)
{
if (testCluster[i] == otherCluster[j])
{
containsVar = true;

