import javax.inject.Inject;

import ru.ivankob.habbitchart.app.model.MetricModel;
import ru.ivankob.habbitchart.domain.Metric;
public Collection<MetricModel> transform(Collection<Metric> metricsCollection) {
Collection<MetricModel> metricModelsCollection;

if (metricsCollection != null &amp;&amp; !metricsCollection.isEmpty()) {

