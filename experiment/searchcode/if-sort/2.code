for(int i = 0 ; i < a.length ; i ++){
a[i] = i * 1.0 ;
if(i % 2 == 0) a[i] = - a[i] ;
}
Sort.show(a);
//		SelectSort.sort(a) ;
// 		InsertSort.sort(a) ;
HeapSort.sort(a);
Sort.show(a) ;
boolean isSort = Sort.isSorted(a) ;
if(isSort == false){
System.out.println(&quot;NONONONONO&quot;) ;

