package com.sort;

public enum Sort {
BUCKET_SORT(&quot;BucketSort&quot;, &quot;Bucket sort&quot;, &quot;BUCKET_SORT_TROCAS&quot;),
for (Sort sort : values) {
if (sort.NAME.equals(name)) {
return sort;
}
}
return null;
}
}

