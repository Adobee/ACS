public boolean addDynamic(Dynamic dynamic) {
if (checkUser()) {
return this.dynamicList.addDynamic(dynamic);
} else {
return this.dynamicList;
}

public boolean delDynamic(Dynamic dynamic) {
if (checkUser()) {
return this.dynamicList.delete(dynamic);

