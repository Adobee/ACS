map = mapTable.get(MapType.TOP_WORLD);
if( map == null ){
map = new TopWorldMap();
mapTable.put(MapType.TOP_WORLD, map);
case TOWN:
map = mapTable.get(MapType.TOWN);
if( map == null ){
map = new TownMap();

