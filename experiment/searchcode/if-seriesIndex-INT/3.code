@Override
public Color getColor(int seriesCount, int seriesIndex, int valueCount, int valueIndex, String xValue)
{
if(seriesIndex<mColourSchemeColours.size())  return Color.decode(mColourSchemeColours.get(seriesIndex).getColourHex());

