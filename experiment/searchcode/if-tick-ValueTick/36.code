import com.positive.charts.util.TextAnchor;

/**
* A value tick.
*/
public abstract class ValueTick extends Tick {

/** The value. */
private final double value;
if (obj == this) {
return true;
}

if ((obj instanceof ValueTick) &amp;&amp; super.equals(obj)) {
return this.value == ((ValueTick) obj).value;

