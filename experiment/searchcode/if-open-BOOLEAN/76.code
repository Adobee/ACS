private boolean open;

public Element(int value, boolean open) {
this.value = value;
this.open = open;
}

public int value() {
return value;
}

public boolean hasNext(Element b) {
if(value < b.value()){

