* @param index Номер записи, получаемый из класса Main (от 1 и больше)
*/
public void removeNode(int index) {
int realIndex = index - 1;
if (realIndex <= notePad.size() &amp;&amp; realIndex >= 0) {
notePad.remove(realIndex);

