* @return
*/
boolean process(T obj, int index, int realIndex);
}

private static int realIndex(int index, int arraySize) {
for (int index = startIndex; index <= endIndex; index++) {
int realIndex = realIndex(index, array.length);
if (!looper.process(array[realIndex], index, realIndex)) {

