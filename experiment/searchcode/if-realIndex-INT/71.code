public StepResults(boolean isIndexEven) {
this.isIndexEven = isIndexEven;
}

public Step get(int index) {
int realIndex = mapIndex(index, isIndexEven);
public void set(int index, Step step) {
int realIndex = mapIndex(index, isIndexEven);
while(results.size() <= realIndex)

