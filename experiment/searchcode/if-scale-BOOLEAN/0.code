public boolean animate(double delta, boolean negative) {
if(negative) {
scale = scale - delta;
if(scale < 0) scale = 0;
return scale == 0;
}else{
scale = scale + delta;
if(scale > maxScale) scale = maxScale;

