private final int numberOfElements;
private int counter;

public Window(final int numberOfElements) {
elements = new ArrayList<>(numberOfElements);
public void addElement(final T instance) {
if (elements.size() < numberOfElements) {
elements.add(instance);

