int codeShifting = 0;
Boolean isNexLine = true;

String csText = &quot;&quot;;

for (String csLine : csLines)
{
csLine = csLine.trim();
csLine = csLine.replace(&quot;  &quot;, &quot; &quot;);
if (!csLine.isEmpty())
{
if (csLine.startsWith(&quot;}&quot;))

