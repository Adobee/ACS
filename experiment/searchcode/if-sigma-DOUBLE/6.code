return sigma;
}
public void setSigma (double s) {
if(s < MIN_SIGMA)
throw new IllegalArgumentException(&quot;Sigma needs to be at least: &quot; + MIN_SIGMA);
* where x^2 + y^2 = r^2 (i.e. lies on the edge of the circle with radius r).
* edge in (0; 1).
* If it&#39;s smaller then MIN_SIGMA, truncates it.
*/
public double getSigmaForEdge (double edge) {

