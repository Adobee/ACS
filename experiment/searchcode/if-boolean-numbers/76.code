* 判断扑克牌是不是顺子
*
* @param numbers
* @return
*/
public boolean isContinuous(int[] numbers) {
if (numbers == null)
for (int i = 0; i < numbers.length; i++) {
if (numbers[i] == 0) {
zero++;
}
}
// 统计间隔数
int i = 0;

