this.segments = segments;
if (segments[7]) setPreferredSize(new Dimension(PADDING*2, PADDING*2+LENGTH*2));
setBackground(Color.BLACK);

if (segments[0]) g2.drawLine(PADDING*2, PADDING, LENGTH, PADDING);
if (segments[1]) g2.drawLine(PADDING, PADDING*2, PADDING, LENGTH);

