Image image;
int padding = 0;

public ImageComponent(Image i, int padding)
{
this.image = i;
this.padding = padding;
g2.setPaint(getBackground());
g2.fillRect(0, 0, w, h);

int x = 0;
int y = 0;

if(iw + padding * 2 < w)

