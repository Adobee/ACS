public int countBuses(int speed, int[] positions, int[] velocities, int time) {
double passTime = 0;
int count = 0;
for (int i = 0; i < positions.length ; i++) {
} else {
passTime = (positions[i] * 1.0) / (speed - velocities[i]);
}
if(passTime >= 0 &amp;&amp; passTime <= time )

