public abstract class Enemy extends BaseEntity implements Pool.Poolable {

//if enemy was processed and scored
protected boolean processed;

public float getMinDistance() {
public abstract Enemy init(Missile missile, float groundLevel);

public boolean isProcessed(){
return this.processed;

