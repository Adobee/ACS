this.outOfOrder = outOfOrder;
this.size = size;
}

@Override
public int getDocId(int pos) {
if (topDocs == null) {
@Override
public float getScore(int pos) {
if (topDocs == null) {
topDocs = tdc.topDocs();

