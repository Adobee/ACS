denom /= factor;
}

public Number add(Number other) {
if (other instanceof Rational) {
return ans;
} else if (other instanceof Int) {
return new Rational(numer + denom * ((Int)other).value(), denom);

