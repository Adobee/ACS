this.h = h;
}

/**
*
* @param other the other entity to check collision with
* @return if the entity collides with the other entity
*/
public boolean intersects(Entity other) {
//LEFTUP CORNER
if(other.getX() >= getX() &amp;&amp; other.getX() <= getX()+getW() &amp;&amp; other.getY() >= getY() &amp;&amp; other.getY()<=getY()+getH()){

