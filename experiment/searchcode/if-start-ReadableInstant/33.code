import org.joda.time.ReadableInstant;
import org.springframework.social.twitter.api.Tweet;
import org.springframework.social.twitter.api.Twitter;
return count == 0 ? 0 : Double.valueOf(count) / NINETY_DAYS;
}

private int numberOfDaysBetween(Date currentDate, Date date) {
ReadableInstant start = new DateTime(date.getTime());

