public final class EventDateTimeUtils {
private EventDateTimeUtils() {
}

/**
* Determines if an instant is between a start (inclusive) and end (exclusive).
* @return Returns  0 if the instant is contained in the range: (getStart() <= instant < getEnd())
*         Returns -1 if the range comes before the instant: (getEnd() <= instant)

