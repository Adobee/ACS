public class Odometer extends AbstractCounter {

private int numdigits;
protected LinkedList<Counter> odometer;
public Odometer(int numdigits) {
super();

if (numdigits <=0){
throw new IllegalArgumentException(&quot;numdigits CANNOT BE SMALLER THAN ZERO&quot;);

