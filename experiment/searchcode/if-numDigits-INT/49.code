int numDigits = DigitCounter.count(i);
if (inc + numDigits >= next) {
int pos = numDigits - 1 - (next - inc - 1);
next *= 10;
nextIndex++;
if (next > 1000000) {
break;
}
}
inc += numDigits;
}

int prod = 1;

