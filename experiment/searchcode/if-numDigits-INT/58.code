private ArrayList[] buckets = new ArrayList[10];

public int[] makeData(int numDigits, int len) {
Random r = new Random();
int[] data = new int[len];
public void sort(int[] data) {
int numDigits=0;
/*	for (int i=0;i<data.length;i++)
if ( (int)(Math.log10(data[i])+1) > numDigits)

