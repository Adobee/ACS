long filter = (-1L) << subIndex;
if((filter &amp; set[index]) == 0){ //no entries in the part of the
index++;
return false;
}

public boolean isSubsetOf(IntSet other){
if(firstIndex > lastIndex) return true;
if(firstIndex < other.firstIndex || lastIndex > other.lastIndex)

