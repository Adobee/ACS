slots.add(new Slot());
}
}

public boolean isSlotAvailable(){
for(Slot slot : slots){
if(slot.isPlaceAvailable())
public Slot getSlot(){
for(Slot slot : slots){
if(slot.isPlaceAvailable())
return slot;

