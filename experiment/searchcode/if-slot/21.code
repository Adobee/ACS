import java.util.ArrayList;

import com.citelic.cache.Cache;
import com.citelic.utility.tools.WikiEqupSlotDumper.EquipSlot;
buffer.get(data, 0, length);
EquipSlot ep = new EquipSlot(id, SlotType.valueOf(new String(
data)));
if (slots.contains(ep))

