import com.rs.cache.Cache;
import com.rs.tools.WikiEqupSlotDumper.EquipSlot;
import com.rs.tools.WikiEqupSlotDumper.EquipSlot.SlotType;
EquipSlot ep = new EquipSlot(id, SlotType.valueOf(new String(data)));
if (slots.contains(ep))
slots.remove(ep);
slots.add(ep);

