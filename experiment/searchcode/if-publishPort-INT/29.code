//verify with ProcessorDetails if we can set the core&#39;s frequency
@Override
public void setCoreFrequency(int coreNo, int frequency)
for(int i=0; i<frequencies.length; i++){
if(i != coreNo){
if(Math.abs(frequencies[i] - frequency) > maxFrequencyGap){

