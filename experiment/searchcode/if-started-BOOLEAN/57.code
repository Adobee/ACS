package start;

public class StartTimer {
int timeLeft;
boolean isCompleted;
boolean isStarted;
public void update(){
if(isStarted)
timeLeft--;
if(timeLeft==0&amp;&amp;isStarted)
isCompleted=true;
}

public boolean stop(){

