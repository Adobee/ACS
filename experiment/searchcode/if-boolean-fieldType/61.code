* @return FieldType Type d&#39;un champs
*/
public static FieldType valueOf(Field field)
{
if (field == null)
type = FieldType.Decimal;
}
else if (field.getType().getName().equals(&quot;boolean&quot;))
{
type = FieldType.CheckBox;

