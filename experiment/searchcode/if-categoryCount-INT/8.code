prepareToDisplay();
}

@Override
protected void executeReport() {
int totalCount, categoryCount;
EntityPool pool = getController().getEntityPool();

totalCount = 0;

if (boxSystem.isSelected()) {
categoryCount = 0;

