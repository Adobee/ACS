foundNumber = false;

prepareFields();

for (int x = 0; x < 3; x++) {
if(iterateBlock(x)){
foundCound++;
private boolean isNumberAllowed(int blockCol, int blockRow,
int number, int fieldCol, int fieldRow) {

if (isNumberInCol(blockCol, blockRow, fieldCol, number)) {

