if (array.size() == n * 2) {
flushPar(array);
return;
}
if (isOpenParValid(array)) {
temp = copyArray(array);
temp.add(&#39;(&#39;);
printPar(temp);
}
if (isCloseParValid(array)) {
temp = copyArray(array);

