public boolean canMoveUp() {
return posX < 2;
}

public Node moveUp() {
int s[][] = copyArray(state);
if (canMoveUp()) {
public Node moveDown() {
int s[][] = copyArray(state);
if (canMoveDown()) {
s[posX][posY] = s[posX-1][posY];

