final int MAX_LEN;
int[] scales;
double[] unscaled;
int index;

final double SCALE;
double sumUnscaled;
int sumScale;
Arrays.fill(scales, 0);
}

private double getScale(int logScale) {
if (logScale == 0.0)
return 1.0;
if (logScale == 1.0)

