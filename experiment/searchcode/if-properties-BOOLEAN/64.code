public void createFlag(String name, boolean Status) {
if(!getSubProperties(FLAGS).contains(name)){
getSubProperties(FLAGS).put(name, Status);
}
}

public void changeFlag(String name, boolean Status){
if(getSubProperties(FLAGS).contains(name)){

