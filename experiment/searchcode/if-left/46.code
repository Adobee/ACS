int right = nums.length - 1;

while (left < right) {
int mid = left + (right - left) / 2;

if (nums[mid] < nums[left]) {
right = mid;
continue;
} else if (nums[mid] > nums[right]) {
left = mid + 1;

