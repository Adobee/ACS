private final HashMap<String, TokenizedPostfix> _tokenizedEquations;

private final boolean _cacheResults;
* @param cacheResults
*            Whether or not to cache tokenized equations into a {@see
*            HashMap}. If this value is <code>true</code>, tokenized

