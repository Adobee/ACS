* Pass in the tokenized command that the user entered
* Note: this may be an empty list if the command was
* not created by the user
boolean firstTimeThru = true;
for(String str: tokenizedCommand) {
sb.append(str);
if(! firstTimeThru)

