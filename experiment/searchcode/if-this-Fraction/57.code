int gcd = gcd(a, b);

this.a = a / gcd;
this.b = b / gcd;
}

public boolean equals(Object object) {
if(object instanceof Fraction) {
Fraction fraction = new Fraction(a++, i);

if(compare(fraction, min) > 0 &amp;&amp; compare(fraction, max) < 0) {
set.add(fraction);

