int N = 1000000;

class Fraction implements Comparable<Fraction> {
int a, b;

Fraction(int a, int b) {
int gcd = gcd(a, b);
Fraction fraction = new Fraction(a, b);

do {
if(max.compareTo(fraction) < 0 &amp;&amp; fraction.compareTo(compare) != 0) {

