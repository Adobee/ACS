* specified confidence value and its corresponding standard deviation.
*/
private static final float[] confidenceLevels = { 0f, 0.001f, 0.005f,
return totalWeight
* (1 - (float) Math.exp(Math.log(CONFIDENCE) / totalWeight));
}
else if (leafError < 1 - Parameter.PRECISION)

