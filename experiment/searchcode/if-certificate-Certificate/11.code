CertificateManager certificateManager=CertificateManager.findById(certificate.certificateManagerId);
if(certificateManager!=null){
certificateManagerList.remove(certificateManager);
public static void save(Certificate certificate){
if(!StringUtils.isEmpty(certificate.certificateManagerId)){
int credit = 0;

