for (int i = 0; i < producingColonies.size(); i++) {
double newDistance = MathBox.getVectorDistance(position, producingColonies.get(i).getPlanet().getPosition());
if (newDistance < distance) {
distance = newDistance;

