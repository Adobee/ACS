private boolean hasScaled = false;
public HeaderLabel(String text) {
super(text);
public void paintBackground(Graphics g) {
super.paintBackground(g);

if (getIcon() != null &amp;&amp; !hasScaled) {

