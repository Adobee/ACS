public class RequestParser  {
private HttpServletRequest request = null;
private boolean isMultiPart = false;
throws RequestParserException {
if(isMultiPart)
return multiRequest.getContentType(name);
else

