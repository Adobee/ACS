public static LineNode removeMiddle(LineNode head) {
if (head == null || head.next == null || head.next.next == null)
return head;
LineNode next = head.next;
LineNode nextNext = head.next.next;
if (head.x == next.x) {

