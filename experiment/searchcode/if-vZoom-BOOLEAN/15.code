void setVmax(int v) {
vmax = 100 * ((v / 100) + 1);
vZoom = ymax / vmax;
}

double xmax = 400, ymax = 400;
double xdivs = 10, ydivs = 10;
double vmax = ymax;
double vZoom = 1;
double vwidth = vmax / vZoom;

int l = 0;

double stepWidth = xmax / steps;

