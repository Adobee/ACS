int dupnum = 0;
int dupindex = A.length;
for (int i = 0; i < A.length; i++) {
int iter = i;

if (dupnum != 0 &amp;&amp; A[i - 1] == dupnum) {
dupindex = A.length;
}
index = new boolean[M];
while (iter < dupindex) {

if (dupnum == 0 &amp;&amp; index[A[iter]]) {

