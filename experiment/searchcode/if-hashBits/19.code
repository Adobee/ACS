private BigInteger strSimHash;

private int hashbits = 128;

public SimHashExample(String tokens) {
this.tokens = tokens;
for (int i = 0; i < this.hashbits; i++) {
if (v[i] >= 0) {
fingerprint = fingerprint.add(new BigInteger(&quot;1&quot;).shiftLeft(i));

