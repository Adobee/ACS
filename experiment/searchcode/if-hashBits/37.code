StringBuffer simHashBuffer = new StringBuffer();
for (int i = 0; i < this.hashbits; i++) {
if (v[i] >= 0) {
public static String getSimHash(String str,int hashbits)
{
if(str==null || &quot;&quot;.equalsIgnoreCase(str.trim()))

