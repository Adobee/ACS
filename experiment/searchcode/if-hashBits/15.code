int bit1 = hash1 &amp; (1 << i);
int bit2 = hash2 &amp; (1 << i);
if(bit1 != bit2){
distance ++;
//For simple reason, all weight are assigned as 1 in this method.
if(bit == 1){
hashBits[HASH_LENGTH-1-j]++;
}else{
hashBits[HASH_LENGTH-1-j]--;

