// Sums all board cells in a 90 degree area, centered at the diver&#39;s position and pointing in
// each of the eight directions.
// Used to determine, what direction the diver should move in over the long term.
// Note: board[0][0] is the top-left corner.  Increasing y-values move down.
if (dir == Direction.NE) {
for (int i = xpos + 1; i < board.length; i++) {

