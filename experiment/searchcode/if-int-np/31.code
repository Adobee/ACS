NumberProperties np = new NumberProperties();
np.number = readInt(&quot;Enter a number: &quot;);
np.evenNumber = np.number%2 == 0;
np.evenPreviousNumber = np.evenNumber;
if (np.evenNumber) {
np.number /= 2;
} else {
np.number = np.number * 3 + 1;

