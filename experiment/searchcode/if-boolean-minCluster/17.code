/** Variable to find out when changes occurred on this step **/
private boolean isWasChanged = true;

/** Private constructor to make this class final **/
minCluster = resultClusters.get(i);
}
}

if (isCenter) {
continue;
}

minCluster.add(cl);

