int cutoffNumber = 0;

if(desiredClusters > 4)
{

this.currentClusters.add(new Cluster(data.get(mid).subList(2, data.get(mid).size())));
ex.printStackTrace();
}

}

public boolean didChange()
{
double changed = 0.0;

if(this.currentClusters == null || this.currentClusters.size() < 1)

