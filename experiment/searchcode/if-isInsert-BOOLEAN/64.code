public GenericFormBean(String entityContext, Boolean _private, Class<T> entityClass, Class<PK> pkType) {
super(entityContext, _private, entityClass, pkType);
}

public boolean isInsert() {
public void doSalvar() {

try {

Boolean isInsert = isInsert();

if (isInsert) {
beforeInsert();
} else {

