if(node == null){
return -1;
}
boolean isInsert = false;
int index = -1;
int len = list.size();
list.add(i, node);
index = i;
isInsert = true;
break;
}
}
if(!isInsert){
list.add(node);
index = list.size() + 1;

