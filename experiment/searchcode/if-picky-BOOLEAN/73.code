private boolean Dragging;
private int mapwidth, mapheight, tx, ty;

public MapViewPane(int x, int y) {
dy -= ViewLocation.y;
g.drawImage(GrassTile, dx, dy, this);
if ((x == Selected.x) &amp;&amp; (y == Selected.y)) {

