* and open the template in the editor.
*/

package br.com.core1.engine;

import br.com.core1.components.clazz.JavaObject;
import com.google.common.base.CaseFormat;
StringBuilder sb = new StringBuilder();
sb.append(value);
int cont = 0;

while (matcher.find()) {
pos = matcher.start();
if (pos > 0)

