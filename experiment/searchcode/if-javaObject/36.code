@Override
public int compare(JavaObject o1, JavaObject o2) {
if(o1.getMembers().size() > 0) {
if(o2.getMembers().size() > 0) {
return copy;
}

public JavaObject filter(String txt) {
if(this.hasChildren()) {
ArrayList<JavaObject> toRemove = new ArrayList<>(this.getMembers().size());

