private int skipFirstLines = 0;
private Boolean hasHeader = false;
private List<String[]> list = new ArrayList<String[]>();
private void initialize(CSVReaderInterface context, BufferedReader reader, int skipLines, Boolean hasHeader) throws Exception {
if (!(context instanceof CSVReaderInterface)) {

