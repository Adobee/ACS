roots = nextRoots;
i++;
}
if( interval[1] != axis) {
s = new Line2D.Double((i - 1) + axis,-roots[0] + axis
roots = e.getValue(-axis);
i = -axis + 1;
boolean skip = false;
while( i < axis ) {
if( i == interval[1]) {

