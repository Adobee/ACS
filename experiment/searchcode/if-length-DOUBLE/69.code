public static double dot(double[] x,double[] y)
{
//向量点乘
double a=0;
if(x.length!=y.length)
{return a;}
for(int i=0;i<x.length;i++)
int P=b[0].length;//列数
double[][] c=new double[M][P];
if(N!=b.length){}//1矩阵的列数不等于2矩阵的行数，无法相乘
for(int i=0;i<M;i++)

