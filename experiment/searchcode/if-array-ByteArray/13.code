array = s.getBytes();
}
public RubberByteArray (byte[] byteArray) {
array = Arrays.copyOf(byteArray, byteArray.length);
public void Add (byte[] byteArray) {
if (byteArray == null || byteArray.length == 0) {
return;
}
int oldSize = array.length;

