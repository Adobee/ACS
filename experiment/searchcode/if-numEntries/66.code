Graphics g = getGraphics();
// Delete the zero when entrying
if (numEntries[0].equals(&quot;0&quot;)) {
numEntries[0] = &quot;&quot;;
}
// Only 2 digit numbers allowed
if (numEntries[0].length() > 1) {

