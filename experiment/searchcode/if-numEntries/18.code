System.out.println(url+&quot; already in cache, not added by thread &quot;+tid);
return;
}

if (numEntries == size)
victim = (victim + 1) % size;
}
else if (numEntries < size)
{
entry[numEntries] = new char[page.length];

