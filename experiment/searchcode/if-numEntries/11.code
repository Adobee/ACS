public boolean addEntry(String name, int iNo, boolean isLink)
{
if (numEntries < NUM_ENTRIES)
{
inodeNo[numEntries] = iNo;
for(int i=0; i<numEntries; i++)
if (inodeNo[i] == iNo &amp;&amp; this.name[i].equals(name))

