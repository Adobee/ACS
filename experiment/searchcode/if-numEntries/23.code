int newScore = e.getScore();
// array numEntries is full
if (numEntries == maxEntries)
System.out.println(&quot;Array numEntries is full&quot;);
if (numEntries - 1 > 0
&amp;&amp; newScore <= entries[numEntries - 1].getScore())
// Not a new High Score

