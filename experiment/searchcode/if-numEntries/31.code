int newScore = e.getScore();

// ��һ��Ҫ��find position��shift������һ��shiftһ��
if (numEntries < board.length || newScore > board[numEntries - 1].getScore()) {
if (numEntries < board.length)
numEntries++;

